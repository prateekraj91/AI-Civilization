# AI Civilization — Findings

## Day 10 (first emergent moment)

In a 40-turn run with three agents, Bob and Alex talked repeatedly and built mutual trust up to +5, while Alex's trust in Kira fell to -6 after their interactions went badly.

I never wrote any rule telling Alex to dislike Kira or to bond with Bob — those relationships emerged on their own from the talk → reaction → trust loop.

Watching a grudge form between agents I didn't script is the first time the simulation felt like a society rather than three separate programs.

## Day 11 (food scarcity)

I reversed the Day 9 abundance on purpose — INITIAL_FOOD 14 -> 3, and respawn went from "refill to 12 every turn" to a drip of ~1 food every 5 turns. Three agents each need a meal roughly every 7 turns, so demand (~0.43 food/turn) now outruns supply (~0.20 food/turn).

My first cut was scarce but TOO scarce in the wrong way: agents dispersed and starved alone in separate corners — 0 talks, 0 trust changes. Scarcity without a shared resource isn't competition, it's just three lonely deaths. Two small changes fixed it without making the world generous: I raised INITIAL_FOOD 3 -> 5 (they live long enough to meet) and CLUSTERED the scarce food into a central arena where they spawn. Now they converge on the same tiles and compete over them.

In a 50-turn Qwen run (qwen3:8b, think-off), a social split emerged on its own: Bob and Alex traded friendly messages and built mutual trust while Alex's exchanges with Kira turned hostile and his trust in her fell to -6. Food stayed scarce enough that agents still starved under the pressure. I scripted none of it — who allies, who feuds, who hoards, who starves — clustering a scarce resource was enough to turn dispersal into a contested commons.

## Day 12 (stealing + grudges)

I added a steal action — rob a neighbour's food when starving and you distrust them — riding the same strategy call as talk, so it cost zero extra inference. A theft drops the victim's trust by 5 and latches a permanent grudge flag, so no later friendliness can repair it.

The mechanic is verified by regression tests and a deterministic offline run: Kira steals from Alex twice, his trust slides -3 -> -8 -> -13, a later friendly message is REFUSED (you can't be liked back into good standing after stealing), and both memories are logged. The grudge then rides into Alex's every later prompt as "Kira: -13 (low, grudge)" — a betrayal is the first thing in this world that can't be undone.

The interesting part is who steals. Kira (independent, competitive) is the only personality that self-selects into theft — friendly Alex and cautious Bob only rob someone they already actively distrust, but Kira's survival-over-relationship wiring makes her a thief. I never named a thief; her personality made her one.

But under Qwen, competent play kept agents fed enough that the theft window rarely opened in the seeds I tried — which is itself the finding: when the model plays well, betrayal stays a genuine last resort, not a habit. In a clean 50-turn Qwen run no theft occurred at all — instead Kira simply out-competed Bob and Alex for the scarce central food and was the lone survivor at turn 50, while Bob died heading toward her food source.

## Day 13 (alliances + betrayal)

I added a mutual, two-sided alliance: one agent proposes via `ally_with_<name>`, and the bond only forms when the other answers with its own `ally_with`. Forming grants +3 trust **both ways**, logs an ALLIANCE event, and writes a memory on both. The benefit is mechanically real, not cosmetic: allies **share food sightings** — each contributes the food in its own perception window, so a pair sees more of a scarce map than either alone. The sharing is folded straight into the partner's strategy prompt ("Food your allies can see (shared with you): Alex sees food at (4, 3)"), and it costs zero new inference — ally and betray ride the same cached strategy call as talk and steal. Betrayal (`betray_alliance_<name>`) dissolves the bond, drops the betrayed agent's trust by 8 (bigger than theft's 5), latches a **permanent grudge** (reusing the Day 12 flag), and records a major memory on both. A grudge on *either* side blocks the pair from ever allying again, and a betrayed ally stops receiving shared sightings the instant the alliance ends.

**Who allies and who betrays is personality, not script.** Friendly Alex and cautious Bob ally readily — once talk has built trust to "high", one proposes and the other accepts. Independent/competitive Kira allies only reluctantly (she joins forces solely with someone she already actively trusts), and she is the only personality that will betray: under real survival pressure, starving beside an ally hoarding food, she renounces the alliance rather than keep paying into it. Friendly and cautious agents never betray the alliances they form.

**What I actually saw, by provider (being precise about provenance):**

- **Offline `random` provider, seed 48 — alliance + sharing emerged on its own.** Alex and Bob talked, trust climbed to "high", Bob proposed on turn 4, Alex accepted on turn 7, and the alliance formed (+3 both ways, event + memories logged). That same turn Bob — who could see no food himself — received Alex's private sighting of food at (4,3) through the shared-perception channel, verbatim in his next strategy prompt. The alliance is produced by the deterministic strategy *executor* (the `random` backend never itself emits "ally"); talk built the trust, and the executor turned it into a mutual bond. This is genuinely emergent — I never named who would ally.

- **Qwen (qwen3:8b, think-off) — no alliance formed in the seeds I ran.** Across the full verify run plus two probe seeds (48, 1), the agents talked only ~3 times each before scarcity scattered them, never reaching the trust an alliance needs; one agent (Kira on seed 48, Bob on seed 1) simply out-competed the others for the central food and survived alone. So under Qwen the *competitive collapse* keeps recurring — the same pattern as Day 12's no-theft runs: when one agent dominates the commons, nobody lives long enough beside a trusted neighbour to form a society. I did **not** observe a Qwen alliance, and I'm not claiming one.

- **Betrayal — verified only as a constructed deterministic scenario, not an organic Qwen event.** Because Kira so rarely enters an alliance at all (offline or under Qwen), I built the allied state through the real handlers, then let the real executor decide: starving Kira beside ally Alex-on-food emitted `betray_alliance_Alex` on her own. The alliance dissolved both sides, Alex's trust fell +7 → -1 with a permanent grudge, both memories recorded it, and the pair could no longer re-ally from either direction. The *mechanic* is real production code and is covered by regression tests; the *setup* is hand-built, and I say so in the harness output rather than dressing it up as something Qwen did.

The honest throughline across Days 11–13: scarcity reliably produces competition under Qwen, but cooperation (talk → trust → alliance) needs the agents to survive near each other long enough to build a relationship — and in these scarce-commons seeds, the strongest competitor usually ends that possibility before it starts. Alliances form readily once trust exists; getting to trust under Qwen scarcity is the hard part.

Day 13 (alliances + betrayal): Added mutual alliances (shared food sightings as the benefit) and betrayal (-8 trust, permanent grudge, no re-allying). Both verified by tests and deterministic runs. Under Qwen, no alliance formed on any seed I tried — same as theft in Day 12. The pattern is now clear: when the model plays competently, the dramatic social actions (steal, ally, betray) almost never fire on their own. Agents compete efficiently and one out-survives the rest; cooperation needs them alive near each other long enough to build trust, and scarcity usually ends that window first. Caveat: the alliance benefit only shapes LLM-driven turns, since the Python executor already navigates to all food globally.

## Day 14 (death event + respawn)

Death already existed since Day 6 (starvation flips `alive` and frees the cell). Day 14 turns it from a silent removal into something the *society* registers, and then refills the population. When an agent starves now: a clear `turn 47: Kira died (starved)` line goes to `events[]`, and EVERY agent that was alive at that moment records a bounded memory ("Kira died on turn 47 — they were starving."). Crucially, the survivors' *relationships* toward the deceased are left untouched — a grudge or an alliance with the dead persists as a remembered fact. The dead are remembered. (Pronoun is gender-neutral "they"; we can't infer gender from a name.)

Respawn is a true social cold-start. A death queues exactly one respawn for `death_turn + RESPAWN_DELAY` (10). When it comes due — and only while the living population is below `TARGET_POPULATION` (3) — a brand-new agent enters from a roster distinct from the starting cast (Mira/Otto/Zane/Nova), placed on a valid empty cell near the contested centre. It has blank memory, empty relationships, empty allies, hunger reset to 0, and a name no living *or dead* agent uses (reusing a dead name would silently graft the deceased's reputation onto the newcomer, since names key every relationship map). No one trusts it; it knows no one. Living agents record "A new agent, Mira, appeared on turn 57." The population rule keeps things bounded: each death is a deficit of one, each respawn fills one, surplus respawns (target already met) are dropped — N → N-1 → N, never unbounded.

Like every social mechanic since Day 8, this adds **zero** new per-turn inference. Death and respawn are pure Python over `world_state`; the newcomer's first turn triggers the same ordinary periodic strategy refresh every agent makes. The benchmark confirms it: ~0.21 strategy calls per agent-turn (offline and Qwen), unchanged from before — newcomers included.

**What I actually saw, by provider (being precise about provenance):**

- **Offline `random` provider, seed 7 — death + respawn ACTUALLY OCCURRED end-to-end.** Kira starved on turn 26; Alex and Bob both recorded the death, and Alex's prior distrust of Kira (trust -3) persisted afterward. On turn 36 (= 26 + 10, exactly) blank-slate Mira entered at the centre with empty memory/relationships and hunger 0; the survivors recorded her arrival. The full `main.py` run chains it naturally — Bob and Kira die, Mira enters turn 27, later Alex dies and Otto enters turn 36 — population repeatedly pulled back toward 3.

- **Qwen (qwen3:8b, think-off), seed 7 — same mechanic, regression-confirmed.** Bob starved on turn 17, both survivors recorded it, and Mira entered on turn 27 (= 17 + 10) as a blank slate. I ran Qwen ONCE as a regression check only — death and respawn are deterministic mechanics, not emergent events, so there is nothing to seed-search for. Per-turn inference was unchanged (0.221 calls/agent-turn).

- **Immediate participation — verified as a deterministic structural check.** "The newcomer can be observed and talked to" is a structural property (respawn registers a normal `Agent` on the grid), not an emergent one, so I prove it with a constructed fixture using the real respawn + talk code: a neighbour's `observe()` detects the fresh newcomer and `handle_talk()` delivers a message to its inbox. I deliberately do NOT rely on whether a competitive Qwen agent happens to talk to the newcomer before it scatters — in the Qwen seed-7 run, Mira died later without being talked to, which is exactly the kind of emergent outcome I'm not claiming as a mechanic test.

The honest throughline: Day 14 is a *mechanic*, fully deterministic, and verified offline and confirmed once under Qwen. The only emergent layer on top — who happens to befriend or rob the newcomer — remains rare under competent Qwen play, consistent with Days 12–13.

Day 14 (death + respawn): When an agent starves, survivors record the death in memory and a death event logs; after a delay, blank-slate newcomers enter knowing no one. This fired live under Qwen — Alex and Kira died at turn 17, Mira and Otto entered at 27 with empty memory and relationships, and Mira later died too, her death recorded by the survivors just like the originals. The world now has a continuous history: agents cycle in and out, but the living carry memory of the dead. Bob's end-of-run memory reads like a short biography of everyone he met and outlived.

## Day 15 (God mode CLI)

Day 15 adds an interactive God mode: the simulation can pause mid-run and drop into a menu (`AICIV_GOD_EVERY=N` pauses every N turns at a clean turn boundary; blank input resumes) where I can reach into the running world and change it, then watch the agents react on their own. All interventions live in a new `god_mode.py`, and the whole point is an **architectural boundary**: every function there reads/writes `world_state` and nothing else. God mode imports only `world` (which owns the state and its low-level mutators) and `population` (to reuse the Day 14 blank-slate spawn path) — and imports NO strategy, trust, conversation, alliance, personality, or llm. A regression test parses the module's AST and asserts the forbidden set is empty. That boundary is what makes a god intervention produce a *reaction* rather than a *scripted action*: I change the world, and the unchanged perception → strategy → executor loop does the rest.

Five commands, each mutating `world_state` only and each logging a tagged line to `events[]` (e.g. `turn 30: [GOD] drought triggered (20 turns)`) so cause→effect stays legible in the run log:
- `spawn_food x y` — add a food tile.
- `spawn_agent name personality` — summon a cold-start citizen through the exact Day 14 `spawn_blank_agent` path (empty memory/relationships/allies, hunger 0, unique name, announced to survivors).
- `trigger_drought [turns]` — set `drought_until = turn + turns` (default 20); `main.maybe_respawn_food` skips all respawn while `turn <= drought_until`.
- `drop_treasure x y [value]` — place a high-value item (default 10). The treasure is recorded in `world_state["treasures"]` AND **mirrored into the food list**, so the existing navigation loop already routes hungry agents to it with zero executor changes; claiming it (`eat`) pays out its full value as hunger relief (10 > a normal meal's 7) plus a wealth entry in inventory, and removes it from both lists. Because only one agent can occupy the tile, they race for it.
- `status` / `help` / `resume` — inspect, list, continue.

Zero new per-turn inference: god mutations are pure `world_state` edits. The benchmark holds at ~0.22 strategy calls per agent-turn even with interventions injected at turns 10/20/25, and a dedicated test confirms the four mutating commands add 0 LLM calls.

**What I actually observed agents do (deterministic `random` provider, seed 3 — these are mechanics, not emergent events, so one offline run is the substance; Ollama was down, so the Qwen smoke-check was a 4-turn `main.py` regression that ran clean):**

- **`spawn_food` drew a hungry agent within 2 turns.** Bob at (5,5), hunger 6, no other food on the map; God spawned food at (5,8). Bob walked south three turns straight onto it (`survival: head to food` → `survival: grab adjacent food`) — the executor read the changed `world_state["food"]` and navigated there. Nothing told Bob to go south.
- **`trigger_drought` zeroed respawn for exactly 20 turns.** Triggered at turn 30 → `drought_until = 50`. Every respawn tick inside the window (turns 35/40/45/50) added 0 food; the first tick to resume adding food was turn 55. Exactly the 20-turn span, visible in the log.
- **`drop_treasure` made agents converge and compete.** Three hungry agents equidistant (distance 2) from a treasure at (5,5), no other food. All three closed in; Alex reached it on turn 3 and claimed it — hunger dropped to 0 (relief 10 > meal 7), inventory `[{'treasure': 10}]`, and it vanished from both the treasure and food lists. The other two lost the race for the single tile, which is the contest.
- **In the real `main.py` loop, the reaction was immediate and unscripted.** Pausing at turn 3 and dropping a treasure on (5,5) — which happened to be Kira's tile — produced `Kira -> eat` on turn 4: she claimed it the moment her own senses reported it. The loop resumed uncorrupted at both pause points (turns 3 and 6), and a typo'd command in the menu was reported without crashing the session.

The throughline for Day 15: unlike the social mechanics of Days 11–14, this isn't about emergence at all — it's about a clean intervention surface. The discipline is the boundary (`god_mode.py` touches only `world_state`), and the payoff is that I never script a reaction: drop food, a drought, or a treasure into the single source of truth, and the existing loop makes the agents head for it, scatter, or race for it on their own. The deterministic runs show each cause→effect plainly; Qwen adds nothing to verify here beyond a smoke-check, so I didn't seed-search.

Day 15 (God mode CLI): Added an interactive pause-and-intervene menu in a new `god_mode.py` that mutates ONLY `world_state` (asserted by an AST import test) — spawn_food, spawn_agent (Day 14 cold-start path), trigger_drought (respawn→0 for exactly 20 turns), drop_treasure (value 10, mirrored into food so existing nav targets it, claim pays out > a meal). Every intervention logs a `[GOD]` event. Reactions are emergent through the unchanged perception→strategy→executor loop, not scripted: offline, a hungry Bob walked straight onto spawned food in 2 turns, three agents raced for a treasure (Alex won, hunger→0), and in the real loop Kira claimed a treasure dropped on her tile the very next turn. Zero new per-turn inference (~0.22 calls/agent-turn held with interventions). 8 new regression tests, all 48 green; Ollama was down so the Qwen check was a short clean `main.py` regression run, per the deterministic-mechanic note.

Day 15 (God mode): Added a pause-and-intervene menu that only mutates world_state — never agent logic, enforced by an import test. I dropped a treasure and triggered a drought mid-run; the world responded on its own. The drought starved out the entire original cast, then the death/respawn cycle repopulated it with blank-slate strangers (Mira, Otto, Zane) who lived, competed, and died in turn — a full generational turnover I never scripted, set off by one drought command. The discipline is the boundary: I change the world, the unchanged agent loop does the rest.

## Day 16 (plague + stranger interventions)

Day 16 adds two more God commands, both still mutating `world_state` only — the Day 15 AST import guard now covers them too (`god_mode.py` imports stay `world` + `population`, plus stdlib `random`, which the test already whitelists). The design rule held: neither command touches a decision, a trust number, or the executor. Each sets a marker or seeds a memory; the *existing* loops do the rest.

**`trigger_plague [name]`** afflicts a random (or named) living agent. The only thing it writes is the victim's `plague_until` turn marker (and a memory that it fell ill). The unchanged `world.update_hunger` reads that marker: while `turn <= plague_until` the agent loses **+3 hunger/turn instead of +1**, and the moment the window elapses with the agent still alive it **recovers** right there in the hunger loop — marker cleared, one "Recovered from the plague" memory written. So survival is its own cure, and starving is the cost of not keeping up — neither is scripted. I deliberately implemented sickness as a hunger-rate change rather than a new subsystem, so it rides the exact survival machinery agents already reason about. Sickness is also socially visible for free: `observe()` renders a plagued neighbour as "Kira (looks sick)" and `record_social_memories` logs "Observed Kira looking sick", so the plague becomes shared knowledge through the perception/memory path agents already have — zero new inference.

**`introduce_stranger name personality`** reuses the Day 14/15 blank-slate cold-start path (`spawn_blank_agent`), so the stranger is a genuine social unknown: empty memory, no relationships, no allies, hunger 0, unique name. The one difference from a respawn is framing: every existing agent gets the memory "A stranger, X, arrived. You know nothing about them." instead of the neutral arrival line. Crucially, **wariness is seeded as memory, not as a hardcoded trust penalty** — no `relationships` entry is created, so the stranger starts at neutral and earns trust (or distrust) only through the existing talk/trust loop over following turns. To support this I gave `spawn_blank_agent` two optional knobs (`arrival_memory` template, `log_event` toggle) so the stranger logs its own tagged `[GOD] stranger X introduced` line and suppresses the default population event.

**What I actually observed (deterministic `random` provider, seed 3 — these are mechanics, not emergent events; honest provider note below):**

- **Plague hunger curve was exactly the window.** Struck Kira at turn 1 (`plague_until = 11`). Isolating the per-turn increment, hunger rose +3 on every turn from 2 through 11 — exactly 10 ticks — then dropped back to +1 from turn 12, with the recovery memory written once. A separate unfed agent (Bob, starting hunger 2) shows the lethal side: +3/turn took him 2 → 5 → 8 → 10 and he **starved on turn 4** under the plague, no eating, no script.
- **Sickness was visible to neighbours.** With Kira afflicted one tile east, Alex's `observe()` read `East: Kira (looks sick)` and his social memory recorded "Observed Kira looking sick" — the plague propagated as ordinary perception.
- **Stranger was a true cold start with memory-only wariness.** Introduced Vera at turn 40: blank memory/relationships/allies, hunger 0. Both Alex and Bob recorded "A stranger, Vera, arrived. You know nothing about them.", and **neither had any trust value toward her** (`'Vera' not in relationships`). She was immediately interactable: Alex stepped adjacent, perceived `East: Vera`, and talked to her through the unchanged conversation layer — the message landed in Vera's inbox. Whether she's ever trusted is left to the talk/trust loop; nothing scripted it.
- **Zero new inference.** A full run with the plague at turn 8 and the stranger at turn 15 held at **0.214 strategy calls/agent-turn** (per-turn design = 1.0; Day 16 adds 0). A dedicated test confirms both commands make no LLM calls.

**Provider honesty:** Ollama was down (as it usually is — my standing note), so I could not do a genuine qwen run. Running with `AICIV_PROVIDER=ollama` exercised the real LLM-dispatch path, where every unreachable strategy call falls back to a safe `wander` and the Python executor takes over; that run completed cleanly with both `[GOD]` events logged, the stranger present, and caching intact (0.250 calls/agent-turn over 8 turns) — so it's a valid regression smoke-check of the dispatch+fallback path, but NOT evidence of emergent qwen social behaviour, and I'm not claiming any. The interesting emergent layer — does the society actually shun the stranger, does anyone help the sick — stays rare under competent play, consistent with Days 12–14, and isn't something a deterministic mechanic needs to prove.

Day 16 (plague + stranger): Added `trigger_plague` (sets a `plague_until` marker; the existing hunger loop drains +3/turn for exactly the window, then auto-recovers or the agent starves — sickness even shows up as "looks sick" in neighbours' perception) and `introduce_stranger` (Day 14 cold-start path, but wariness is seeded as a *memory* — "you know nothing about them" — not a trust penalty, so integration rides the normal talk/trust loop). Both mutate world_state only; the AST boundary test still passes. Deterministic runs showed the exact 10-turn hunger curve and recovery, a starving unfed agent dying under the plague, a stranger entering blank-slate and being talked to with no scripted trust. Zero new per-turn inference (~0.21 calls/agent-turn). 5 new regression tests, all 53 green. Ollama was down so the qwen check was a fallback-path smoke run, not a real qwen run — stated plainly rather than dressed up.

Day 16 (plague + stranger): Added two God commands. Plague accelerates an agent's hunger drain (+3/turn) for 10 turns via a world_state marker the existing hunger loop reads — I struck Bob and he starved in 4 turns under scarcity, a visibly shortened life from one command. Stranger adds a blank-slate agent (empty relationships, wariness seeded as memory not a trust penalty) — Vera entered, competed, and starved without integrating. Both mutate world_state only; the import boundary held. As in Days 12-14, the mechanics fire but the social reactions (shunning the stranger, noticing the sick) stayed rare under competent Qwen play.

## Days 18–19 (making it watchable: renderer + speed + highlights)

These were presentation milestones, not new mechanics — the agents behave exactly as before. But seeing the simulation as a live dashboard instead of log spam changed what I could notice. With the grid, hunger bars, trust digests, and a highlighted event feed all updating in place, the throughline of a run became legible at a glance: I could watch a hunger bar drain toward death, see an alliance flash green the moment it formed, and read a drought's [GOD] line ripple into a wave of starvation a few turns later.

The renderer is strictly read-only — it imports only the world-state layer, never agent/trust/strategy/god logic (enforced by an import test), the same boundary discipline god_mode follows in the other direction (write-only). That triad — agents decide, god_mode mutates, renderer displays, everything through one world_state — is the cleanest part of the architecture and the reason new systems can be added later without a rewrite.

Watching real Qwen runs at adjustable speed also reinforced the central finding from Days 12–16: cooperation is rare and precious. In one 100-turn drought run, a single alliance formed (Nova and Mira2, organically, under scarcity) and the rest was a slow cascade of starvation across generations. Seeing it animate — rather than reading it as a log — is what made the rarity feel meaningful rather than abstract.

## V2 / M0.2 — Tiered cognition (LLM cost scales with drama, not population)

The core scaling problem for V2: you can't run hundreds of agents if every one makes an LLM call every turn. M0.2 solves it with a budgeted attention system. Every agent runs the cheap zero-LLM heuristic mind by default; a small fixed budget of "focal" slots run the full LLM mind; and each turn the focal slots go to the most *interesting* agents, scored purely from signals that already exist in world_state (betrayal > theft > alliance > hostility > trust-shift > talk, plus threat and proximity).

The result that matters: the LLM's attention demonstrably follows the drama. In a budget-2 run, a theft victim was promoted to the LLM mind the turn after being robbed, held focal for its minimum tenure, then got demoted when a betrayal elsewhere made those two agents the more interesting pair — the focal set physically moved to where the story was. And cost stayed flat: holding the budget at 8 while raising population from 10 to 50 agents kept LLM calls at ~40-62 per run instead of the ~200 a per-agent model would cost. Cost scales with how much is happening, not with how many agents exist.

Two design judgments worth recording. First, I used only the *decaying event* of a trust crossing (the moment a friendship or grudge forms) rather than a static "currently has a grudge" flag — the static version double-counted and pinned a victim focal forever, which both inflated cost and broke demotion. The decaying signal is what lets a quieted agent fall back to the cheap mind. Second, demotion is *relative*, not absolute: with a tight budget someone always fills each slot, so an agent leaves focal only when out-dramatized by someone more interesting, not merely when its own life goes quiet. That's the honest behaviour of a budgeted system.

The interestingness weights are a reasonable first cut, not yet load-bearing — they'll need real tuning once Phase 1 adds civilization signals (discovery, teaching, leadership) for the scorer to weigh. The system is sound; the tuning evolves as the world gets richer.

## V2 / M1.2 — Discovery (unscripted invention over a prerequisite tree)

M1.1 let knowledge spread; M1.2 lets agents CREATE it. A small tech tree (fire -> tools/cooking -> farming) is defined as data — each item is a set of prerequisites — and each turn an agent that knows an item's prerequisites has a small probabilistic chance of inventing it, shaped by personality (curious tinker more) and situation (fed, idle agents invent; starving ones don't). There is no timer: discovery is a per-agent roll against conditions, never a scripted "turn N = farming". A discovered item enters the agent's knowledge and from there spreads through the unchanged M1.1 diffusion — invention and propagation compose, with zero added LLM calls.

The result that matters: discovery is genuinely emergent. At small population the first-discovery turn swings across a wide range (turn 1 to 20) depending on the seed — who happens to be fed, curious, and idle determines when invention fires, so different runs invent different things on different turns and in different downstream orders. The prerequisite chain strictly gates: with fire made unreachable, hundreds of attempts invented nothing downstream — no leapfrogging to farming without tools. And a discovered item then diffused out to dozens of knowers via M1.1, closing the invent -> spread loop.

One honest nuance worth recording: at large population the base item (the one with no prerequisites) is essentially always discovered on turn 1 — with dozens of fed, curious tinkerers each rolling every turn, the law of large numbers makes someone hit it immediately. So at scale the base tech's timing LOOKS fixed even though the mechanic isn't; the genuine run-to-run variation shifts to the downstream chain, which still depends on the base spreading first. This is arguably realistic (a large society discovers basics fast) rather than a flaw, and the lever (DISCOVERY_BASE) exists if base invention should stay precious even in crowds. The "varies, not scripted" property is verified at small population where base discovery is a true gamble.

Discovery rate is tuned so the full chain completes within ~20-25 turns at the demo scale — visible but not instant. Nothing is guaranteed in one turn, and the prerequisite chain is strictly enforced, so M1.3 (tech changing the world) has real, agent-discovered items to build on.

## V2 / M1.3 — Technology changes the world (Phase 1 complete)

This is the milestone where knowledge stops being a label and becomes a force. M1.1 spread knowledge; M1.2 invented it; until now a known tech was an inert word in a set. M1.3 makes three techs materially alter the survival dynamics the sim already had, each gated on the agent actually KNOWING the tech, each flowing through the existing hunger/food loop rather than a new subsystem:
- fire -> cooking: a knower gets more relief from the same food (EAT_RELIEF + FIRE_EAT_BONUS), so scarce food stretches further.
- tools -> reach: a knower forages from an adjacent tile, not only its own, widening what's reachable.
- farming -> production (the headline): a fed farmer grows food into an empty neighboring tile via world.place_food — producing supply instead of only consuming it.

The result that matters — the headline A/B experiment: two matched runs, same seed and population, one with farming available to discover and spread, one without (control). The farming population reached ~96% survival against the control's ~60%, and its food supply stabilized (~210) where the control stayed at a marginal scarcity baseline. The population that discovered and spread farming broke out of the scarcity death spiral; the one that didn't, didn't. Same world, run twice — the only difference is what its population figured out and taught each other.

Crucially, this is a measured consequence, not a scripted win. Farming is gated three ways so it can't be a thumb on the scale: the agent must know farming, must be fed enough to tinker rather than forage (FARM_HUNGER_CUTOFF), and production is bounded by a per-capita ceiling (FARM_FOOD_PER_CAPITA = 2.0/agent) so the world doesn't fill with infinite food. The early-scramble deaths remain: a few agents still die before farming bootstraps, because the population has to earn its way out of scarcity before the tech can stabilize it. Survival improves because reachable food rises in world_state, not because any agent was hardcoded to live.

Honest tuning note: the first cut (FARM_YIELD = 0.5, no cap) was clearly too strong — a fully agrarian population hit 100% survival and food exploded unbounded (538 and climbing), escaping scarcity into an unrealistic infinite-food world. Adding the per-capita ceiling fixed both at once: survival landed at a believable ~96% and food plateaued sustainably. The levers are explicit and defensible — FARM_YIELD for rate, FARM_FOOD_PER_CAPITA for the abundance ceiling, FARM_HUNGER_CUTOFF for survival pressure — which matters because Phase 2's economy will sit directly on this food supply.

Zero added LLM calls (all effects are state reads in the existing loop); v1 byte-identical when the tree is absent; 91 tests green; god_mode and renderer AST boundaries intact. One side-fix: the M1.1 pure-diffusion verify switched its test item from "fire" to a neutral token ("song"), because once fire has a real survival effect a diffusion-only experiment must use an inert item or it measures the effect instead of the spread.

Phase 1 is complete: agents invent technology unscripted, it spreads through their social network, and it changes whether they live or die. The food economy is now a lever that culture can move — the foundation Phase 2 (settlement & economy) builds on. Tagged v2.1.
## V2 / M2.1 — Settlement (Phase 2 opens)

The historical step: through Phase 1 every agent was a NOMAD — forage, wander, die or drift on. M1.3 changed the supply side (a knowing population PRODUCES food via farming), making a place reliably worth staying at for the first time. M2.1 is the consequence: nomads become SETTLERS around that reliable food, and a SETTLEMENT — the first durable civilizational artifact — appears as explicit, persistent state in world_state (`{id, center, members, founded}`) that later milestones (storage, trade, towns, governance, eras) will accumulate on.

The design tension M2.1 had to resolve: the artifact must be EXPLICIT and persistent (an era arc needs something durable to carry forward), but the ACT of settling must EMERGE from the food economy, never a scripted "turn N a village appears". The resolution is a deterministic threshold on SUSTAINED presence near food — zero LLM calls, zero RNG. Each turn an agent near food (within FOOD_RADIUS) advances a `settle_streak`, reset to 0 the moment it isn't near food. A settlement founds when ≥ MIN_SETTLERS (3) spatially-clustered agents have all sustained a streak of ≥ SUSTAIN_TURNS; an existing settlement then recruits any nomad that gathers at its reliable food. Members get a gentle home-pull in `strategy.choose_action` (drift back toward the centre when beyond HOME_RADIUS) — placed AFTER the survival override, so a starving member still forages outward and survival always wins. The home-pull lives in the shared executor, so both the LLM and heuristic minds get it for free.

The headline A/B experiment — matched seed and population, farming ON vs OFF: with farming, **13–16 settlements emerged** (~82–84% of 100 agents became members), clustered at the farmed-food locations (100% of centres sat on/next to standing food); **without farming, ZERO settlements formed**. Same world, run twice — the only difference is whether the population could maintain reliable food. Settling is a measured consequence of the food economy, not a scripted event.

The load-bearing tuning insight (the real result): the FIRST cut used SUSTAIN_TURNS = 5 and it was **far too eager** — it formed ~12 settlements *even in the no-farming control*, all at turn 5, which read as a timer. The cause was honest and instructive: the world starts with a scattered food dump that briefly feeds everyone, so a short window let any cluster settle regardless of farming — settlement stopped being a consequence of the economy. The fix is the whole point of the milestone: the sustain window must be LONGER than transient food can keep a cluster fed. At SUSTAIN_TURNS = 20, only CONTINUOUSLY-replenished food (a farm plot grown beside its farmers each turn) keeps streaks alive that long; the initial dump depletes first and the control's streaks collapse before they can found anything. This is the emergent test for "reliable source" done through TIME rather than a special tag on a tile — we never label food as farmed-vs-respawned; reliability is detected by whether a place keeps feeding the same agents.

Is formation eager or reluctant? Calibrated, with a clear-eyed caveat. Emergence checks out: across seeds, settlements form at VARYING locations and counts, and foundings spread across turns (20, 21, 22, 27, 29 / 20, 21, 23, 24 / 20, 21, 24, 32, 39 …). The honest nuance — exactly mirroring the M1.2 "base discovery at scale" note — is that the FIRST settlement's turn is floored at SUSTAIN_TURNS (you cannot sustain 20 turns in fewer than 20), so with abundant farmers the earliest village reliably appears right at turn 20. That floor is structural and defensible, not a timer: WHICH turns beyond it, WHERE, and HOW MANY all vary by seed, and with no reliable food nothing forms at all. If the first village should stay precious even in a farming-rich crowd, SUSTAIN_TURNS (or MIN_SETTLERS) is the lever.

Home-pull is measurable and correctly subordinate to survival: settled members stay tightly clustered around their centre (mean Chebyshev distance ~1.97, ≈ HOME_RADIUS 2) versus a nomad-equivalent sprawl of ~7.85; and a member pushed to starvation immediately abandons the home-pull to forage toward food (verified both as a macro clustering metric and a deterministic micro-test).

Compose / boundaries: zero added LLM calls and zero RNG (the formation threshold is deterministic, so it cannot desync the seeded stream); the default 3-agent run is byte-identical when the system is off (`settlements=False`, the default — the loop never calls `settlement.update`) and the home-pull is inert for any nomad (`settlement is None`); all of Phase 0 + Phase 1 intact; renderer stays read-only (settlements drawn as a `#`/magenta overlay in both `world.render` and the rich grid, pure reads); god_mode untouched. 99 tests green (91 + 8 new: forms only with enough sustained settlers near reliable food; too few never found; no reliable food → no settlement; isolated nomad never joins while a gatherer does; pulled home when fed but not when starving; nomad movement unaffected; update is zero-LLM/zero-RNG; off run == v1).

Phase 2 is open: nomads are now a society with PLACES — durable settlements that emerged from the food economy and will carry forward as the substrate for storage, trade, and the village→town→city era arc. Tagged v2.2.

## V2 / M2.2 — Storage & surplus (the sim grows wealth)

The historical step: M1.3 made farming PRODUCE food and M2.1 made nomads SETTLE around it — but a settled agent still consumed hand-to-mouth. Farming's deeper unlock in real history was not just more food but food you could KEEP: a storable SURPLUS. Stored surplus is what first created wealth, a survival buffer, and (later) inequality and trade. M2.2 gives each agent a bounded PERSONAL `stockpile` and makes that wealth matter — it is the moment the economy gets an asymmetry to turn on. Scope was held strictly to storage + emergent inequality + a survival buffer; no trade/exchange/markets/prices/specialization (that is M2.3).

Two rules carry the milestone. (1) **Storing requires settlement** — only an agent with `settlement is not None` banks; a nomad stores nothing, which reinforces M2.1's value (a place worth staying at is a place worth keeping a granary). (2) **Wealth is never assigned — it EMERGES.** Banking rate is a pure product of traits the agent already has: `STORE_RATE × STORE_PROPENSITY[personality] × (FARMING_BONUS if it knows farming)`. A settled, well-fed agent beside food banks that amount each turn (capped at `STORAGE_CAP`), where "well-fed" (hunger ≤ STORE_HUNGER_MAX) is the "beyond immediate need" test — you store the excess, never food you needed to eat now. There is no new "richness" stat; wealth varies purely as a consequence of who an agent is and what it knows.

The survival buffer is the immediate consequence and the reason wealth matters: when a member would otherwise starve (hunger critical, no reachable food) it draws one stored meal (`BUFFER_COST` units → `BUFFER_RELIEF` hunger) to survive instead of dying. This is wired into the existing starvation step in `run_agent_turn`, right after the eat-at-the-brink check — so an agent with savings weathers a food shock that kills one without.

**HEADLINE 1 — emergent inequality.** In an all-farmer population (100 agents, 60 turns), stockpile wealth came out *unequal and structured*, not uniform and not random: Gini ≈ 0.23–0.25, and the wealth distribution correlated with the trait-derived banking rate at **r ≈ +0.74 to +0.84**. Mean wealth ordered exactly as the traits predict at the extremes — competitive/independent agents richest (~17.5/20), friendly/sharing agents poorest (~5.7/20). On the knowledge axis (half the cast farming), farmers averaged ~10 vs non-farmers ~4 — producers accumulate faster. Every agent started at 0.0; the spread is entirely emergent.

**HEADLINE 2 — wealth buffers survival (reusing the god_mode drought).** After a village settled and accumulated varying wealth over a 55-turn warmup, a drought-driven famine shock (god_mode `trigger_drought` reused as the trigger, plus a total crop failure so the famine actually bites a self-feeding farming village) ran for 12 turns. Result across seeds: **~83–84% of the wealthy (≥ one stored meal) survived; 0% of the poor did.** The wealthy drew their stockpiles down and lived; the savings-less, with nothing to spend, starved. Wealth is a survival buffer in the most literal sense.

The honest tuning judgment (the real result of M2.2): accumulation is **calibrated, not too uniform and not too strong** — but it took retuning to get there. The first cut (STORE_RATE = 0.5) was too fast: over a long settled life *every* personality saturated the cap, so wealth collapsed back to uniform (everyone ≈ 20) and inequality vanished — the cap, meant as an anti-infinity guard, was erasing the very gradient the milestone is about. Dropping STORE_RATE to 0.2 fixed it: now only genuine hoarders (a competitive farmer at ~0.6/turn) approach the cap while the poorest (a friendly non-farmer at ~0.1/turn) accrue a thin reserve, so wealth stays a visible gradient. The cap is the load-bearing lever — it bounds the rich, but set too low relative to the rate it also bounds the poor and the gap closes. One residual nuance worth recording: at the *middle* of the personality ladder the ordering is noisy — cautious (propensity 1.2) and curious (1.0) come out nearly tied/occasionally inverted (~12.2 vs ~12.5), because cap saturation compresses the top and survival/positioning add noise. The *extremes* (competitive richest, friendly poorest) and the knowledge axis (farmer > non-farmer) are robust; the fine-grained middle ranking is not something to over-read. The buffer is correctly strong-but-finite: a single stored meal is the line between life and death in the famine (so the poor die), but even a maxed stockpile only buys ~2 meals, so a long enough drought eventually takes the rich too (survival fell from 100% at a 10-turn shock to ~18% at 18 turns) — savings delay starvation, they don't abolish it.

Compose / boundaries: **zero added LLM calls and zero RNG** — accumulation is deterministic float state-math, so it can't desync the seeded stream; the draw-down is deterministic too. The default run is byte-identical when the system is off (`storage_on=False`, the default — the loop never calls `storage.accumulate`, the buffer step and the wealth overlay both gate on the flag), and storage is *also* intrinsically gated on settlement, so a v1 nomad never banks regardless. `storage.py` imports only `world` + `strategy` (one-directional), so the world layer stays dependency-free; the renderer/summary wealth line is a pure read printed only when storage is on; god_mode's drought is REUSED, not modified. 105 tests green (99 + 6 new: surplus banks only above need and only when settled; stockpile never exceeds the cap; a starving member with savings survives while one without dies; wealth tracks personality + knowledge and was never assigned; accumulate is zero-LLM/zero-RNG; storage-off run == v1).

The sim now has WEALTH — a bounded personal surplus that emerges from character and knowledge, and that decides who survives a shock. That asymmetry (some agents have more than others, and it is worth something) is exactly the substrate Phase 2's trade (M2.3) needs. Tagged v2.3.

## V2 / M2.3 — Trade, money & proprietary knowledge (Phase 2 CLOSED)

The historical step: M2.2 created WEALTH and inequality — some agents hold far more food/skill than others. M2.3 makes that asymmetry MOVE. Trade exists precisely BECAUSE agents differ (rich/poor, skilled/unskilled, fed/starving); this milestone lets those differences flow through VOLUNTARY exchange at a price that EMERGES from circumstance. It is the moment settlements become an economy, and it closes Phase 2.

Four pieces, all pure state-math (zero added LLM calls, zero RNG of their own):

**Specialization — a second producer skill.** `hunting` joins the tech tree as a sibling of `farming` off the same `tools` prereq, with its own producer function (`knowledge.hunt`) gated identically on KNOWING the skill. It produces food by a distinct mechanic/location — roaming game on a wider ring (Chebyshev 2–3) rather than a tended adjacent plot — so a population now holds two genuinely different producer skills. That skill gap is what gives knowledge-trade something real to exchange: a farmer lacks hunting and vice-versa.

**Money — emergent and food-backed (not fiat).** A new `agent.money` is minted ONLY from food surplus that runs PAST the M2.2 storage cap (`economy.mint`): once an agent's larder is full the food it keeps producing can't be stored, so instead of being wasted it becomes a money claim on that food. Money has value only because it is redeemable as food to survive (`storage.draw_down` now spends stockpile then money) and because others accept it in trade. AUTHORITY-DECREED / MINTED (fiat) currency and WAGE-LABOR are explicitly out of scope, deferred to Phase 3 (stated in code).

**Trade — voluntary exchange at an emergent price.** `economy.trade` is a deterministic pass over adjacent pairs (one best trade per pair per turn) matching food↔money and guarded-knowledge↔money exchanges. The price is the MIDPOINT of the seller's reservation and the buyer's value, so it MOVES with conditions: buyer hunger lifts the food price, seller surplus (larder near the cap) sinks it, and a skill's LOCAL RARITY (how many nearby already know it) plus the buyer's skill-gap set its worth. Both sides end up better off by their own valuation — the price sits strictly between reservation and value — which is exactly what separates trade from theft (untouched).

**Proprietary knowledge — guard vs. give.** `knowledge.guards(agent, item)` decides, FROM PERSONALITY (never assigned), whether a holder withholds a skill from free M1.1 diffusion: an independent/competitive agent guards its valuable skills and sells them; a friendly/curious/cautious agent still teaches free. `diffuse` skips a guarded item (when the economy is on); `trade` is the additional, paid-only path for what a guard won't give away. So the same skill leaks free from generous holders while guarded holders sell it.

**HEADLINE 1 — emergent price.** The SAME skill 'hunting' prices at **7.0** money when rare and the buyer lacks a producer skill, **3.4** when semi-common, **4.0** when the buyer already farms (smaller skill-gap), and **no deal at all (None)** when common enough that the buyer's value falls below the guard's fixed reservation. In two real executed trades it sold for **7.0** in a village where only the seller knew it and **0.0** (no sale) where 3 of 4 neighbours already hunted. Food prices rose monotonically with buyer hunger (1.5 → 3.0 → 4.5 per unit at hunger 3/6/9) and with seller scarcity. A fixed ratio is falsified: price tracks circumstance.

**HEADLINE 2 — trade redistributes by asymmetry.** In the controlled case a rich, unskilled farmer (food+money 35, knows only farming) beside a poor, starving hunter (0 wealth, hunger 8, knows only hunting) traded: the skill flowed skilled→unskilled (the farmer learned hunting), and food/money flowed rich→poor (the hunter ended with a survival buffer) — both better off. At population scale (120 agents, 70 turns) **45 trades fired (31 knowledge, 14 food) and 27 agents bought a producer skill they lacked**. Surplus and skills move from those who have them to those who need them.

The honest judgment (the real result of M2.3): **a real economy emerges** — prices move with scarcity, desperation and surplus; surplus and skills flow to where they're needed; and knowledge is genuinely guarded by some and given by others. Three calibration insights worth recording. (1) **Local rarity must exclude the seller.** The first cut counted the seller among "nearby knowers," so in a small neighbourhood where only the seller knew a skill, rarity computed as 0 ("everyone here knows it") and NO trade ever fired — the opposite of intended. Excluding the prospective seller from the rarity denominator fixed it: a skill only the seller holds nearby is correctly dear. (2) **The guard's reservation must be a FIXED floor, not rarity-scaled.** Initially both buyer-value and seller-reservation scaled linearly with rarity, so their ratio was constant and a deal was NEVER impossible (price just shrank toward zero). Making the reservation a fixed floor means that as a skill spreads its value sinks below the floor and the skill becomes genuinely un-sellable (price None) — that crossing is what makes a common skill un-tradable rather than merely cheap, and it is what stops guarded knowledge from selling forever. (3) **Money is realistically scarce early.** Because money mints only from surplus PAST the cap, it appears only once agents saturate their larders (the richest few), so early exchange is dominated by knowledge-for-food barter and money circulates mainly among the wealthy — an honest consequence of food-backing, not a bug; and a desperate buyer pays a steep premium (a hunger-8 buyer spent 15 money for ~3.8 food), which is the desperation price working as designed.

Compose / boundaries: zero added LLM calls and zero RNG (mint and trade are deterministic over a stable iteration, so they can't desync the seeded stream); the default run is byte-identical when the economy is off (`economy_on=False` — mint/trade never called, diffuse's guard-skip gated off, money ignored in the buffer), and hunting production is a no-op drawing no RNG when nobody is a fed hunter, exactly like farming, so adding the unconditional `hunt()` call and the `hunting` tech-tree entry leaves v1 and every M1.x run byte-identical. `economy.py` imports only world + storage + knowledge (one-directional); the renderer/summary money+skills line is a pure read printed only when the economy is on; god_mode untouched (AST boundary tests green). 113 tests green (105 + 8 new: emergent price varies with conditions; trade mutually beneficial/voluntary; competitive-guarded knowledge doesn't free-diffuse but sells; friendly knowledge still free-diffuses; hunting produces food only for knowers; surplus→money→purchase round-trips and money redeems to survive; mint/trade zero-LLM/zero-RNG; economy-off run == v1).

Phase 2 is CLOSED: settlements that emerged from the food economy now have storable wealth, a food-backed currency, specialization, and voluntary trade at emergent prices — a working economy. The asymmetries are in motion. Tagged v2.2.

## V2 / M3.1 — Wage labor: the first institution (Phase 3 opens)

The historical step: Phases 1–2 built emergent MATERIAL mechanics (knowledge, food, wealth, prices) that mostly EQUILIBRATE — left alone they settle toward a steady state. Phase 3 builds INSTITUTIONS: persistent structures that coordinate and constrain many agents, and the governing rule for all of Phase 3 is that an institution must EMERGE from existing asymmetries, never be installed. M3.1 is the first, and the first DISEQUILIBRATING mechanic: a rich agent EMPLOYS a poor one to produce for it at a wage, and because the employer captures `output − wage` the rich–poor gap COMPOUNDS instead of settling. That compounding is the intended result — it manufactures the class tension later Phase 3 milestones (law, conflict, governance) will respond to. Scope was held strictly to employment relationships + emergent wages + measurable compounding; NO governance/leaders/law/tax/revolt (later Phase 3), NO fiat/minted money (still deferred — wages are paid in the existing food-backed money/stockpile).

The mechanic, all pure state-math (zero added LLM calls, zero RNG of its own; `labor.py`):

**Roles EMERGE from wealth + skill, never assigned.** An EMPLOYER is a settled agent with CAPITAL (`money + stockpile ≥ EMPLOYER_MIN_CAPITAL = 5`) AND a producer skill it can't fully work alone (the means of production) — your rich M2.3 producers. A WORKER is a settled POOR agent (`wealth < WORKER_MAX_WEALTH = 5`) with NO producer skill of its own, for whom a wage beats foraging/starving — your M2.2 have-nots. `is_employer`/`is_worker` are PURE READS of existing state with no flag set anywhere: an agent with no capital can never be an employer (`capacity = wealth // CAPITAL_PER_WORKER`, 0 when broke) and a self-sufficient agent (owns a producer skill, or wealth ≥ `SELF_SUFFICIENT_WEALTH = 15`) never takes a wage. Strip an employer's capital and the role evaporates — verified.

**The relationship PERSISTS — this is what makes it an institution.** A link `{employer, worker, wage, since}` lives in `world_state["employments"]` and SURVIVES across turns until it ends. Each employed turn (`labor.update`): the worker produces `LABOR_OUTPUT = 2.0` for the employer (means + labor → the EMPLOYER's stockpile, overflow past the cap → money under the M2.3 rule), the employer PAYS the worker the wage (`economy._settle`, money first then food), and the worker CONSUMES `COST_OF_LIVING = SUBSISTENCE_WAGE` of subsistence (the wage IS its food — which is why it survives). Net: employer `+(output − wage)`, worker `+(wage − cost-of-living)` ≈ 0 at a subsistence wage. A link ENDS when a party dies, the employer loses its capital/means (lets the worker go), or the worker becomes self-sufficient (gains a skill / climbs past 15 → QUITS, upward mobility — with hysteresis so it isn't hired-and-fired on one threshold).

**The wage EMERGES from the labor market.** `wage = subsistence + leverage × (output − subsistence)`, bounded to `[SUBSISTENCE_WAGE = 1.0, LABOR_OUTPUT = 2.0)`, where `leverage = market_tightness × (1 − desperation)`. Tightness = `openings / (openings + workers)`: a WORKER'S market (scarce labor) → leverage ~1 → wage near output; an EMPLOYER'S market (abundant labor) → leverage ~0 → wage near subsistence. Desperation = `hunger / HUNGER_MAX` discounts a starving worker's leverage. Both an abundant-labor glut AND worker desperation compress the band toward subsistence — emergent EXPLOITATION — but subsistence is POSSIBLE, never automatic: the same work pays differently as conditions change. `wage < output` always (employer profits) and `wage ≥ subsistence` always (worker survives), so the relationship is voluntary and mutually beneficial, just lopsided.

**HEADLINE 1 — compounding inequality (the disequilibrium).** Matched cast (2 rich+skilled employers + 8 poor+unskilled+hungry workers, same start), wage labor ON vs OFF, 20 turns. Wealth Gini with labor ON climbed **0.45 → 0.51 → 0.55 → 0.57 → 0.59** (turns 0/5/10/15/20) — monotone, compounding; with labor OFF it stayed **flat at 0.45** (no exchange, static wealth). By turn 20 the employers had banked ~87 each while every worker sat at ~5.9 (near subsistence). Same agents, same start — the only difference is whether the institution ran. Inequality compounds rather than equilibrating, which is the whole point.

**HEADLINE 2 — emergent wage / exploitation.** The SAME job (output 2.0) priced across the labor market: in a worker's market (1 worker, 8 openings) a real hire formed at wage **1.89** (near output — the worker captures the value); in an employer's market (8 desperate workers, 1 opening) the real hire formed at **1.00 — exactly subsistence** (the employer captures the surplus). The micro table shows the full surface: a fed worker earns 1.95 / 1.50 / 1.05 across scarce / balanced / glut markets, and a starving worker earns 1.00 in EVERY market (desperation crushes leverage to zero). A fixed wage is falsified — subsistence EMERGED from the supply+desperation conditions, it was not hardcoded.

**Mutually entered, not slavery/theft.** Two identical desperate poor workers, capital enough to hire one: the employed worker was fed by its (near-subsistence) wage and SURVIVED (and even accumulated ~0.9), while the identical UNEMPLOYED one starved to death. Even when exploited the worker still gains NET — its life — which is exactly what separates emergent exploitation from slavery/theft.

The honest judgment (the real result of M3.1): **the mechanic is sound and all three claims hold deterministically** — roles fall out of existing wealth/skill with no flag, the wage genuinely moves with the labor market (reaching subsistence under glut+desperation without being scripted there), and inequality measurably compounds while the institution runs. The instructive finding is about ORGANIC PERSISTENCE in the full loop. Running the live simulation (40 procedurally-generated agents, settlements + economy + tech-tree, farming seeded), wage labor **DID emerge organically during the transient** — at turn 40 there were 11 employers, an unskilled worker, and 2 active employment links, all falling out of real `world_state` with nothing assigned. But by turn 80+ it had DISSOLVED: free M1.1 knowledge diffusion taught farming to all 38 survivors, so everyone became skilled-and-rich, the unskilled underclass vanished, and with no one lacking the means of production there was no one to hire. This is not a bug in M3.1 — it is an honest interplay finding: **wage labor requires a persistent inequality of the MEANS of production, and free knowledge diffusion competes that inequality away.** Persistent organic wage labor therefore depends on the upstream asymmetry persisting — proprietary/guarded knowledge (M2.3) keeping skills scarce, or capital concentration — which is why the deterministic verification (like every M2.x verify before it) constructs the settled rich-skilled/poor-unskilled class structure directly rather than waiting on a long offline run to manufacture and then preserve it. The class engine is built and works; keeping a working class in existence over long horizons is a tuning/Phase-3 question (it is exactly the tension law and governance will arbitrate), not a defect of the wage mechanic.

Compose / boundaries: zero added LLM calls and zero RNG (the labor market is deterministic state-math over a stable iteration — employers matched to nearest available workers with sorted tie-breaks — so it can't desync the seeded stream); the default run is byte-identical when wage labor is off (`labor_on=False`, the default — `labor.update` is never called and no links form; the summary employment overlay gates on the flag). Employment links live ONLY in `world_state` (no new `Agent` field), so an off run is structurally unchanged. `labor.py` imports only world + storage + economy (one-directional), keeping the world layer dependency-free; `--labor` implies `--economy` (and so settlements + storage), runs AFTER trade/mint so this turn's wealth/skill drives who employs whom; the renderer/summary employment line is a pure read printed only when labor is on; god_mode untouched (AST boundary tests green). **123 tests green** (113 + 10 new: wage varies with supply+desperation (not fixed); under glut+desperation wage reaches subsistence but never below the survival floor; output flows to the employer and wage to the worker; an agent without capital never employs; roles emerge from wealth+skill and aren't assigned; the link persists across turns and is mutually entered; worker quits when self-sufficient / broke employer lets go; inequality compounds with labor on vs flat off; labor is zero-LLM/zero-RNG; labor-off run == v1).

Phase 3 is OPEN: the first institution exists. Wage labor emerges from the M2.2/M2.3 class structure, its price moves with the labor market down to emergent exploitation, and it makes inequality COMPOUND instead of equilibrate — the disequilibrium and class engine the rest of Phase 3 (law, conflict, governance) will run on.

## V2 / M3.2 — Legitimate leadership: authority by trust (Phase 3)

The historical step: M3.1 built the first institution (wage labor) — political-economic power DOWNSTREAM of wealth, the rich employing the poor and inequality compounding. M3.2 builds the first POLITICAL institution and, crucially, the first power that is NOT downstream of wealth: a LEADER legitimated by TRUST. The most-trusted agent need not be the richest, so political power DECOUPLES from economic power — the tension later Phase 3 milestones (law, taxation, conflict) will turn on. Scope was held strictly to emergence of a legitimate leader from the existing trust network + one minimal real effect + loss of legitimacy; NO taxation, NO law/legislation, NO revolt (later Phase 3). A leader in M3.2 has INFLUENCE, not the power to tax or legislate.

The mechanic is a PURE READ of the EXISTING v1 trust system — `leadership.py` writes no trust values, makes zero LLM calls, and draws zero RNG; it reads `agent.relationships` and its own `world_state["leaders"]` record:

**Leadership EMERGES from a cohered trust cluster, never a global-max lookup.** A leader emerges within a SETTLEMENT only when a real FOLLOWING coheres: a candidate needs `≥ MIN_FOLLOWERS = 2` co-settlers each trusting it `≥ FORM_TRUST` (tied to `trust.HIGH_THRESHOLD = 2`, so it reads exactly as the v1 trust system's own "high"). The leader is the CENTRE of the cluster — the candidate the most co-settlers trust (most followers, alphabetical tie-break) — stored as `world_state["leaders"][sid] = {leader, followers, since}`. The single most-trusted agent with ONE ardent admirer does NOT clear the bar — authority requires a CLUSTER, not a high score — and a fractured, low-trust settlement where no candidate reaches the bar has NO leader at all (correct: not every group has one; we do not force one).

**Legitimacy is EARNED and CONTINGENT — this is what makes it legitimacy, not a crown.** Leadership persists only while the following persists. HYSTERESIS (a Schmitt trigger on trust) stops it flickering on a single-turn wobble: a following FORMS on the high bar (`FORM_TRUST = 2`) but an incumbent is RETAINED on a lower bar (`KEEP_TRUST = 1`), so a follower drifting `2 → 1` does not unseat anyone, while real erosion (trust falling to `0` or negative — e.g. the leader turning hostile, a `−3` hit) drops the follower below the keep bar and can end the role. A NEW, strictly-more-trusted centre DISPLACES the incumbent when the following shifts (its form-following bigger than the incumbent's), resetting `since`. If a leader's retained following falls below `MIN_FOLLOWERS` and no one else qualifies, the leadership DISSOLVES.

**The minimal real effect is COORDINATING INFLUENCE — never tax/law.** A leader coordinates its following: a follower coheres MORE TIGHTLY around its leader than a mere settler does around the settlement centre. The M2.1 home-pull (strategy step 3b) is retargeted for a follower to the LEADER'S current position at a tighter radius (`LED_HOME_RADIUS = 1` vs `settlement.HOME_RADIUS = 2`); the leader itself keeps the ordinary settlement pull (it is the centre, not its own follower). So a LED settlement is measurably more cohesive than an identical UNLED one — with no taxation, no legislation, no coercion: pure coordinating influence flowing through an existing system.

**HEADLINE 1 — legitimacy, not a leaderboard.** Three cases. (a) A cohered cluster of 3 co-settlers trusting "Mara" high → Mara emerges as leader (following {F1,F2,F3}, since turn 1). (b) One ardent admirer trusting "Idol" at 9 — the highest single trust score in the world — produces NO leader, because one edge is not a following. (c) A fractured settlement where each agent admires a different co-settler reaches no cluster → NO leader. Emergence is conditional on a cohered following, not an automatic max lookup.

**HEADLINE 2 — political power decoupled from economic power.** A settlement with a penniless "Poor" (wealth 0), a far-richer "Rich" (wealth 99), and 3 co-settlers who trust Poor HIGH and distrust Rich: the emergent leader is **Poor**, NOT the richest agent (Rich). Trust, not wealth, drives leadership — the M3.2 point. (If the leader were always the richest, trust would have added nothing — a fail; it isn't.)

**CONTINGENT — lost, hysteretic, displaceable.** "Lena" leads with 2 high-trust followers (turn 1); a follower wobbles `2 → 1` and Lena stays leader (no single-turn flicker, turn 2); the leader turns hostile and both followers fall below the keep bar → Lena LOSES legitimacy (turn 3). Separately, a fresh more-trusted centre "Cyrus" DISPLACES still-incumbent Lena when the following shifts to it, resetting the tenure. Legitimacy tracks the ongoing following, with hysteresis.

**EFFECT IS REAL.** Four followers start at Chebyshev distance 3 from a leader pinned at the settlement centre; driven through the same strategy/executor the live loop uses for 12 turns: with leadership ON the mean follower distance to the leader settled at **1.00** (`LED_HOME_RADIUS`), with leadership OFF (only the looser settlement pull) at **2.00**. The led settlement is measurably tighter — real coordinating influence, no tax or law.

The honest judgment (the real result of M3.2): **a legitimate leader genuinely EMERGES from a real following — only when one coheres, sometimes nobody, sometimes a non-richest agent — and the role is genuinely CONTINGENT on ongoing trust.** No piece is a disguised global-max, wealth-proxy, or scripted crown. The strongest evidence is the ORGANIC run (not the constructed verify), and it is now a COMMITTED, reproducible artifact — `verify_m32.py` DEMO F and `test_leadership_emerges_organically_from_built_trust_in_a_full_run`, not prose about a one-off: a full seeded simulation (seed 7) of 7 clustered farmers who cohere into one settlement and build trust **purely through the conversation loop — zero injected trust, every relationship earned in play**. The settlement founds at turn 20 and produces **P1 as leader at turn 20 (2 followers), displaced by P0 at turn 31 (3 vs 2 followers), displaced by P2 at turn 51 (5 vs 4)** — a contingent, shifting authority that emerged and changed hands entirely from the trust network the agents built among themselves, identical on re-run and adding zero model decision calls. The leader was decided by who the cluster trusted, not by who was richest. (An honesty note for the record: an earlier draft of this section described an uncommitted P1→P4→Hub run as the evidence; that exact run was not reproducible from any committed script, so it was replaced with the seed-7 run that DEMO F actually executes — the phenomenon is the same, but the claim is now backed by a runnable artifact.) One design note worth recording: leadership reads trust but the EFFECT (the tighter home-pull) is the deliberately minimal influence the scope allows — it makes a led group cohere, which is a real, measurable difference, but it is intentionally NOT yet the power to redistribute or compel; that (taxation, law) is the next Phase 3 step, and decoupling political legitimacy from wealth FIRST is precisely what gives those later powers a non-economic source to draw on.

Compose / boundaries: zero added LLM calls and zero RNG (leadership is a deterministic read over sorted settlement ids and stable `world_state["agents"]` order with sorted name tie-breaks, so it can't desync the seeded stream), and — the load-bearing invariant — it writes NO trust values (verified: the relationship network is byte-identical before and after 29 passes). The default run is byte-identical when leadership is off (`leadership_on=False`, the default — `leadership.update` is never called, `leaders` stays empty, and the strategy home-pull's `following_target` returns `None` so the M2.1 pull is unchanged; the summary leadership overlay gates on the flag). The leadership record lives ONLY in `world_state` (no new `Agent` field), so an off run is structurally unchanged. `leadership.py` imports only `world` + `trust` (one-directional), keeping the world layer dependency-free; `--leadership` implies `--settlements` ONLY (a leader needs a settlement to lead, but NOT the economy/labor — it is the first power not downstream of wealth), runs AFTER `settlement.update` so this turn's membership is current; the renderer/summary leadership line is a pure read printed only when leadership is on; god_mode untouched (AST boundary tests green); emergence/loss/displacement are mirrored into the chronological `world_state["events"]` log (as settlement foundings are) so authority change is part of the durable civilizational record. **130 tests green** (123 + 7 new: a leader emerges only with a cohered following (not a global max, none in a fractured group); the leader can be a non-wealthiest agent (power decoupled from wealth); leadership is lost when trust erodes with hysteresis and can be displaced by a more-trusted centre; the leadership effect makes a led settlement more cohesive than unled; leadership reads trust but writes no trust values + zero LLM/RNG; leadership-off run == v1; and the load-bearing organic test — a leader emerges and changes hands from trust agents BUILD in a full seeded run, reproducibly).

The first POLITICAL institution exists, and the first authority that wealth cannot simply buy. Leadership emerges from the v1 trust network as the centre of a coherent following, is contingent on ongoing legitimacy (lost when trust erodes, displaced by a more-trusted centre, with hysteresis), and coordinates its followers — political power decoupled from economic power, the asymmetry the rest of Phase 3 (law, taxation, conflict) will run on. Tagged v3.2.

## V2 / M3.3 — Taxation & redistribution: legitimacy acts on wealth (Phase 3)

The historical step: M3.1 built the CLASS ENGINE (wage labor — the rich employer captures the worker's surplus and inequality COMPOUNDS, a disequilibrating spiral). M3.2 built the LEGITIMACY ENGINE (a leader legitimated by TRUST, a political power DECOUPLED from wealth). M3.3 is their COLLISION — the first force that BENDS the M3.1 spiral: a legitimate leader TAXES its wealthy followers and REDISTRIBUTES to its poor ones. Political legitimacy acquires the power to act on economic inequality. Scope was held strictly to taxation + redistribution + a legitimacy backlash that self-limits over-taxation; NO law/legislation, NO revolt (later Phase 3), NO fiat money (taxes move the existing food-backed money/stockpile, conserving total wealth — redistribution, not minting).

The mechanic is deterministic state-math (`taxation.py`, zero LLM, zero RNG) that COMPOSES the two engines and, uniquely for Phase 3, WRITES trust (M3.2 only read it):

**Taxation requires LEGITIMACY (this ties the two engines).** ONLY a settlement with a legitimate M3.2 leader can tax — the power is downstream of legitimacy, not of wealth or a flag. The leader taxes its FOLLOWERS (the M3.2 trust cluster), never non-followers, never itself: a follower whose wealth exceeds `RICH_THRESHOLD = 10` is taxed `tax_rate` of the EXCESS above it (progressive) into a pool; the pool is redistributed to followers below `POOR_THRESHOLD = 5`, weighted by NEED (the poorest receive the most). Taxation only fires when there is BOTH a tax base and a redistribution target (it exists to redistribute). Total wealth is CONSERVED (pool taken == pool given to the cent).

**Redistribution BENDS inequality (the headline).** Matched led settlements, the M3.1 labor spiral (2 rich employers + 7 poor hungry workers, an EMPLOYER'S market -> subsistence wages -> employers compound) running in BOTH arms, taxation the only difference. Within-settlement wealth Gini over 20 turns: **taxation OFF stays HIGH — 0.665 -> 0.608 (the spiral persists); taxation ON is bent down and held LOW — 0.665 -> 0.293 -> 0.460, every turn at or below the untaxed arm, ending 0.148 lower.** The ON curve drifting back up from 0.293 to 0.460 is the dynamic tension made visible: redistribution fights the labor spiral each turn, holding inequality far below where it would otherwise sit. Political legitimacy measurably checks the M3.1 spiral — the milestone's whole point.

**Legitimacy BACKLASH self-limits taxation (emergent consent of the governed).** Taxation costs the leader trust — but only OVERREACH does. Up to `CONSENT_RATE = 0.35` the governed accept taxation as legitimate (consent), so a moderate levy draws NO resentment; above it each taxed follower withdraws `round((rate − CONSENT_RATE) × RESENT_SCALE)` trust per turn. Redistribution meanwhile EARNS the leader `+GRATITUDE` from each poor follower it lifts. The SAME leader, two rates, two fates: **MODERATE (30%, within consent) — the taxed-rich trust holds at 2 (no resentment), the poor's trust rises to 3 (gratitude), the leader is SUSTAINED indefinitely; OVER (90%) — resentment of −2/turn crashes the rich below M3.2's `KEEP_TRUST`, the following collapses below `MIN_FOLLOWERS`, and next turn M3.2's contingency STRIPS the leadership (and with it the power to tax — taxing stops).** Tyranny is punished by withdrawal of consent, emergent, not scripted. This is the FIRST Phase-3 institution that writes trust, and it writes through the existing `trust.adjust_trust` (the logged, grudge-aware path conversations use), so the backlash is a first-class part of the trust network the leadership engine reads — the two engines are genuinely wired together, not bolted side by side.

**FLOWS are correct.** A constructed cast (leader 8, Rich 50, Poor1 1, Poor2 3, Middle 7, Outsider 99-but-not-a-follower): after one levy at 30%, Rich 50->38 (taxed on the 40 excess), Poor1 1->9 and Poor2 3->7 (the POOREST lifted MOST, need-weighted), and the leader, the mid-wealth follower, and the non-follower outsider are ALL untouched; total wealth conserved 168.0 -> 168.0.

The honest judgment (the real result of M3.3): **redistribution measurably BENDS the inequality curve, and the backlash makes over-taxation genuinely SELF-LIMITING — moderate taxation is sustained by consent while tyranny is punished by trust loss that fires M3.2's contingency.** No piece is mis-tuned: the consent band (taxation below `CONSENT_RATE` drawing zero resentment) is what lets moderate taxation persist where a naive per-turn trust cost would erode any leader who ever taxed; and because the rich are taxed on the EXCESS above a threshold, the levy is progressive rather than a flat head-tax. Two honest caveats for the record. (1) The headline OFF arm is engineered to be an EMPLOYER'S market (workers held hungry/at subsistence — the M3.1 treadmill) so the spiral PERSISTS and the contrast is clean; in a WORKER'S market (scarce labor, high wages) labor itself compresses inequality and both arms fall, though taxation ON still ends materially lower — so taxation reliably *reduces* inequality, but how dramatic the bend looks depends on the underlying labor market. (2) In fully ORGANIC end-to-end runs (`--labor --leadership --taxation`, seeds 3/7) leadership emerges from built trust and the leader DOES tax and redistribute, but the pools are tiny (~0.0–0.2) because organic wealth stays modest — few followers cross `RICH_THRESHOLD`. The strong-inequality demonstration is therefore the constructed verify, which is honest about being constructed; the organic run proves the institution COMPOSES and fires, not that organic agents spontaneously generate dynastic wealth (they don't, yet). Both are reported.

Compose / boundaries: zero added LLM calls and zero RNG (taxation is deterministic state-math over sorted settlement ids and sorted follower names; verified RNG-untouched and `{decision:0, strategy:0}` across 19 passes), and it CONSERVES total wealth (verified byte-equal before/after). It WRITES trust — by design, the milestone's point — but only through `trust.adjust_trust`, the same logged path conversations use, so the writes are first-class and inspectable. The default run is byte-identical when taxation is off (`taxation_on=False`, the default — `taxation.update` is never called). Taxation rides `world_state` (`taxation_on`, `tax_rate`, plus the M3.2 leader record) with no new `Agent` field. `taxation.py` imports only world + storage + trust (one-directional). `--taxation` implies `--leadership` (and so `--settlements`) — a leader is needed to levy — but NOT `--labor` (taxation acts on whatever wealth exists; pair with `--labor` for the spiral to bend); runs AFTER `leadership.update` so it taxes with this turn's leader and the trust it writes feeds next turn's leadership re-evaluation (the backlash loop); god_mode untouched (AST boundary tests green); levy events mirrored into the chronological `world_state["events"]` log. **135 tests green** (130 + 5 new: only a legitimate leader can tax (no leader -> no redistribution); redistribution lowers within-settlement Gini vs an untaxed run; over-taxation costs legitimacy while moderate is sustained; flows go rich->poor among followers and conserve wealth; taxation-off run == v1).

The first REDISTRIBUTIVE institution exists: political legitimacy can now check economic inequality, and the check is bounded by the consent of the governed — over-reach dissolves the very legitimacy that enables it. The two Phase-3 engines (class and legitimacy) are now coupled: wealth still compounds (M3.1), trust still confers contingent authority (M3.2), and that authority can now bend the wealth curve (M3.3) only so far as the governed permit. Law, taxation-of-trade, and conflict are the next collisions. Tagged v3.3.

## V2 / M3.4 — Conquest & monarchy: power seized by force (Phase 3)

The historical step: M3.2 built authority by CONSENT (trust-legitimacy, power granted from below). M3.4 builds the OTHER source of power: DOMINATION — power SEIZED from above by FORCE. A wealthy agent converts money into an ARMY of real fighters and attacks a settlement to seize it, becoming MONARCH — a persistent ruling title held by force, not consent. This completes the class engine's arc: M3.1 wealth COMPOUNDS → M3.2/M3.3 legitimacy RESISTS and REDISTRIBUTES → M3.4 wealth ESCALATES to violence and takes the crown anyway. Scope was held strictly to conflict (force vs force), conquest of ONE settlement, the MONARCH institution (rule by force, losable), and the force-vs-legitimacy collision; NO underclass revolt, NO inter-kingdom war/diplomacy, NO multi-settlement kingdoms (all later).

The mechanic (`monarchy.py`, zero LLM, **zero RNG** — fully deterministic so a fight can never desync the seeded stream) COMPOSES M3.1 wealth, M3.2 loyalty, and the death machinery:

**Military power EMERGES from wealth via REAL fighters (never a wealth compare).** An ASPIRANT (wealth ≥ `MIN_WAR_CHEST`=10) raises a FORCE by SPENDING money to hire FIGHTERS — real, poor/desperate agents (wealth < `MERC_MAX_WEALTH`=5, echoing M3.1's labor pool) within `MUSTER_RADIUS`, paying each `FIGHTER_COST`=5 (money then food). Attacker force = the NUMBER of fighters actually mustered, capped by `war_chest // FIGHTER_COST` AND by how many mercenaries are in range. So power runs wealth → soldiers → force, with soldiers being real agents: a broke aspirant musters nobody and conquers nothing.

**A fight turns on MUSTERED FORCE, not on wealth.** Attacker force is resolved against the settlement's DEFENCE: a MONARCH's standing GARRISON, else a legitimate M3.2 LEADER's loyal FOLLOWERS (loyalty becomes defensive force — the collision), else the militia of living MEMBERS. The attacker wins iff `attacker_force > defender_force` (strict — a tie is held by the seat; the attacker must OVERCOME the defence). War is COSTLY: each side loses `round(CASUALTY_RATE=0.5 × opposing_force)` fighters (capped at its own size) — real agents who DIE through `population.announce_death` (a battle event, survivor memories, a queued respawn, exactly like starvation). Figureheads (the aspirant, the defending leader/monarch) are not slain — they win or lose the TITLE; the rank-and-file pay in blood, so a defended conquest thins the leader's following (feeding M3.2 next turn) and a failed assault burns the aspirant's army.

**MONARCH — the institution (persistent, rule-by-force, LOSABLE).** The victor becomes monarch: `world_state["monarchs"][sid] = {monarch, since, garrison}`, the garrison being the surviving army (a standing force that defends the crown). A monarch rules by FORCE: it LEVIES `MONARCH_LEVY_RATE`=0.20 of each member's wealth above a threshold into its OWN coffers, WITHOUT consent. The crown is LOSABLE — a stronger later aspirant attacks the garrison with the same mechanic and OVERTHROWS the monarch (succession reset; never permanent). **The sharp contrast with M3.3:** M3.3 taxation needs a legitimate leader and the CONSENT of the governed, is REDISTRIBUTIVE (rich followers → poor followers), conserves wealth, and SELF-LIMITS through a trust backlash; a monarch's levy needs NEITHER a leader NOR consent, is EXTRACTIVE (members → the crown), and has no trust check — domination, not government.

**Monarch vs trust-leader: two roles, two sources, force takes precedence.** Conquest does NOT erase the `leaders[]` record — the trust-leader may still be loved (consent survives occupation) — but the monarch overrides governance: it is the monarch, not the consent-leader, who rules and levies the seized settlement. FORCE > CONSENT for control; the dark point of M3.4 is exactly that a trusted, legitimate leader can be conquered by a bought army and reduced to a powerless figurehead under a crown it never granted (verified: after an overwhelming assault, `monarchs[S001]=Rich` while `leaders[S001]=Chief` coexist).

**HEADLINE 1 — wealth → force → crown.** A wealthy aspirant (Rich, wealth 30 → could fund 6 fighters) musters 4 REAL fighters (only 4 mercenaries were in range), spending wealth 30 → 10, and seizes an unled 2-member town (4 > 2 militia) to become monarch; a BROKE aspirant (wealth 3 < the war-chest floor) musters 0 fighters and takes nothing. The crown is bought with real soldiers funded by wealth — not a number compare.

**HEADLINE 2 — force vs legitimacy collides, BOTH outcomes.** Against Chief's 4 loyal followers: the WEALTHIEST agent in the world (Rich, wealth 200) who can muster only 3 fighters is REPELLED (3 < 4 — loyalty repels the richer-but-smaller force); the same Rich mustering 7 fighters WINS (7 > 4 — overwhelming bought force overcomes loyalty). The outcome turns on mustered force, not wealth: a real fight, not a foregone wealth-max.

The honest judgment (the real result of M3.4): **monarchy genuinely EMERGES from wealth-funded force — not a wealth-max lookup — force-vs-legitimacy genuinely COLLIDES (loyalty can repel, overwhelming wealth wins), and the crown is genuinely CONTESTED (losable to a stronger army).** No piece is a foregone conclusion: because force = the count of *real* mustered fighters (gated by both war chest and merc availability) and defence = the count of *loyal* defenders, the richest agent loses when it cannot field enough soldiers — wealth is necessary but not sufficient. One deliberate design choice worth recording: the resolution is DETERMINISTIC on the force counts (strict `>`, defender holds ties) rather than a seeded coin-flip — I chose reproducible determinism over hidden RNG (the prompt allowed either) because it keeps the seeded stream un-desynced and makes the collision legible; "favours the stronger" is honoured, and the non-determinism-on-wealth comes from loyalty being an independent input, not from a die. Two honest caveats. (1) The LOOP launches only WINNABLE assaults — a `fieldable_force > defenders` guard, added after an unguarded first cut had aspirants spamming doomed 2-fighter attacks every turn and bleeding the population for nothing; a rational commander raises an army to win, so an aspirant who cannot out-muster the defence bides its time. This is a LOOP policy only — `attempt_conquest` itself resolves any matchup, so the verify can still stage a doomed assault and watch loyalty repel it. (2) With that guard, fully ORGANIC `--labor --leadership --taxation --monarchy` runs produce NO successful conquest (and, correctly, no doomed spam): organic agents never concentrate enough wealth to field an army larger than a village's loyal core — the same limitation as M3.1's transient wage labor and M3.3's tiny organic pools. So the dramatic wealth→crown path is demonstrated in the (honestly-labelled) constructed verify, which a would-be tyrant's concentrated wealth makes possible; the loop wiring itself IS exercised end-to-end in `verify_m34.py` DEMO F (a real `monarchy.update` battle). This is a genuinely meaningful finding, not a gap: in an egalitarian society nobody can buy a big enough army to conquer — which is precisely why the verify must construct the tyrant to show what happens when someone can.

Compose / boundaries: zero added LLM calls and zero RNG (deterministic iteration over sorted settlement ids, aspirants by wealth then name, mercenaries by distance then name, casualties by name — verified RNG-untouched and `{decision:0, strategy:0}`, and the battle trajectory byte-identical on re-run). The default run is byte-identical when monarchy is off (`monarchy_on=False`, the default — `monarchy.update` is never called, no armies/crowns/battle deaths form; the summary monarch overlay gates on the flag). Records live only in `world_state["monarchs"]` (no new `Agent` field). `monarchy.py` imports world + population + economy (one-directional). `--monarchy` implies `--settlements` only (a town to seize) — NOT the economy or leadership (pair with `--economy`/`--labor` for army-funding wealth and `--leadership` for the loyalty to collide with). Battle deaths route through the SAME `population.announce_death` as starvation, generalised with optional `note`/`final_memory` params whose defaults reproduce the original starvation strings EXACTLY (so every existing death stays byte-identical) — a war death reads "fell in battle" and queues a respawn like any death. god_mode untouched (AST boundary tests green); conquest/levy/overthrow events mirrored into `world_state["events"]`. **141 tests green** (135 + 6 new: force scales with wealth-funded fighters and a broke aspirant conquers nothing; loyal followers repel a smaller-force richer attacker but not an overwhelming one; a monarch levies without consent; the crown is overthrowable by a stronger force; war kills real agents; monarchy-off run == v1).

The SECOND source of power exists: domination by force, alongside M3.2's authority by consent. The class engine reaches its dark climax — wealth, having compounded (M3.1) and been resisted and redistributed by legitimacy (M3.2/M3.3), now escalates to violence and buys the crown outright (M3.4) — yet even the crown is not safe, contestable by the next, larger army. Underclass revolt (the consent-source striking back), inter-kingdom war, and multi-settlement kingdoms are the next collisions. Tagged v3.4.

## V2 / M3.5 — Kingdoms & vassalage: feudalism (Phase 3)

The historical step: M3.4 built a MONARCH who seizes ONE settlement by force. M3.5 is the SCALE-UP — a monarch conquers NEIGHBOURING settlements into a multi-settlement KINGDOM, a two-level FEUDAL HIERARCHY (KING → VASSAL LORDS → their settlements). The model is faithful feudalism: the vassal bargain is TRIBUTE + military SERVICE flowing UP, PROTECTION + local AUTONOMY flowing DOWN, and loyalty is CONDITIONAL — it erodes under a grasping overlord and a pushed vassal can BREAK AWAY. That conditional loyalty makes royal power CONTINGENT (on keeping vassals loyal), not absolute. Scope was held strictly to formation-by-conquest, the vassal hierarchy, the tribute/service bargain, conditional loyalty, and BREAKAWAY (a vassal leaving the realm); baronial civil war, defection to a RIVAL king (needs M3.6), and rebellion cascades are deferred and stated as boundaries in `kingdoms.py`.

The mechanic (`kingdoms.py`, zero LLM, **zero RNG**) COMPOSES M3.4 conquest, M3.2 trust, and M3.3's backlash shape — and to do it without duplicating the fight, M3.4's `attempt_conquest` was refactored to call a new shared `monarchy.resolve_battle` (the identical strict-`>` resolution + `CASUALTY_RATE` deaths), which M3.5 reuses verbatim, so both milestones fight by the same maths (M3.4 verified byte-identical after the refactor):

**Formation by conquest → vassalage (HEADLINE 1).** A monarch's `conquer_neighbour` aims the M3.4 fight OUTWARD at an adjacent settlement (within `KINGDOM_REACH`=8). On victory the conquered settlement joins the realm and KEEPS its local ruler — its M3.2 trust-leader or M3.4 local monarch — who swears FEALTY and rules on as a VASSAL LORD (the local `leaders[]`/`monarchs[]` record is NOT erased: local autonomy preserved). A ruler-less town is held DIRECTLY (king installed as its monarch). A hopeless defence (outnumbered ≥ `SUBMIT_RATIO`=3) SUBMITS without bloodshed. Verified: King (monarch of S001) conquers trust-led S002 then S003, Chief2/Chief3 become vassals, `kingdoms[King]` shows a two-level realm of 3 settlements with both local leaderships intact.

**The feudal bargain — tribute up, service up, autonomy/protection down (HEADLINE 2).** TRIBUTE cascades UP the levels (`tribute`): a vassal levies `TRIBUTE_RATE`=0.20 of its settlement members' wealth-above-threshold (members → vassal), then a `KING_SHARE` of THAT fresh take flows up to the king (vassal → king) — verified across both levels (members 40.0→34.0, vassal 10.0→14.5, king 10.0→11.5) and CONSERVING total wealth (60.0 == 60.0): a realm concentrates wealth at the crown *through* its vassals. MILITARY SERVICE (`muster_realm`): the king's host = its own bought fighters PLUS every LOYAL vassal's mustered contingent — realm strength = the SUM of loyal vassals' forces (verified: the host includes the vassal's fighters; after breakaway it does not).

**Conditional loyalty — the generative feudal tension (HEADLINE 3).** A vassal's loyalty IS its trust in the king (reusing the v1 trust system; fealty seeds it to `LOYAL_TRUST`=2). Heavy royal demands ERODE it through the EXACT M3.3 tax-backlash shape (now aimed at the crown): when the king's share exceeds `KING_CONSENT`=0.35 each vassal withdraws `round((rate − KING_CONSENT) × RESENT_SCALE)` trust per turn. A vassal at/below `BREAKAWAY_TRUST`=−2 for `BREAKAWAY_PATIENCE`=2 consecutive turns BREAKS AWAY (its settlement leaves the realm). Verified — same vassal, two treatments, two fates: a grasping king (share 0.9) drives Chief's trust 2 → 0 → −2 → break-away on turn 3 (NOT on the first hard turn — the patience counter is the hysteresis; recovered loyalty resets it), while a fair king (share 0.25, within consent) draws no resentment and holds the vassal indefinitely. Royal power is contingent on not over-grasping — feudal tyranny self-limits exactly as M3.3's civic tyranny did.

The honest judgment (the real result of M3.5): **kingdoms genuinely EMERGE as faithful feudal hierarchies — conquest → vassalage (local ruler kept), tribute cascading up across real levels and conserved, loyal vassals serving militarily, the map consolidating — and loyalty is genuinely CONDITIONAL: a pushed vassal breaks away, a fair king holds his realm, neither scripted.** No piece is a foregone conclusion: breakaway turns on accumulated trust (an independent input), not on a flag, and a fair crown provably keeps what a grasping one loses. Two honest findings worth recording. (1) Like M3.4, the `update` loop launches only WINNABLE conquests (a `realm_host_size > defenders` guard) — a LOOP policy only; `conquer_neighbour` itself resolves any matchup, so the verify can still stage matchups directly. (2) CONSOLIDATION is real but SELF-LIMITING, and the limit is itself an emergent finding, not a bug: in `verify_m35.py` DEMO D a rich king consolidates 5 settlements → 2 independent (realm grows S001→S002→S003) and then STALLS — because hiring a mercenary PAYS it `FIGHTER_COST`=5, lifting the once-desperate fighter to wealth ≥ `MERC_MAX_WEALTH` so it will never enlist again; an army CONSUMES the pool of the desperate (verified: after the campaign all 40 mercenaries are paid out of eligibility and the king can field a host of 0). So conquest is throttled by available manpower, not just gold — a grimly plausible constraint (a realm can only grow as fast as it has poor willing to fight). As with M3.4, fully ORGANIC short runs (`--kingdoms --economy --labor --leadership`) produce no kingdom: organic agents never concentrate enough wealth to become a monarch and then out-field a neighbour, so the dramatic arc is shown in the honestly-labelled constructed verify, while the loop wiring is exercised end-to-end in DEMO D/E (real `kingdoms.update`).

Compose / boundaries: zero added LLM calls and zero RNG (deterministic iteration over sorted king names, settlement ids, member/vassal names; the fight reuses RNG-free `monarchy.resolve_battle` — verified RNG-untouched, `{decision:0, strategy:0}`, and the realm trajectory byte-identical on re-run). Trust writes (fealty + erosion) go through the existing logged `trust.adjust_trust` (the M3.3 path). The default run is byte-identical when kingdoms are off (`kingdoms_on=False`, the default — `kingdoms.update` is never called; the summary kingdom overlay gates on the flag). Records live only in `world_state["kingdoms"]` (no new `Agent` field). `kingdoms.py` imports world + monarchy + economy + trust (one-directional). `--kingdoms` implies `--monarchy` + `--settlements` (a king is a monarch who expanded; pair with `--economy`/`--labor` for army-funding wealth and `--leadership` for vassalable trust-leaders); `--tribute-rate` sets the crown's share. god_mode untouched (AST boundary tests green); formation/tribute/breakaway events mirrored into `world_state["events"]`. **146 tests green** (141 + 5 new: conquest of a neighbour makes its ruler a vassal; tribute cascades settlement→vassal→king and conserves wealth; the king musters a loyal vassal's fighters but not a broken-away one's; over-tribute breaks a vassal away with hysteresis while a fair-treated one stays; kingdoms-off run == v1).

Single settlements are now FEUDAL KINGDOMS: a king atop vassal lords, bound by tribute and service, held together only so long as the vassals stay loyal. The two power sources fuse here — a vassal lord rules its town by local consent or local force, while owing fealty upward to a crown that took it by force — and the whole structure is contingent, a realm one grasping levy away from fragmenting. These kingdoms are the units inter-kingdom war (M3.6) will set against each other; vassal defection to a rival crown and organised rebellion are the collisions that follow. Tagged v3.5.

## V2 / M3.6 — Inter-kingdom war & empire: the CLIMAX of Phase 3

The historical step: M3.5 built feudal KINGDOMS (king → vassal lords → settlements). M3.6 sets them against EACH OTHER in WAR — whole feudal hosts clash — and the loser's king is SUBJUGATED into the victor's realm, forming a THIRD level of the same hierarchy: an EMPIRE (EMPEROR → subject-KING → vassal-lords → settlements). And empires are no more permanent than kingdoms: a subjugated subject-king is a high-level vassal whose loyalty is just as CONDITIONAL, so an emperor who over-taxes or weakens FRAGMENTS. Power is contingent at EVERY level — the thematic capstone of Phase 3. Scope was held strictly to inter-kingdom war, subjugation into a multi-level empire, and fragmentation (breakaway one level up); vassal DEFECTION-to-the-enemy mid-battle, DIPLOMACY/ALLIANCES between kingdoms, and fiat money are deferred and stated as boundaries in `empire.py`.

The mechanic (`empire.py`, zero LLM, **zero RNG**) RIDES M3.5 and M3.4 with almost no new machinery: the fight is the shared `monarchy.resolve_battle` (the identical strict-`>` resolution + `CASUALTY_RATE` deaths, now used by M3.4 single-settlement, M3.5 realm, and M3.6 empire); the muster is `kingdoms.muster_realm` (king + loyal vassal-lords), which `imperial_host` wraps to add each loyal subject-king's WHOLE host, recursively, for a multi-level empire; loyalty, the consent band, the backlash, the breakaway hysteresis and the trust thresholds are M3.5's constants applied one level up. No constant was duplicated and no fight re-implemented.

**Loyalty decides the war (HEADLINE 1 — the core coupling).** Each kingdom's war strength is its LOYAL host (`imperial_host`: a disloyal or broken-away vassal/subject-king answers no call), NOT its nominal size or wealth. Verified with the SAME two kingdoms run twice: a RICHER kingdom (wealth 500) whose two vassals are DISLOYAL fields a loyal host of 4 and LOSES to a POORER kingdom (wealth 50) whose vassals all muster (host 9); flip that same kingdom's vassals to LOYAL and it fields a full host of 12 and WINS the same matchup. Same kingdoms, loyalty flipped, opposite results — wealth funds fighters but loyalty fields them, so governance beats raw wealth and a rich tyrant's realm is brittle.

**Subjugation → multi-level empire (HEADLINE 2).** On defeat the loser's king becomes a high-level VASSAL (a subject-king) of the victor, swearing fealty (trust seeded to `LOYAL_TRUST`) but KEEPING his own internal realm (his `kingdoms[loser]` record — vassals/settlements — is untouched). `world_state["empires"][emperor] = {emperor, subject_kings, founded, discontent}`. Tribute now cascades through MORE levels (settlement → lord → subject-king → emperor): `kingdoms.tribute` fills each subject-king from his realm, then `empire.tribute` cascades a share of his wealth up to the emperor — verified wealth-conserved across all levels (70.0 == 70.0) and reaching the imperial crown THROUGH the subject-king. The emperor can muster the loyal subject-king's WHOLE host (verified: the imperial muster pulls the subject-king's vassal-lord's contingent too).

**Fragmentation — empires rise AND fall (HEADLINE 3).** A subject-king's loyalty is conditional exactly as a vassal-lord's: an emperor whose imperial share exceeds the consent band (0.35) erodes the subject-king's trust (the M3.3-shape backlash), and a WEAKENING emperor (one who can no longer field a host as large as the subject-king's own — the protection bargain failing) erodes it too; a subject-king whose trust collapses for `BREAKAWAY_PATIENCE`=2 consecutive turns BREAKS AWAY, reclaiming independence WITH his realm. Verified with the same subject-king under two emperors: a GRASPING emperor (share 0.9) sees trust fall 2→0→−2 and the subject-king break away on turn 3 (NOT turn 1 — hysteresis holds); a FAIR emperor (share 0.25) holds trust at 2 and keeps the empire indefinitely. Same subject-king, two imperial treatments, two fates.

The honest judgment (the real result of M3.6): **war genuinely turns on LOYAL host strength — a richer-but-disloyal kingdom provably loses to a poorer-but-loyal one and wins only when its vassals are loyal, so governance beats wealth and a rich tyrant's empire is brittle; defeated kings genuinely subjugate into faithful multi-level empires (tribute cascading through the new level, conserved, the emperor fielding the subject-king's whole host); and empires genuinely FRAGMENT when emperors overreach — rise AND fall, with every power remaining contingent.** No piece is a foregone conclusion: the war outcome turns on accumulated loyalty (an independent input), not on a wealth-max or a flag, and the breakaway turns on trust the agents' own treatment produced. The same two honest caveats as M3.4/M3.5 carry up one level and are worth recording. (1) The `update` loop launches only WINNABLE wars (an `imperial_host_size` guard — a king does not march a host it knows is too small, so no suicidal wars; verified: the weaker Rich never attacks the stronger Poor while Poor attacks Rich). This is a LOOP policy only — `wage_war` itself resolves any matchup the caller stages, so the verify can drive a doomed war directly. (2) Fully ORGANIC short runs produce no empire, for the SAME compounding reason all of Phase 3's force milestones do: organic agents never concentrate enough wealth to become a monarch, then a king who out-fields a neighbour, then an emperor who out-fields a whole rival realm — three escalating thresholds an egalitarian society never crosses. So the dramatic arc is shown in the honestly-labelled constructed verify (`verify_m36.py`), while the loop wiring (`empire.update` launching real wars, tribute, and fragmentation) is exercised end-to-end in DEMO D/E. As at every prior force milestone, this is a meaningful finding, not a gap: empires require a tyrant rich enough to build the host that conquers a kingdom, and an egalitarian world simply has none.

Compose / boundaries: zero added LLM calls and zero RNG (deterministic iteration over sorted emperor/subject-king/king names; the clash reuses RNG-free `monarchy.resolve_battle` — verified RNG-untouched, `{decision:0, strategy:0}`, war trajectory byte-identical on re-run). Trust writes (fealty + imperial backlash + weakening erosion) go through the logged `trust.adjust_trust` (the M3.3 path). The default run is byte-identical when empire is off (`empire_on=False`, the default — `empire.update` is never called; the summary empire overlay gates on the flag). Records live only in `world_state["empires"]` (no new `Agent` field); `kingdoms[subject_king]` is left intact. `empire.py` imports kingdoms + monarchy + economy + trust + world (one-directional). `--empire` implies `--kingdoms` (→ `--monarchy` + `--settlements`); `--imperial-share` sets the emperor's cut. god_mode untouched (AST boundary tests green); the empire overlay lives in the main summary (the renderer module is untouched, so its read-only AST boundary stays green); war/subjugation/tribute/fragmentation events mirrored into `world_state["events"]`. **152 tests green** (146 + 6 new: war musters the whole LOYAL host excluding disloyal vassals; a richer-but-disloyal kingdom loses then wins when its vassals are loyal; a defeated king is subjugated into a multi-level empire; imperial tribute cascades through the subject-king level and conserves wealth; an over-taxed subject-king fragments away with hysteresis while a fair-treated one stays; empire-off run == v1).

Phase 3 is complete. The class engine's whole arc now stands: wealth COMPOUNDS (M3.1) → legitimacy by CONSENT resists and redistributes it (M3.2/M3.3) → wealth ESCALATES to FORCE and seizes the crown (M3.4) → force SCALES into feudal KINGDOMS (M3.5) → kingdoms WAR and the victors build EMPIRES (M3.6). And the capstone insight is that none of it is permanent: every power structure Phase 3 built — the wage, the leader, the crown, the realm, the empire — is contingent on something it must keep earning (subsistence, trust, force, loyalty), and can fall when it stops. An emperor who over-taxes his subject-kings loses them exactly as a leader who over-taxes his followers loses them; the same conditional-loyalty mechanic governs every level. Tagged v3.6.

## V2 / M4.2 — Inheritance at death (wealth cascades down family lines)

The historical step: M4.1 (Phase 4 opener) made life GENERATIONAL — agents pair, bear children who inherit temperament, age, and die of OLD AGE — but a dead agent's WEALTH still vanished, so each death erased a life's savings and time could not accumulate. M4.2 makes death the ENGINE of generational wealth: on ANY death the deceased's money + stockpile form an ESTATE that PASSES TO KIN. Scope was held strictly to MOVABLE wealth: dynastic succession of TITLES (crowns, vassal seats) stays M4.3 — a dead ruler's title record still clears exactly as today; M4.2 moves money and grain, never a seat (stated as a boundary in `lineage.py`).

The mechanic (`lineage.settle_estate`, **zero LLM, zero RNG**) rides the ONE death funnel every cause already shares — `population.announce_death` (old age, starvation, battle all route through it), so a single hook inherits identically regardless of how the agent died. It is gated on `lineage_on` and lazily imported inside `announce_death`, so with lineage OFF the inheritance code is never even imported and the run stays byte-identical. The estate is split PARTIBLY and EQUALLY down a fixed kin-order derived from the existing `parents` links: surviving CHILDREN (dependents included) → surviving PARENTS → SIBLINGS (agents sharing a parent) → ESCHEAT to the settlement's ruler (monarch first, else trust-leader, read from the existing `monarchs`/`leaders` records — read-only, the seat is untouched) → vanish if there is neither kin nor ruler, exactly as pre-M4.2. Money (a food-claim with no store cap) flows to heirs in full; inherited FOOD respects the M2.2 granary cap (`STORAGE_CAP`) and any overflow DROPS at the deceased's tile as ground food rather than vanishing.

**Wealth flows down, conserved (HEADLINE 1).** A parent dying with money 30 + stockpile 10 leaves an estate of 40 that its two children split to exactly 15 money / 5 food each; conservation holds to floating-point precision (estate == to-heirs + ground-drop; nothing minted, nothing lost, and the wealth leaves the corpse so it can't be double-counted). Cap-overflow is honest: a sole heir with a near-full granary inheriting 40 food fills to the cap (20) and the remaining 26 units land as 26 whole ground-food tiles on the map. Kin-order BINDS — with a child, a living parent and a sibling all present the CHILD takes everything; remove the child and the PARENT takes it (not the sibling); remove both and the SIBLING inherits. Each fallback fires only when the closer tier is empty.

**Inheritance COMPOUNDS inequality across generations (HEADLINE 2 — the reason the milestone exists).** A/B over a 220-turn multi-generation run (seed 7, heuristic minds, zero LLM), inheritance ON vs the SAME run with `settle_estate` stubbed to the vanish-at-death baseline: descendant-generation wealth Gini is **0.404 with inheritance vs 0.325 without** — death passing wealth on measurably CONCENTRATES it. And it concentrates down REAL family lines: the wealthiest living descendant (Milo2, holding 33.2) sits at generation depth 3, and its wealth is shown cascading down its exact ancestral line — **Cyn → Iris → Iris2 → Milo2**, three successive inheritance events read from the durable events log — a house whose wealth persists across four generations, while a non-heir descendant (Juno3) starts from 0.00. Rich houses stay rich; poor and kinless lines start their descendants from nothing. Note inheritance is not a passive tag: because a stockpile buffers survival (M2.2), an heir with an inherited granary outlives one without, so inheritance also feeds back into WHO survives to inherit again — the compounding is causal, not cosmetic (verified: a dependent orphan with an inherited 18-unit granary survives 15 foodless turns where an identical pennyless orphan starves).

**Escheat — the crown profits from a kinless death (HEADLINE 3).** A kinless settled agent dying under a monarch has its estate ABSORBED by the crown (logged distinctly, "…escheated to Rex (no kin)"); with no monarch it escheats to the trust-leader; with neither ruler nor kin the estate VANISHES exactly as before M4.2, and no bystander's wealth moves (verified conserved, 20.00 → 20.00).

The honest judgment (the real result of M4.2): **wealth genuinely CASCADES down real family lines — the kin-order is binding (children over parents over siblings, each fallback demonstrated), the transfer is conserved to floating-point precision, and a traced four-generation dynasty holds inherited wealth while non-heirs start from zero — and inheritance measurably COMPOUNDS inequality (descendant Gini 0.404 vs 0.325 in a controlled A/B against the vanish baseline).** No piece is scripted: heirs are resolved from the agents' own `parents` links and the split is a mechanical equal division, so who inherits and how much both fall out of the family structure births produced, not an assignment. The one honest caveat, shared with every emergent Phase 2/3 result: the A/B's magnitude depends on wealth having accumulated unequally in the first place (M2.2's emergent gradient) and on enough generations turning over — a shorter or more egalitarian run shows the mechanism working but a smaller gap. That is a property of the world, not a mis-tuning of the estate: M4.2's job is to stop death from erasing wealth, and with that one change history starts accumulating in families. Compose/boundaries: zero added LLM and zero RNG (equal split down sorted heirs, deterministic ground-drop placement); `--lineage` off byte-identical (estate vanishes as today; inheritance code never imported); seeded lineage-on runs reproduce byte-for-byte; titles untouched (M4.3). **198 tests green** (192 + 6 new: equal split + conservation + cap-overflow ground drop; kin-order fallbacks binding; escheat to monarch and to leader, vanish with neither; events/memories written via the real death path; inheritance only when lineage on; an inherited granary keeps a dependent orphan alive).

## V2 / M4.3 — Dynasties: titles pass to heirs (Arc 1 CLOSED)

The historical step: M4.2 made WEALTH dynastic, but TITLES still evaporated — when a monarch/king died of old age his crown record simply cleared and the realm dissolved (observed live pre-M4.3: Wren's 2-settlement kingdom vanished at his death while his children split his gold). M4.3 makes TITLES pass to heirs — monarch seats (M3.4), kingdom crowns and vassal lordships (M3.5), imperial thrones and subject-king seats (M3.6). The core design tension is faithful to real dynasties: WEALTH is PARTIBLE (M4.2's equal split, untouched) but a TITLE is IMPARTIBLE — a crown cannot be divided, so it passes by PRIMOGENITURE-style SINGLE succession. Both fire on the SAME death from the SAME hook: the eldest child gets the crown; ALL children still split the gold.

The mechanic (`lineage.succeed_titles`, **zero LLM, zero RNG**) is the second transfer inside the ONE death funnel every cause already shares — `population.announce_death` — run just before `settle_estate`, so old-age, starvation and battle deaths all succeed identically and a same-turn escheat routes to the SUCCESSOR (see below). The single heir is the ELDEST surviving kin down the SAME kin-order M4.2 uses (children → parents → siblings; age desc, name as the deterministic tiebreak). `_transfer_titles` rewrites every force-title record from the deceased's name to the heir's: the sid-keyed `monarchs`/vassal holder fields, and the name-keyed `kingdoms`/`empires` dicts are RE-KEYED (with their vassal/subject-king and discontent bookkeeping carried across), so the realm STRUCTURE survives intact under the heir. Scope is stated in comments: only FORCE titles are dynastic — trust-LEADERSHIP (M3.2, consent) is NOT hereditary and is never touched. Gated on `lineage_on` and lazily imported, so lineage-OFF runs never call it and stay byte-identical.

**The crown survives death (HEADLINE 1).** A monarch with an heir dies of old age; the eldest child is CROWNED (records transferred, coronation logged — "Aldo succeeded Wren as [monarch of S001; king of the realm of S001, S002] (eldest child)"); the realm (kingdom, vassal lordship of S002, tribute flow) CONTINUES under the heir, verified still standing after the ordinary `kingdoms.update` runs three more turns. CONTRAST shown on the same death with succession suppressed: the pre-M4.3 baseline lets the realm dissolve (the dead king's vassals break from a leaderless crown). And M4.2 is untouched: on that one death the eldest gets the CROWN (impartible) while ALL children split the GOLD equally (Aldo 24.0, Bess 24.0) — partible wealth, impartible title, both from one hook.

**Succession is a crisis test — loyalty is NOT inherited (HEADLINE 2, the milestone's core).** The heir inherits the SEAT, not the LOYALTY: `succeed_titles` copies NO trust, so a vassal's loyalty toward the heir is only what it PERSONALLY holds. Same beloved king, same realm, two heirs, two fates via the EXISTING M3.5 breakaway machinery: a COLD/distrusted heir (the vassal's personal trust in him is −3, not the +3 it held for his father) sees its vassal loyalty stay below the breakaway floor and BREAK AWAY within a few turns ("Vale BROKE AWAY from Aldo's realm — S002 is independent again"); the SAME succession where the heir already holds the vassal's personal trust (+3) HOLDS the realm. No new loyalty mechanic was added — the existing breakaway logic firing on a cold-start heir IS the feature. Weak heirs lose what their fathers built.

**Extinct lines dissolve into contestable power (HEADLINE 3).** A kinless king dies; the line is EXTINGUISHED and logged distinctly ("the line of Wren is extinguished; the crown of […] lies vacant"); `succeed_titles` moves nothing, leaving the records to clear exactly as today — the realm dissolves into independent settlements via the existing breakaway machinery, and the vacant home seat is re-contested by the ordinary organic conquest machinery (a later aspirant, Zara, musters and seizes S001). No special succession-war system: the leaderless settlements are fought over naturally.

**Multi-level and regency.** The same primogeniture rules bind at every level: an EMPEROR's death re-keys the whole `empires` record to his heir (imperial throne + his own realm + his home monarch seat all pass), and a subject-king's death passes his subordinate seat and his own realm to HIS heir. A DEPENDENT child heir inherits the seat as a historically-real REGENCY — it holds the crown (defended by its inherited garrison) but its levy/muster/war powers stay DORMANT via the existing `is_dependent_child` gate (extended to the levy loop and the conquest/war aggressor loops) until it comes of age; verified a child regent extracts zero levy and is never an aspirant. The one M4.2 interaction: a kinless estate that would escheat to a ruler who died the SAME turn routes to the SUCCESSOR (succession runs first, so `_settlement_ruler` reads the living heir).

The honest judgment (the result of M4.3 and the close of Arc 1): **crowns genuinely pass down bloodlines while LOYALTY must be re-earned by each heir — succession is a real crisis, and weak heirs lose what their fathers built.** Nothing is scripted: the heir is resolved from the agents' own `parents` links, the seat transfer is a mechanical re-keying of the existing records, and the breakaway that unseats a cold heir is the UNCHANGED M3.5/M3.6 loyalty machinery reading the heir's own (uninherited) trust — so whether a dynasty holds or fragments falls out of the heir's standing, not an assignment. Extinct lines dissolve cleanly into power the ordinary conquest machinery contests. This was confirmed emergent in a 120-turn integrated run (lineage + monarchy + kingdoms + empire, heuristic minds, seed 7): 15 old-age deaths produced 2 organic coronations (vassal lords succeeded by their eldest children) and 2 distinct extinctions (including a king holding four monarch seats whose line simply ended, leaving four vacant crowns) — dynasties that rise, rule, and fall by the quality of their heirs, with no scripting. Compose/boundaries: zero added LLM and zero RNG (eldest-first choice over sorted records, re-keying only); `--lineage` off byte-identical (succession never called; the four `is_dependent_child` guards are no-ops when lineage is off); seeded lineage-on runs reproduce byte-for-byte; M4.2's wealth split unchanged on the same deaths; leadership (M3.2) never hereditary. **208 tests green** (198 + 10 new: title transfer on every death cause + coronation logged; eldest-first order with parent/sibling fallbacks and name tiebreak; loyalty NOT copied; cold-heir breakaway vs known-heir hold; extinct-line dissolution + distinct event + contestability; leadership never hereditary; dependent-heir regency; escheat-to-successor same-turn; multi-level emperor/subject-king succession; off == byte-identical).

## V2 / M4.4 — Discontent: the pressure gauge of class conflict (Arc 2 OPENS)

The historical step: Phase 3 built a pressure engine with NO relief valve — inequality COMPOUNDS (M3.1 wage labor), monarchs LEVY by force (M3.4), tribute is EXTRACTED up a feudal hierarchy (M3.5), and dynastic heirs inherit crowns they never earned (M4.3). Until now the oppressed just STARVED QUIETLY. M4.4 opens Arc 2 (Revolt & Class Conflict) by giving that pressure a legible, verified GAUGE — per-agent DISCONTENT — so the next milestone (M4.5) can make it BLOW. Scope was held strictly to the MEASURE: `discontent.py` never takes an action against a ruler (no uprising, mob, or riot fires here — that is M4.5); it only reads existing state and writes a gauge. This boundary is stated at the top of the module and in the loop comment.

The gauge is DERIVED from EXISTING signals — no new psychology is invented. Each turn, for every settled adult (dependent children and nomads excluded via the M4.1 `is_dependent_child` gate and the settlement check), three drivers ADD grievance, all pure reads of state other systems already compute: (1) DEPRIVATION AMID PLENTY — being hungry (>= `HUNGER_PANG`) while a settlement-mate holds a large stockpile (>= `PLENTY_WEALTH`), local inequality FELT not an abstract Gini; scales with the agent's hunger fraction; the strongest driver (`DEPRIVATION_WEIGHT`=1.5). (2) EXPLOITATION — selling labor (M3.1) for a wage near the subsistence floor; grievance scales with the employer's capture share `(output − wage)/(output − subsistence)`, 1.0 at a subsistence wage, 0 at full output (`EXPLOITATION_WEIGHT`=0.8). (3) EXTRACTION — being levied by a monarch (M3.4) or tributed up a kingdom (M3.5), weighted TWO ways: by BURDEN relative to means (`due / max(wealth, MEANS_FLOOR)`, capped at 1) AND by LEGITIMACY — the agent's personal TRUST in whoever rules its settlement (`clamp(1 − LEGIT_SLOPE·trust, LEGIT_MIN, LEGIT_MAX)`), so a trusted ruler's take stings far less than a hated one's (`EXTRACTION_WEIGHT`=3.0). A consensual M3.3 leader is not "extraction" at all here and registers ZERO — consent, not theft. DECAY WITH HYSTERESIS: the gauge accumulates with NO decay while any grievance is active, and ebbs slowly (`DECAY`=0.3/turn) only when every driver is silent — so it rises fast and falls slow (grievances outlast their causes). Floored at 0, soft-capped at `DISCONTENT_CAP`=25. `settlement_pressure(sid, state)` is DERIVED on demand (never stored) — the count of a settlement's living members over `RESENTMENT_THRESHOLD`=6, the number M4.5 will trigger on. Per-agent discontent lives in `world_state["discontent"]` (a `{name: float}` map, no new `Agent` field), so an off run never calls `update`, no key is ever written, and the run is byte-identical to v1.

**Oppression raises the gauge — each driver demonstrable (HEADLINE 1).** A/B on the SAME settlement: a subject under a levying monarch accumulates discontent 4.5 over 10 turns; the identical subject free/unruled stays at 0.0; the same wealth under a CONSENSUAL M3.3 trust-leader (whom the agent trusts) registers 0.0 — consent, not extraction. Each driver ALONE over 6 turns is individually demonstrable: deprivation 7.2, exploitation 4.8, extraction 2.7, with all conditions absent a flat 0.0 — and deprivation amid plenty is provably the strongest, exactly as designed.

**Legitimacy buffers grievance (HEADLINE 2).** The SAME monarch takes the SAME levy for 10 turns, differing only in the subject's trust: a distrusted ruler (−5) draws discontent 8.1, a neutral ruler (0) draws 4.5, a trusted ruler (+5) draws just 1.3 — a trusted crown draws well under 40% of a hated one's grievance from the identical coin. Consent is exactly the difference between a tax and a theft, and it is the UNCHANGED v1 trust network doing the buffering.

**Grievances outlast their causes (HEADLINE 3).** Seven turns of hunger amid plenty climb the gauge at 1.20/turn to a peak of 8.4; feeding the agent (the grievance goes silent) then ebbs it at just 0.30/turn — it FALLS ~4x SLOWER than it rose (asymmetric slopes = hysteresis, so one good harvest does not erase a decade of oppression); sustained relief eventually returns it to a hard floor of 0.0.

**Composition — pressure is emergent (COMPOSE).** In an integrated tyranny-vs-fair-rule scene (a levying monarch's town with a wealthy commoner and two starving poor, beside a consensual leader's town of fed followers), running the ACTUAL institutions each turn (`monarchy.levy` + `taxation.update`) alongside the gauge: the two STARVING poor carry the most discontent (14.8, 13.2), the taxed wealthy commoner a moderate amount (4.2), and the monarch himself ZERO — the poorest/most-taxed carry the highest, ranked by the drivers not assigned. The tyrant's settlement shows pressure 2 (aggregate 32.2) against the fair leader's 0 (aggregate 0.0). The number M4.5 will trigger on falls out of the class engine.

The honest judgment (the real result of M4.4): **discontent genuinely TRACKS oppression — each of the three drivers is individually demonstrable and silent unless its condition holds, legitimacy materially buffers the SAME extraction (trusted 1.3 vs hated 8.1), and grievances provably outlast their causes (rising 4x faster than they decay, then flooring at 0). It is DERIVED from existing state, not an assigned mood: who resents and how much falls out of hunger, wages, levies and trust the other systems already compute.** One piece is honestly weaker than its design brief and is worth recording: the EXTRACTION driver's "burden relative to means" weighting (`due/means`) is written to make the same SUM sting a poor agent more than a rich one — but the institutions levy PROPORTIONALLY (a rate of wealth above a threshold), so `due/wealth` is roughly flat across wealth and, because of the threshold exemption, if anything mildly favors the RICH registering more burden. So on a proportional levy the burden weighting does NOT by itself make the poor resent extraction more; it mostly acts as a bounded scaler (its real, verified job is keeping a near-broke agent's grievance from exploding, via `MEANS_FLOOR` and the cap). What makes the poor carry the most discontent in the composition is the DEPRIVATION driver (hunger amid plenty), not the burden term — and that is exactly what the numbers show. This is a faithful mechanic pointed at the wrong levy shape, not a mis-computation; it becomes fully load-bearing only if/when a flat head-tax is introduced. A second caveat, shared with every emergent Phase 2/3 result: a fully ORGANIC short run produces neither a monarch nor a settlement wealthy enough to gall its hungry, so the drivers are shown in the honestly-labelled constructed verify (`verify_m44.py`) while the loop wiring runs end-to-end under `--discontent`. Compose/boundaries: zero added LLM and zero new RNG (deterministic iteration over sorted names, pure state-math — verified `{decision:0, strategy:15}` identical with the gauge on vs off, and seeded on-runs byte-identical); `--discontent` off byte-identical (the gauge is never called, no `world_state["discontent"]` key is written, and `create_world` clears the map so it cannot leak across runs); a dependent-child regent (M4.3) draws no extraction grievance since it levies nothing. Per-agent discontent shows in the AGENT SUMMARY and settlement pressure in a new SETTLEMENT PRESSURE world summary, both gated on the flag. `discontent.py` imports labor + monarchy + kingdoms + world (one-directional, a higher layer than any institution). **218 tests green** (208 + 10 new: off == byte-identical incl. no leaked key; each driver raises the gauge only when its condition holds + all-off flat zero; legitimacy buffering; extraction burden bounded by means; asymmetric decay + hard floor; no decay while a grievance persists; `settlement_pressure` counts only living above-threshold members; sparse single threshold-crossing log; regency levy draws no grievance; zero added LLM + deterministic).

## V2 / M4.5 — Uprising: the revolt fires (Arc 2 second milestone)

The historical step: M4.4 built the discontent GAUGE but it only MEASURED — the oppressed seethed and starved. M4.5 (`uprising.py`) is the RELIEF VALVE: when a settlement's resentful poor become a MAJORITY carrying enough aggregate grievance, they RISE against their force-based ruler. The mob's weapon is NUMBERS — unpaid, unarmed risers — the exact INVERSE of M3.4 conquest, where force must be BOUGHT. So a rich tyrant buys guards and crushes the rising; a tyrant whose treasury is drained falls to sheer numbers: wealth is the counter-revolutionary weapon. Scope was held strictly to the uprising EVENT: it ends with the ruler DEPOSED and the seat VACANT. WHO fills the seat — a rising's leader becoming a legitimate trust-based ruler (the REVOLUTIONARY FIGURE) — is M4.6, stated as a boundary in `_depose` and the module docstring; for now the force-title simply clears.

THE TRIGGER — a resentful MAJORITY carrying real weight (both gates documented/tunable). An uprising fires in a settlement iff BOTH hold: (1) the resentful faction (M4.4 above-threshold members, ex-ruler, ex-children, ex-defenders) is >= `UPRISING_FRACTION`=0.5 of the non-ruler members — a majority, not an absolute count, so scale doesn't distort it; AND (2) their AGGREGATE discontent >= `UPRISING_MIN_PRESSURE`=2×RESENTMENT_THRESHOLD=12.0 — so a tiny village of two mildly-annoyed people cannot "revolt". A crushed settlement is under a FEAR cooldown (`UPRISING_COOLDOWN`=8 turns) and cannot rise again until it lapses. **Only a FORCE ruler is a valid target**: the felt oppressor is resolved exactly as M4.4's `discontent._ruler_and_due` sees it — a VASSAL LORD if the settlement is a realm vassal (M3.5 tribute), else the MONARCH holding it (M3.4 levy), else None. A consent-based trust-leader (M3.2) is NEVER a target — the people chose them, so there is nothing to overthrow (revolt-immune BY CONSTRUCTION, and M4.4 already shows fair rule generates ~zero resentment — belt and braces).

THE BATTLE reuses `monarchy.resolve_battle`, INVERTED. THE MOB's force = the COUNT of risers (the resentful members themselves). THE RULER's defence = his standing garrison / loyal followers (`monarchy.defenders_of`) PLUS any mercenaries he can still MUSTER from his war chest (`monarchy.muster`, excluding the risers and existing defenders). The SAME shared battle maths decides it (strict `>`, casualties both sides via `population.announce_death` — real logged deaths + queued respawns). OUTCOMES — CRUSHED (mob loses): survivors' discontent partially reset by fear (`FEAR_RETAIN`=0.6, NOT to zero — the grievance persists and M4.4's decay/rise continues), a fear cooldown falls, the garrison shrinks to survivors, failed rising logged. VICTORIOUS (mob wins): (a) EXPROPRIATION FIRST — the ruler's whole hoard is seized and split equally among surviving risers (conserved to the exact float, residual to the first riser by name), which ZEROES the estate BEFORE the death so M4.2 `settle_estate` finds nothing — the heirs are DISINHERITED; (b) DEPOSE — the settlement SECEDES from any realm via the existing M3.5 machinery (new `kingdoms.secede_settlement`, factored out of the breakaway path), the local monarch record clears, and the ruler is killed through the normal death path — with the force-title already gone, M4.3 `succeed_titles` finds nothing to pass, so NO heir inherits the crown and the seat is left VACANT; (c) the victors' discontent drops to `VICTOR_DISCONTENT`=0.0 (the grievance is answered). Ordering (seize → clear title → announce_death) is the crux that makes Arc 1 compose correctly and prevents any double-count.

**Oppression predicts uprisings (HEADLINE 1).** Same settlement, three reigns, gauge accumulated over 12 turns then the trigger tested: under a DISTRUSTED monarch taking a levy the resentful count climbs to 3/3 and it WOULD rise (and does — an event fires when the valve runs); under a TRUSTED monarch taking the IDENTICAL levy the M4.4 legitimacy buffer keeps grievance so low the resentful count stays 0 and it never rises; a CONSENT trust-leader with a fully-resentful (hungry-amid-plenty) populace is immune by construction — no force ruler to overthrow. Oppression causes revolt; good governance is revolt-immunity.

**Wealth is the counter-revolutionary weapon (HEADLINE 2).** The SAME 3-strong penniless mob (discontent 12 each) against a monarch with a pool of poor bystanders to hire: a RICH tyrant (war chest 200) musters 5 guards → 5 > 3 → CRUSHED (2+2 fell, crown holds); the SAME tyrant DRAINED (war chest 0.5) musters 0 → 3 > 0 → DEPOSED. Same mob, two treasuries, two fates. (This also emerged in an integrated staged-kingdom CLI run: monarch Rex's 6-strong garrison crushed a nascent 2-riser rising on turn 15 — the loop wiring firing end-to-end.)

**The revolution expropriates (HEADLINE 3).** A successful rising against a monarch holding 40.0: the hoard is seized and split among the 3 risers (13.33 each, summing to exactly 40.00 — conserved to the decimal) and the ruler's adult heir receives 0.00. CONTRAST on the identical setup: the same monarch dying of OLD AGE leaves the whole 40.0 to the heir via M4.2. Revolution interrupts inheritance — the wealth goes to the peasants who rose, not the heirs.

**A house can fall (HEADLINE 4 — the Arc 1 × Arc 2 composition).** A beloved but broke old king is trusted (+5) by his commoners, who distrust (−5) his unearned adult heir. Under the old king the same levy breeds no resentment (0 resentful over a 12-turn reign) and no rising. He dies of old age → M4.3 succession CROWNS the heir monarch of the settlement. Now the identical levy, filtered through the distrusted heir's illegitimacy (M4.4), breeds grievance until the settlement RISES and DEPOSES him — the seat goes vacant and the dynasty ENDS. The House rose by conquest, passed the crown by blood (M4.3), and fell to the people its unearned heir could not command.

The honest judgment (the real result of M4.5): **revolt genuinely EMERGES from accumulated oppression — a distrusted extractor's settlement provably rises while a trusted or consent-led one provably does not; WEALTH genuinely buys counter-revolution (the same mob crushed by a rich crown, victorious over a drained one — mob force is pure numbers, so a penniless mob can win and a funded ruler can crush it); and a successful rising truly EXPROPRIATES, disinheriting the M4.2/M4.3 heirs.** No piece is scripted: the trigger reads M4.4's emergent gauge, the target is whoever the gauge says extracts, the battle is the unchanged shared maths, and who wins turns on numbers-vs-bought-guards (an independent input), not a flag. Two honest caveats, both shared with every Phase 3 force milestone. (1) The consent-immunity is TRUE BUT PARTLY DEFINITIONAL: a trust-leader is excluded from the target set by construction — the design's own choice — though it is also independently true that M4.4 gives a trusted ruler almost no grievance to begin with, so the immunity is belt-and-braces, not purely a definition. (2) A fully ORGANIC short run rarely produces a monarch rich enough to be worth overthrowing AND a resentful majority under him, for the same compounding reason all of Phase 3's force milestones do; so the four headlines are shown in the honestly-labelled constructed `verify_m45.py`, while the loop wiring firing end-to-end is demonstrated in a staged run (the turn-15 crushing above). A subtle third point worth recording: the ruler's mercenary defence is drawn from the POOR bystander pool, so in a settlement where ALL the poor are risers there is literally no one left to hire — a rich tyrant surrounded only by his own rebels falls anyway. That is a faithful (and rather pointed) consequence of reusing the M3.4 muster, not a bug: guards must be recruited from somewhere, and a universally-hated crown has no loyal poor to buy. Compose/boundaries: zero added LLM and zero new RNG (deterministic sorted iteration, reusing RNG-free `resolve_battle`; verified `{decision:0, strategy:15}` identical on vs off and two on-runs byte-identical); `--uprising` off byte-identical (never called, no cooldown key written; `create_world` clears cooldowns so nothing leaks across runs); `--uprising` implies `--discontent`; deaths route through the one death funnel so M4.2 inheritance and M4.3 succession compose correctly (both no-ops for a deposed seat because the title is cleared and the hoard seized first). `uprising.py` imports discontent + monarchy + kingdoms + population + world (one-directional — the highest institutional layer). **227 tests green** (218 + 9 new: both trigger gates required; consent-led never rises; mob=numbers penniless-wins/funded-crushes; crushed→partial-reset+cooldown+persistent-grievance; victory→deposed+title-cleared+kingdom-secession; expropriation conserved & pre-empting inheritance; deaths compose with succession/inheritance; off==byte-identical; zero added LLM + deterministic). Levers: UPRISING_FRACTION (majority bar), UPRISING_MIN_PRESSURE (aggregate floor), UPRISING_COOLDOWN (fear), FEAR_RETAIN (crushed reset depth).

## V2 / M4.6 — The Revolutionary: a rising's leader rules by consent (Arc 2 CLOSED)

The historical step: M4.5 ends a won rising with the tyrant deposed and the seat VACANT. M4.6 (in `uprising.py`, gated on the same `uprising_on`) fills it — the agent who LED the rising emerges as the settlement's new LEADER: power seized by FORCE, then LEGITIMISED by CONSENT. This closes the political cycle the whole project has circled — consent (M3.2) → force (M3.4) → revolt (M4.5) → consent again (M4.6) — and produces the project's SECOND emergent great-figure: the REVOLUTIONARY, born of grievance (where M3.4's conqueror was born of wealth). Scope held strictly: NO new institution, NO new title, NO special power — the revolutionary rules through the UNCHANGED M3.2 leadership.

WHO LEADS (derived, not assigned — `_pick_revolutionary`). Among the ORDINARY surviving risers, the mob's focal point is the one scoring highest on a WEIGHTED SUM of (a) his own DISCONTENT (the angriest — most grievance) and (b) the TRUST his FELLOW risers place in him (the M3.2 trust-cluster: the riser the mob most trusts). Equal default weights (`REV_GRIEVANCE_WEIGHT`=`REV_TRUST_WEIGHT`=1.0), tunable; deterministic name tiebreak. "Ordinary" is judged RELATIVE, not on an absolute bar: a candidate is excluded from LEADING only if his (pre-expropriation) wealth exceeds the MEDIAN rioter's by more than `REVOLUTIONARY_WEALTH_MARGIN`=15 — a clear wealth OUTLIER riding the mob (an excluded outlier still rises and fights, it just does not lead the poor it stands above). **This relative gate is a real correction found during verification**: the first cut used an absolute poor-bar (MERC_MAX_WEALTH=5), but that wrongly excludes the very people who feel EXTRACTION grievance — a member must hold wealth above the levy threshold (5) to be levied at all, so a levy-driven rising produces risers all "too rich" to lead. The median-relative margin fixes it: a settlement of equally-modest levy-payers all qualify, while a lone 100-wealth opportunist among penniless risers is excluded.

HOW HE TAKES POWER — BY CONSENT (`_seed_leadership`). The victory SEEDS his standing: every OTHER surviving riser gains `VICTORY_TRUST_BUMP`=3 trust in him (sized to clear the M3.2 FORM_TRUST bar from neutral), written through the v1 `trust.adjust_trust` (logged; a rioter who already carries a GRUDGE against him is refused — not everyone rallies). The uprising installs NO leader record itself: the UNCHANGED M3.2 `leadership.update` then ELECTS him from that trust exactly as it elects any leader — a `leaders[sid]` record, never a `monarchs[sid]` one. So he holds the seat purely by CONSENT, and if the following is too thin to clear MIN_FOLLOWERS (too few survivors), M3.2 simply seats NO ONE and the seat honestly stays vacant — never force-installed. Ordering inside a won rising: pick revolutionary (reads discontent, BEFORE the grievance is answered) → expropriate → depose → seed leadership → reset discontent.

**The mob throws up a leader (HEADLINE 1).** A won rising against a drained tyrant, risers A(disc 12), B(disc 20, trusted by A/C/Rex), C(disc 12), and Rex(disc 25 but wealth 100): the revolutionary is B — the angriest COMMONER his fellows trust, NOT the richest/angriest Rex (a wealth outlier, excluded), NOT arbitrary. The uprising installs no seat; then M3.2 `leadership.update` elects B leader of S001 (a consent record, monarch record absent). Derived from grievance + the risers' own trust.

**Power by consent, not crown (HEADLINE 2).** The revolutionary A rules S001 by consent (followers B, C). When his following erodes — B and C's trust falls below the M3.2 keep-bar — the UNCHANGED `leadership.update` strips him and the seat empties. He falls exactly like any M3.2 leader; he was never a monarch, so there is no permanent grip. The revolution gives a following, not a crown.

**The cycle closes / the revolution devours its children (HEADLINE 3).** The SAME revolutionary figure R, two paths after taking power: ruling FAIRLY (no extraction) his settlement generates no grievance and never rises against him; but if he becomes an EXTRACTOR — seizing the crown of his own settlement (M3.4), now levying by force from members who never consented — the SAME M4.4 discontent accumulates and the SAME M4.5 machinery rises against HIM and deposes him. No new mechanic; it simply composes. The wheel turns again.

**The full cycle in one run (COMPOSE).** Consent → force → revolt → consent again, end-to-end: (1) a trust-leader L rules S001 by his followers' trust (M3.2); (2) a rich outsider Tyr musters an army and CONQUERS S001 by force (the real M3.4 `attempt_conquest` — L's loyal followers die defending him, the bought army's survivors become Tyr's garrison); (3) Tyr rules the commoners by force and is distrusted, grievance builds over turns until the people RISE and depose him (M4.5), the mob throwing up revolutionary D; (4) M3.2 `leadership.update` elects D leader of S001 by consent — an M3.2 leader, not a crown. The wheel turned full circle in a single run.

The honest judgment (the result of M4.6 and the close of Arc 2): **the revolutionary genuinely EMERGES from the mob — derived from grievance and the risers' OWN trust, not assigned; he rules only by CONSENT, losable like any M3.2 leader the moment his following erodes; and the cycle truly CLOSES — a revolutionary who turns extractor is overthrown by the exact same machinery, no immunity, no new mechanic.** Nothing is scripted: the leader is the argmax of a documented score over the risers, the seat is filled by the UNCHANGED M3.2 election reading trust the victory earned (not written by fiat), and whether he keeps it turns on ongoing consent. Two honest caveats. (1) The "ordinary riser" wealth gate is RELATIVE (median-margin) precisely because an absolute one mis-fired on extraction-driven risings — a genuine tuning correction, documented, not a fudge; a settlement of uniformly-wealthy risers would still elect one of its own, which is the honest right answer (there is no poorer alternative). (2) As at every Arc-1/Arc-2 and Phase-3 milestone, the four headlines run in the honestly-labelled constructed `verify_m46.py` because organic runs rarely produce the full rich-tyrant-then-resentful-majority setup; the loop wiring firing end-to-end is shown in a staged run (a crushed rising, and the M3.2 election path exercised). Compose/boundaries: zero added LLM and zero new RNG (the pick is a deterministic argmax over sorted risers; the trust seed and M3.2 election are RNG-free — verified `{decision:0, strategy:15}` identical on vs off and two on-runs byte-identical); `--uprising` off byte-identical (the whole path is gated); `--uprising` now implies `--discontent` AND `--leadership` (the revolutionary needs the M3.2 institution to be elected). `uprising.py` gained imports of `leadership` + `trust` (one-directional; leadership imports only trust+world, so no cycle). **231 tests green** (227 + 4 new, deliberately minimal per the milestone's guidance: the revolutionary is derived-from-risers-not-assigned and rich agents are excluded; he takes a consent-seat via M3.2 and is displaceable; a revolutionary-turned-extractor is risen against; too-few-survivors leaves the seat honestly vacant). Levers: REV_GRIEVANCE_WEIGHT/REV_TRUST_WEIGHT (who leads), REVOLUTIONARY_WEALTH_MARGIN (the outlier gate), VICTORY_TRUST_BUMP (how much legitimacy the victory earns).

**Arc 2 CLOSED.** The class engine's full arc now stands end to end: inequality COMPOUNDS (M3.1) → legitimacy RESISTS and REDISTRIBUTES it (M3.2/M3.3) → wealth ESCALATES to FORCE and takes the crown (M3.4) → force SCALES into KINGDOMS and EMPIRES (M3.5/M3.6) → generations make it DYNASTIC (M4.1-M4.3) → the oppressed accumulate DISCONTENT (M4.4) → they RISE and depose the tyrant (M4.5) → and one of their own takes power by CONSENT again (M4.6). Every power structure the project builds — the wage, the leader, the crown, the realm, the empire, the dynasty, and now the revolutionary himself — is contingent on something it must keep earning, and the wheel of consent → force → revolt → consent turns without a scripted hand on it.

## V2 / M4.7 — Beliefs emerge: the inner life (Arc 3 OPENS)

The historical step: through Arc 2 agents had personalities, knowledge, trust, wealth and politics but no IDEAS ABOUT THE WORLD — no stories about what the world IS. Real institutions ran on legitimacy stories, not just trust math. M4.7 (new module `beliefs.py`) opens Arc 3 (Belief & Culture) by giving the civilization an inner life: short, fixed BELIEF strings that FORM from what an agent has actually lived through and SPREAD between agents like knowledge. Beliefs are STATE, never model-generated — ZERO LLM. Scope held strictly to formation + spread: NO priests, faiths-as-institutions, ruler-legitimacy effects, or cultural friction (those are M4.8 religion / M4.9 identity). Beliefs here are INERT culture — held, spread, clustered, but conferring no mechanical power yet; the module never grants a bonus, changes a vote, or wins a battle (stated as a boundary in the docstring).

THE CATALOGUE (8 fixed strings, 3 contradiction pairs). "the land provides" ⟷ "the world is cruel"; "we are stronger together" ⟷ "the strong take what they want"; "wealth is virtue" ⟷ "greed is a poison"; plus standalone "knowledge is power" and "the dead watch us". Each has a concrete, checkable FORMATION CONDITION read from lived state, accumulated in per-agent experience counters (`world_state["belief_exp"]`, maintained by `beliefs.update` — ZERO new Agent field): a fed streak (hunger ≤ 2 for ABUNDANCE_TURNS=8 → "the land provides"), a starvation streak (hunger ≥ 6 for HARDSHIP_TURNS=6 → "the world is cruel"), cumulative turns under a FORCE ruler (monarch/vassal-lord, EXTRACTION_TURNS=5 → "the strong take what they want"), turns in a trusting settled group / raised as a fed child (SOLIDARITY_TURNS=6 → "we are stronger together"), knowing a producer skill while fed (SKILL_FED_TURNS=3 → "knowledge is power"), deaths witnessed (a monotonic dead-count diff credited to all living, MANY_DEATHS=4 → "the dead watch us"), a wealth streak (≥ 15 for RICH_TURNS=6 → "wealth is virtue"), and hunger-amid-a-neighbour's-plenty (DEPRIVED_TURNS=5 → "greed is a poison"). Formation is a DETERMINISTIC threshold — earned, never assigned or random — and forming a belief drops its contradiction (your own lived truth is the most trusted source, overriding the old story).

SPREAD REUSES THE M1.1 DIFFUSION MODEL. `beliefs.spread` is the knowledge-diffusion pattern (turn-start snapshot, one hop, sorted-order determinism) applied to the belief set, and it calls `knowledge.adoption_probability(learner, teacher, state)` VERBATIM for the per-contact chance — so beliefs inherit the exact trust-weighting (TRUST_COEFF), personality receptivity (curious 1.6, independent 0.5), and the M4.1 childhood learning boost (a dependent child soaks up its parents' beliefs) with no parallel model. The one belief-specific rule: a CONTRADICTORY belief overwrites the incumbent one only when the learner trusts the source ≥ FLIP_TRUST (=HIGH_THRESHOLD=2) — a worldview flip needs a trusted mouth, not a stranger's. Spread draws RNG from the seeded stream exactly as knowledge diffusion does (and no-ops drawing none when nobody believes anything yet).

**Beliefs are earned, not assigned (HEADLINE 1).** Each condition binds individually: sustained abundance → "the land provides"; sustained starvation → "the world is cruel"; turns under a crown → "the strong take what they want"; a producer skill while fed → "knowledge is power"; an agent who lives through none of them → forms NOTHING. Nothing is stamped on; every belief is the output of a threshold on real state.

**Beliefs spread by trust (HEADLINE 2).** A belief adopted within 4 turns from a trusted (+5) neighbour in 29/30 trials vs a distrusted (−5) one in 10/30 — far faster from a trusted mouth (the isolation control: an agent with no contact never adopts a belief it did not live, 0). Personality shifts receptivity (curious 25/30 vs independent 8/30 in 3 turns). And a contradictory belief flips the old one only from a trusted source (+5 → renounces "the land provides" for "the world is cruel"; trust 0 → keeps its worldview). Consent of the believer, not coercion.

**Proto-cultures emerge (HEADLINE 3 — the milestone's reason to exist).** Two settlements living different lives, run through the real `beliefs.update` loop (experience → form → spread) for 15 turns and geographically separated (no cross-town contact): the PROSPER town (fed, cohesive, high mutual trust) grew a culture of "the land provides" ×4 + "we are stronger together" ×4; the SUFFER town (starving under a levying crown) grew "the world is cruel" ×3 + "the strong take what they want" ×3 + "greed is a poison" ×3. Two distinct belief systems side by side, and the suffering town's is measurably the opposite of the prosperous one's — nobody assigned a culture; shared experience plus trust-weighted spread produced one. Children compose: a dependent child took up its parent's "the world is cruel" by turn 1 through the childhood boost (culture inherited by upbringing, not blood). And it emerges in an integrated staged run too: a monarch's settlement organically grew "knowledge is power" ×5 + "the strong take what they want" ×4 + "wealth is virtue" ×2 over 40 turns.

The honest judgment (the result of M4.7): **beliefs genuinely FORM from lived experience — each of the eight conditions is individually demonstrable and a control that lives through none forms nothing, so belief is the earned output of a threshold on real state, never assigned; they SPREAD by trust EXACTLY as knowledge does (the same `adoption_probability`, so trusted-fast / distrusted-slow / isolated-never / curious-over-independent all fall out for free); and they CLUSTER into proto-cultures that differ by what a settlement has SUFFERED, with children inheriting the local worldview.** No piece is scripted: the catalogue strings are fixed but WHICH beliefs an agent holds is a pure function of its history, and WHERE they cluster is a pure function of who lived near and trusted whom. Two honest caveats. (1) The catalogue and its thresholds are hand-authored — this is deliberate (beliefs must be legible fixed strings for M4.8/M4.9 and the renderer, and ZERO-LLM rules that out generation), so the DESIGN is authored while the DISTRIBUTION is emergent; a belief the catalogue lacks simply cannot form. (2) Contradiction handling means a cohesive-but-suffering town can flip "the strong take what they want" into "we are stronger together" if solidarity also accumulates (they share both an oppressor and each other) — realistic, but it means the exact dominant set depends on which streak crosses first; the verify keeps the contrast clean by holding the suffering town's peer trust below the solidarity bar, and this ordering-sensitivity is noted rather than hidden. Compose/boundaries: ZERO LLM (fixed catalogue strings); formation deterministic (ZERO RNG), spread draws the seeded RNG like knowledge (verified two on-runs byte-identical and `{decision:0, strategy:15}` identical on vs off); `--beliefs` off byte-identical to the v1 baseline (never called, no "beliefs" key written; `create_world` clears beliefs + belief_exp so nothing leaks across runs); per-agent beliefs in the AGENT SUMMARY and per-settlement dominant sets in a new BELIEF CULTURES readout. `beliefs.py` imports knowledge (the shared adoption model) + economy (producer skills) + trust + world (one-directional — a higher layer than any of them). **236 tests green** (231 + 5 new, minimal per the milestone's guidance: each formation condition binds + a control forms none; trust-weighted spread + isolation; contradiction flip only from a trusted source; children inherit via the childhood boost; off == byte-identical + zero added LLM). Levers: the per-belief thresholds (ABUNDANCE_TURNS, HARDSHIP_TURNS, EXTRACTION_TURNS, ...) tune how much lived experience a conviction costs; FLIP_TRUST gates worldview change; the spread rate is inherited wholesale from knowledge's ADOPTION_BASE/TRUST_COEFF/PERSONALITY_ADOPT.

## V2 / M4.8 — Religion as institution: shared belief becomes power

The historical step: M4.7 gave settlements distinct dominant belief sets (proto-cultures from lived experience). M4.8 (new module `religion.py`) turns a coherent shared set into a named FAITH, raises a PROPHET from among the devout, and makes faith touch LEGITIMACY — a ruler aligned with the local faith is trusted and blessed, one who defies it is resented, so a conqueror who imposes on a hostile-faith town is harder to hold. The crown now answers to the altar. Scope held strictly to faith formation + prophet emergence + the legitimacy hook: NO theocracy (a prophet becoming the political ruler) and NO cultural friction/assimilation (M4.9), stated as boundaries in the module docstring.

THE ARCHITECTURE — faith adds NO new political mechanic; it only MOVES TRUST. The load-bearing design decision (and the reason M4.8 touches no other module's code): faith's entire effect is to nudge believers' trust in their rulers, and the UNCHANGED existing systems convert that into consequences. M4.4's `legitimacy_factor` already reads an agent's trust in its extractor to buffer/sharpen the discontent of a levy; M3.5's breakaway already reads a vassal's trust in its king; M4.5's uprising already reads discontent. So `religion.py` writes ONLY trust (through the logged `trust.adjust_trust`) and records faith state — `discontent.py`, `kingdoms.py`, `uprising.py` are all untouched, yet all three respond. This is the cleanest possible "faith is legitimacy": no bonus, no immunity, no new title — just a hand on the trust dial the whole edifice already turns on.

FAITH FORMATION (emergent, deterministic, ZERO RNG). Each turn `form_faiths` recomputes the world's faiths from current beliefs: a settlement whose LIVING members share a CORE of ≥ FAITH_CORE_MIN=2 beliefs (each held by ≥ FAITH_MAJORITY=0.5 of them), with a congregation ≥ FAITH_MIN_FOLLOWERS=3, crystallises that core into a faith. Settlements with the IDENTICAL core belong to ONE faith; divergent cores are different faiths; a fractured town forms none. The name is a fixed per-belief epithet mapping (e.g. "the land provides"→"the Bountiful Earth", "the strong take what they want"→"the Iron Truth") joined deterministically — no LLM. Faiths persist (keeping founded turn + prophet) while their core still qualifies and DISSOLVE when it erodes (M4.7 belief change), so faith tracks the living culture. THE PROPHET (`_prophet_of`, the third derived great-figure after the M3.4 conqueror and the M4.6 revolutionary): the faith's most DEVOUT follower (holds the whole core) who is TRUSTED by ≥ PROPHET_MIN_BACKERS=2 fellow believers (the M3.2 trust-cluster among co-believers), scored by devotion + trust-backing, name-tiebroken. Not the richest, not assigned; a faith with no sufficiently devout-and-trusted figure has NONE (honest). THE LEGITIMACY NUDGE (`apply_legitimacy`): for each faith settlement with a force ruler (`_sovereign` = realm king, else monarch, else trust-leader), every believer's trust in that ruler is stepped UP by FAITH_TRUST_STEP=1 if the ruler holds the core (aligned) and DOWN if not (defiant), with a PROPHET (not the ruler) adding ±PROPHET_TRUST_STEP=1 — accruing per turn like the M3.5 tribute backlash, bounded to ±FAITH_TRUST_BOUND=6 so faith cannot run away.

**Faith emerges from shared belief (HEADLINE 1).** A town where four members share {the land provides, we are stronger together} crystallises "the Faith of the Bountiful Earth and the Covenant" (4 followers); a town where every member believes something different forms none; two towns with the same core share ONE faith, and when one diverges to {the world is cruel, the strong take what they want} they split into TWO. Emergent from M4.7's clustering, not declared.

**The prophet is derived (HEADLINE 2).** Among four equally-devout followers plus a fifth who is the RICHEST, the prophet is the one the flock actually TRUSTS (Pb, trusted by the others) — not the rich Croesus, not arbitrary. A congregation that trusts no one enough has no prophet at all.

**Faith is legitimacy (HEADLINE 3 — the teeth).** The SAME king ruling a vassal town, run through `religion.update` + the UNCHANGED `kingdoms.update` for 8 turns: an ALIGNED king (who shares the town's faith) keeps his vassal's loyalty (+6) and HOLDS the realm; a DEFIANT king (who rejects it) sees the believing vassal's loyalty erode to −3 and LOSES the town to breakaway through the existing M3.5 machinery; a defiant king with an OPPOSING PROPHET erodes deeper (−4). Same crown, same extraction, two faith-alignments, two fates. The discontent side of the same coin: under the SAME levy an aligned monarch's town holds member loyalty at +6 and discontent at 1.4, while a defiant monarch's collapses to −6 and 5.0 — faith buffering M4.4's grievance for an aligned crown and sharpening it against a defiant one. And it composes with M4.7 (COMPOSE): a town whose faith was formed BY SUFFERING (the conquered "the world is cruel / the strong take what they want" belief) can be held by a conqueror who SHARES that grim creed but is lost by one alien to it — the belief a town formed by suffering now decides who can rule it.

The honest judgment (the result of M4.8): **faith genuinely EMERGES from shared belief (coherence crystallises a faith, fracture forms none, same/divergent cores share/split — all from M4.7's distribution, nothing declared); the PROPHET arises from devotion-and-trust, not assignment (provably the trusted-devout, not the richest, and absent when no figure qualifies); and faith is a REAL legitimacy force — the same ruler doing the same thing is held when aligned and undermined when defiant, feeding actual breakaways and hotter discontent.** No piece is scripted: the faith is a pure function of who believes what, the prophet is an argmax over the flock's own devotion and trust, and the teeth are entirely the EXISTING M4.4/M3.5/M4.5 systems reading trust that `religion.update` moved — `religion.py` changes no other module. It emerges organically too: a staged monarchy run grew "the Faith of the Enlightened and the Iron Truth" (knowledge-is-power + the-strong-take) with prophet Rex, its monarch aligned. Two honest caveats. (1) The catalogue epithets and the formation/prophet/nudge thresholds are hand-authored (as at M4.7): the DESIGN is authored while the DISTRIBUTION — which faiths form where, who prophesies, which crowns fall — is emergent. (2) The faith-alignment test is "the ruler holds the WHOLE core" — a binary; a partial-alignment gradient (a ruler who shares one of two core beliefs) is deliberately not modelled here (it would blur the clean aligned/defiant demonstration and is a natural M4.9 refinement), so a ruler is either blessed or condemned with nothing between. Compose/boundaries: ZERO LLM (fixed epithet strings), ZERO RNG (deterministic formation + argmax + integer trust steps — verified two on-runs byte-identical and `{decision:0, strategy:15}` identical on vs off); `--religion` off byte-identical to the beliefs-only baseline (never called, no "faiths" key written; `create_world` clears faiths so nothing leaks); `--religion` implies `--beliefs`. Faiths/cores/followers/prophet in a new FAITHS readout, per-agent Faith line, per-settlement ruler alignment in the summary. `religion.py` imports beliefs + trust + world only (one-directional — it reaches DOWN to move trust, never sideways into the institutions it influences). **241 tests green** (236 + 5 new, minimal per guidance: faith forms on coherence / not when fractured / same-vs-divergent cores; prophet derived not assigned + none when untrusted; aligned-vs-defiant discontent+loyalty delta; defiant-king vassal erosion past the breakaway floor + prophet amplifies; off == byte-identical + zero LLM). Levers: FAITH_MAJORITY/FAITH_CORE_MIN/FAITH_MIN_FOLLOWERS (how coherent a culture must be to become a faith), PROPHET_MIN_BACKERS (how trusted a prophet must be), FAITH_TRUST_STEP/PROPHET_TRUST_STEP/FAITH_TRUST_BOUND (how hard faith moves the loyalty dial).

## V2 / M4.9 — Cultural identity & friction: the imperial problem (Arc 3 CLOSED)

The historical step: M4.7 gave settlements distinct belief sets; M4.8 made a shared set a faith with legitimacy power. M4.9 (new module `culture.py`) bundles belief + faith into a CULTURAL IDENTITY and creates the deep imperial problem — conquering a FOREIGN culture breeds chronic unrest and takes GENERATIONS to assimilate: you can seize a people in a day but not make them yours for decades. It CLOSES Arc 3. Scope held to cultural identity + conquest friction + generational assimilation, with an explicit boundary: NO separate named "independence movement" — cultural revolt EMERGES from the existing discontent (M4.5) and breakaway (M3.5) machinery.

THE ARCHITECTURE (same discipline as M4.8): culture adds NO new political mechanic and changes NO other module's code. Its whole effect is to SUSTAIN the trust dial — a foreign-ruled province's members withdraw loyalty from their alien ruler EVERY turn — and, for assimilation, to write children's beliefs (M4.7 state). The UNCHANGED M4.4 (discontent), M3.5 (breakaway) and M4.5 (uprising) systems read that trust and turn it into consequences; `culture.py` imports only beliefs + religion (for `_sovereign`) + trust + world, reaching DOWN to move trust, never sideways into the institutions it destabilises.

CULTURAL IDENTITY (derived). `culture_signature(sid)` = the beliefs a CULTURE_MAJORITY=0.5 of a settlement's living members hold (its dominant set, bundling M4.7 beliefs and the M4.8 faith through them). `same_culture(a, b)` compares two agents by Jaccard overlap of their belief sets (≥ CULTURE_SIM_THRESHOLD=0.5). An agent is FOREIGN to a culture if it shares fewer than CULTURE_SHARE=0.5 of the signature. CONQUEST FRICTION (`apply_friction`, deterministic — ZERO RNG): each turn, in a settlement whose force ruler (`religion._sovereign`: realm king / monarch / leader) is alien to its culture, every member STILL foreign to that ruler withdraws CULTURE_TRUST_STEP=1 trust from him (bounded ±CULTURE_TRUST_BOUND=6) — a chronic loyalty tax the same extraction never levies on a same-culture crown, and the assimilated stop paying it. GENERATIONAL ASSIMILATION (`assimilate`, the module's only RNG, gated): under sustained foreign rule, dependent CHILDREN (the M4.1 gate) adopt one of the ruler's beliefs per turn at ASSIMILATION_RATE=0.15 (dropping its M4.7 contradiction — the ruling culture is the dominant source), while ADULTS keep theirs; `assimilation_progress(sid)` is the fraction of members who have taken on the ruler's culture, rising monotonically as generations turn.

**Same vs foreign conquest (HEADLINE 1).** A same-culture conqueror holds member loyalty at +0 and discontent at 3.2; a foreign-culture conqueror doing the SAME levy collapses it to −6 and 5.0 — chronic, every turn. And it feeds the existing machinery: a same-culture king HOLDS his conquered vassal province while a foreign-culture king LOSES it to breakaway (through the unchanged M3.5 loyalty erosion). Same conqueror, two cultures, two fates — the foreign province fractures where the same-culture one holds.

**Assimilation takes generations (HEADLINE 2).** In a child-rich foreign-ruled town, assimilation is 25% after one turn and drifts to 50% over ~60 turns as the children take on the ruler's culture while the one adult holdout keeps hers (verified: child K1 ends holding the ruler's "the strong take what they want", the Elder still "we are stronger together") — and once the young (the majority) have assimilated, the town's signature drifts, the ruler is no longer foreign, and the FAULT LINE FADES. The RACE, both outcomes shown: a child-rich town ASSIMILATES (the fault heals) while a childless foreign province, with no one to assimilate, BREAKS AWAY first (the fault fractures the realm before its culture could ever drift). Neither is scripted — the outcome falls out of the town's demographics.

**The multi-cultural empire fragments along cultural lines (HEADLINE 3).** A native-culture king ruling four provinces — two of his own culture, two foreign (divergent beliefs, the M4.7 conquered proto-culture) — run through `culture.update` + the unchanged `kingdoms.update` for 10 turns: BOTH foreign provinces break away while the same-culture core holds. The realm fragments along its cultural fault lines, not randomly. (Constructed scenario, honestly labelled — and it needed the king's war chest zeroed so the organic re-conquest loop stayed out and the CULTURAL breakaway was isolated.)

The honest judgment (the result of M4.9 and the CLOSE of Arc 3): **foreign conquest genuinely breeds chronic unrest a same-culture conquest avoids (same levy, +0/3.2 vs −6/5.0, and the province fractures); assimilation truly takes GENERATIONS via children not turns (the young drift, the adults never do, and the fault heals only once a generation has turned over); and multi-cultural empires fragment along CULTURAL fault lines (both foreign provinces break, the core holds).** No piece is scripted: culture is a pure function of who believes what, foreign-ness is a set-overlap test, the friction is an integer loyalty tax on exactly the members who don't share the ruler's culture, and every downstream consequence is the existing trust/discontent/loyalty machinery reading a dial culture merely sustained — cultural revolt is literally the M3.5/M4.5 systems firing on culturally-eroded trust, never a bespoke "independence" mechanic. Three honest caveats. (1) Foreign-ness and alignment are BINARY set-overlap thresholds (share ≥ half the signature or not); a continuous cultural-distance gradient is deliberately not modelled (it would blur the clean two-fates demonstrations) — a natural refinement, not a gap. (2) Assimilation requires `--lineage` (it works through dependent children, the M4.1 gate), so the generational drift only manifests when lineage is on — the friction teeth work without it, but "assimilation" as such needs generations to exist. (3) As at every constructed-demo milestone, HEADLINE 3's clean fragmentation needed the king's war chest zeroed to keep the organic re-conquest loop from re-vassalising broken-away provinces (the two systems genuinely interact — a rich king CAN reconquer a seceded town, which is correct behaviour, just not what H3 is isolating); the friction/assimilation mechanics themselves draw no such staging. Compose/boundaries: ZERO LLM (culture is derived STATE), friction deterministic + assimilation seeded (verified two on-runs byte-identical and `{decision:0, strategy:15}` identical on vs off); `--culture` off byte-identical to the religion-only baseline (never called; `create_world` resets the flag — culture holds no state of its own beyond the beliefs/trust the lower layers already reset); `--culture` implies `--religion` (→ `--beliefs`). Per-settlement culture signature, foreign-rule flag and assimilation progress in a new CULTURES readout. **246 tests green** (241 + 5 new, minimal per guidance: same-vs-foreign chronic friction delta; foreign province breaks away where same-culture holds; children assimilate while adults don't; assimilation completes over generations and the fault line fades; off == byte-identical + zero LLM). Levers: CULTURE_MAJORITY/CULTURE_SHARE/CULTURE_SIM_THRESHOLD (how a culture is drawn and who counts as foreign), CULTURE_TRUST_STEP/CULTURE_TRUST_BOUND (how hard foreign rule chafes), ASSIMILATION_RATE (how many generations it takes to make a people yours).

**Arc 3 CLOSED.** The civilization now has an inner life that becomes history: agents form BELIEFS from what they live (M4.7), a coherent creed becomes a FAITH with a prophet and legitimacy power (M4.8), and belief + faith bundle into a CULTURAL IDENTITY whose friction is the permanent fault line of every empire (M4.9). Layered on Arc 1 (the House) and Arc 2 (the class engine and its revolts), the model now generates the full texture of a civilization — who has power, who resents it, what people believe, and why an empire of many peoples can conquer everything and still fly apart.

## V2 / M4.10 — Writing & records: institutional memory (Arc 4 OPENS)

The historical step: until now everything died with its holder — a ruler's policy at death, a skill when its last knower died (the knowledge-extinction collapse a civilization that forgets farming can't recover from), history the moment it scrolled past. WRITING (new module `writing.py`) is the keystone of the modernity arc: recorded things OUTLIVE their makers. A settlement that becomes LITERATE gains three powers of the written word — persistent LAW, knowledge PRESERVATION and recorded HISTORY — so its institutions, skills and memory accumulate across generations instead of resetting at every death. It is the substrate the Chronicle (Arc 6) will read. Scope held to writing-as-a-tech + the three powers; LAW is minimal (written policy survives succession; NO courts/enforcement/disputes), stated as a boundary. Zero LLM — records are STATE, never prose.

WRITING IS A TECH (reuses the M1.2 machinery). `discover_writing` lets an agent invent `writing` only with the prior tech `tools` AND from a SETTLEMENT holding a food SURPLUS (`lineage.settlement_surplus`) — scribes need stability and spare capacity — with the chance drawn from the EXISTING `knowledge.discovery_probability` (personality/hunger-shaped, probabilistic, not a timer), and once known it SPREADS through the ordinary M1.1 `knowledge.diffuse` like any skill (writing is not proprietary, so it diffuses freely). A settlement is LITERATE once ≥ LITERACY_MIN=1 living member can write (`is_literate`). So writing is earned and unscripted exactly like fire/tools/farming — verified: with tools + settlement + surplus a scribe invents it; knock out ANY of the three (no surplus, no tools, no settlement) and no writing ever appears; from four tools-holding settlers in a surplus town it was invented and spread the town to literacy by turn 2, and it emerges organically in a staged run (S001 became literate with a full archive of every tech). `--writing` implies `--tech-tree` (writing's prereq `tools` lives there).

THE THREE POWERS, each purely additive STATE (no other module's code changes). (a) PERSISTENT LAW (`enact_laws`): in a literate ruled settlement the ruler's policy is inscribed into `laws[sid]` (tax/levy/tribute framework + set_by + turn); when the ruler has since changed — a succession — the heir INHERITS the standing written law (its policy persists, only `set_by` updates, `inherited_from` records the predecessor). An ILLITERATE settlement never gets a `laws[sid]` entry, so its policy dies with its ruler. (b) KNOWLEDGE PRESERVATION (`preserve_knowledge`): a literate settlement archives every ARCHIVABLE tech a living member knows into `archives[sid]`, then RE-TEACHES any archived tech NO living member currently knows to one living adult member from the records — deterministically, ZERO RNG — so a skill can never go extinct while a literate town stands. An illiterate town has no archive and loses a skill with its last knower. (c) RECORDED HISTORY (`record_history`): this turn's MAJOR events (matched on a keyword set — foundings, coronations, wars, uprisings, secessions, extinctions) that NAME a literate settlement are appended to its persistent `chronicles[sid]` as structured `{turn, event}` entries; minor ticks (trust changes, routine levies) are omitted, and an illiterate settlement records nothing.

**Writing emerges as a tech (HEADLINE 1).** Tools + settlement + surplus → invents writing; remove the surplus, the tools, or the settlement and it never does. From tools-holding settlers in a surplus town it was invented and spread to literacy by turn 2 — earned through the existing discovery+diffusion, prereqs binding, no scripting.

**Institutions outlive individuals (HEADLINE 2).** A literate King inscribes a tax/levy law; he dies of old age and the M4.3 heir INHERITS the written law (`set_by: Heir, inherited_from: King` — the policy carried across intact); the IDENTICAL succession in an illiterate town leaves the written law `None` — a blank slate. Same event, literacy vs not, two outcomes: literacy makes the institution outlive the individual.

**Literacy cures knowledge extinction (HEADLINE 3 — the composition payoff).** Two towns both know farming; the last farmer dies and the fields are barren. The LITERATE town RE-TEACHES farming from its records and keeps producing food (5 tiles grown over 9 turns, farming alive); the ILLITERATE town's farming is gone forever and the fields stay barren (0 food, the town starves). Writing is precisely the cure for the knowledge-extinction collapse the earlier finding observed — a literate civilization cannot forget farming.

The honest judgment (the result of M4.10): **writing genuinely EMERGES as a tech (invented via the existing M1.2 discovery under binding prereqs, spread via M1.1 diffusion, varying by who is fed/curious/lucky); institutions genuinely OUTLIVE their makers only where literacy exists (the heir inherits the written law, the archive re-teaches a dead skill, the chronicle persists — each with an illiterate contrast that loses it); and literacy provably CURES the knowledge-extinction collapse (the literate town re-grows food where the illiterate one goes barren).** No piece is scripted: whether a town becomes literate falls out of the discovery machinery, and every power keys purely on `is_literate` reading who knows the writing skill. Three honest caveats. (1) LAW is a persistent POLICY RECORD, not a live rate the institutions read: the tax/levy values are captured from the global world-state rates, so "the heir inherits the framework" is demonstrated as the record surviving succession (literate) vs vanishing (illiterate) — deliberately minimal per scope (no courts/enforcement), and wiring the institutions to govern by per-settlement written rates is a natural follow-on, not done here. (2) The re-teaching is DETERMINISTIC and immediate (a forgotten archived skill is restored the same turn to a stable adult) rather than a gradual re-learning — chosen so the cure is crisp and reproducible; a slower scholarly recovery would be a tuning change, not a new mechanic. (3) The chronicle captures major events that NAME the settlement (uprisings, coronations, secessions), so a faith-scoped event like a prophet's rise — which names the faith, not the town — is NOT chronicled by the sid-match; this is a documented limitation of the keyword+sid filter, not a claim that every conceivable major is recorded. Compose/boundaries: ZERO LLM (records are structured STATE), only writing DISCOVERY draws RNG (law/archive/chronicle are deterministic — verified two on-runs byte-identical and `{decision:0, strategy:17}` identical on vs off); `--writing` off byte-identical to the tech-tree baseline (never called; `create_world` clears laws/archives/chronicles); `--writing` implies `--tech-tree`. Literacy, written law, archive and chronicle length show in a new WRITTEN RECORDS readout. `writing.py` imports knowledge (discovery model + the tech vocabulary) + lineage (the surplus read) + world (one-directional — a higher layer). **251 tests green** (246 + 5 new, minimal per guidance: writing prereqs bind; heir inherits written law only when literate; literacy re-teaches a wiped skill while an illiterate town loses it; chronicle accumulates only when literate; off == byte-identical + zero LLM). Levers: LITERACY_MIN (how many scribes make a town literate), ARCHIVABLE (which knowledge is preserved), MAJOR_KEYWORDS (what counts as chronicle-worthy history), and writing's prereqs (tools + surplus) for how hard literacy is to reach.

**Arc 4 OPENS.** With memory escaping the individual, the civilization gains the power to ACCUMULATE — the precondition for the rest of the modernity road: institutions that compound instead of resetting, knowledge that ratchets instead of oscillating, and a written past for the Chronicle to one day narrate.

## V2 / M4.11 — Metallurgy & arms: technology transforms war and work

The historical step: M4.10 made tech act on institutions (memory). M4.11 (new module `metallurgy.py`) makes it transform the MATERIAL balance of power — better tools strengthen the economy, and ARMS multiply force in battle, so KNOWLEDGE starts to beat NUMBERS and control of the knowledge of arms becomes politically decisive. Scope held to a metallurgy tech branch with two effects (tool yield + armed force multiplier) and its compositions; full era progression (Bronze/Iron eras + visible town evolution) is M4.12, stated as a boundary. Zero LLM.

METALLURGY AS A TECH (reuses M1.2). A small chain past `tools`: `metalworking` (better tools, prereq tools) then `weapons` (arms, prereq metalworking), invented by `metallurgy.discover` only from a SETTLEMENT holding a food SURPLUS (a forge needs a stable, provisioned town — the same environmental gate writing uses), with the chance drawn from the EXISTING `knowledge.discovery_probability` and a turn-start snapshot preventing a within-turn tools→metalworking→weapons cascade. Once known, both spread through the ordinary M1.1 `knowledge.diffuse` like any skill (not proprietary → free diffusion). Verified: metalworking needs tools+settlement+surplus and weapons needs metalworking — knock out any prereq and neither appears.

THE TWO EFFECTS, each keyed on the skill so a non-metallurgy run is byte-identical. (1) BETTER TOOLS (economy): `knowledge.farm` grows food at `METALWORK_YIELD_MULT`=2.0 the base rate for a farmer who knows `metalworking` — gated on `metallurgy_on` AND the skill, and (critically) the per-farmer DRAW COUNT is unchanged (only the success threshold moves), so a run without metalworking is byte-identical. (2) ARMS MULTIPLY FORCE (the teeth): `monarchy.resolve_battle` — the shared math reused by M3.4 conquest, M3.5/M3.6 war AND M4.5 uprising — now decides on effective FORCE via `metallurgy.combat_force`, where an ARMED combatant (knows `weapons`) counts `ARMED_MULTIPLIER`=1.8 of an unarmed head; casualties stay capped at the real body COUNT (weapons win fights, not conjure corpses). When NO combatant is armed the force is exactly the head count and every half-integer casualty rounds identically, so the resolution is byte-identical to before — verified across the full suite (all prior battle tests unchanged) and by the off-run golden comparison.

**Metallurgy emerges + strengthens the economy (HEADLINE 1).** Prereqs bind (tools + settlement + surplus for metalworking; metalworking for weapons). Over ~60 turns a neolithic farmer grew 29 food where a metalworking farmer grew 59 — a metallurgical people is materially more productive, unscripted and emergent from the yield lever.

**Knowledge beats numbers (HEADLINE 2).** In the shared battle math 3 ARMED beat 4 unarmed (1.8×3 = 5.4 > 4), 3 unarmed lose to 4 unarmed (numbers), and 3 armed lose to 4 armed (equal arms → back to numbers). And it decides a REAL conquest through the unchanged institution path: a warlord's 3 armed mercenaries SEIZED a town defended by 5 unarmed militia (`attempt_conquest`, fewer fighters, won) — so the multiplier tilts conquest, inter-kingdom war and uprising alike.

**Arms and the revolt balance (HEADLINE 3 — the sharp composition).** A mob of 5 rises against a garrison of 3: with the ruler's guard ARMED and the mob bare-handed the mob is CRUSHED (5 vs 5.4 — steel beats numbers, and M4.5's "wealth buys counter-revolution" gains real teeth); arm the COMMONERS too and the mob WINS again (9 vs 5.4 — numbers decide); with neither armed the numbers win as before. Same uprising, three arms-configs, three fates — because arms spread by tech diffusion, WHO is allowed to know weapons determines WHO can resist. This composition emerges with no uprising-specific code: `resolve_battle` reads the combatants' knowledge and the M4.5 machinery does the rest.

The honest judgment (the result of M4.11): **metallurgy genuinely EMERGES as a tech (invented under binding prereqs, spread by diffusion, varying by who is fed/curious/lucky); it genuinely strengthens the economy (a 2× yield gap) and multiplies force so knowledge beats numbers (a smaller armed host provably beats a larger unarmed one, in a real conquest, and equal arms fall back to a count); and control of weapon-knowledge genuinely shifts the REVOLT balance (armed rulers crush unarmed mobs; armed commoners revolt anyway).** No piece is scripted: the force comparison is a pure weighted sum over the combatants' own `weapons` knowledge, the yield boost a pure function of the farmer's `metalworking` knowledge, and every downstream fate (who conquers, who holds, who revolts) is the EXISTING battle machinery reading those weights — `resolve_battle` is the only battle edit and it is a no-op without arms. Three honest caveats. (1) `ARMED_MULTIPLIER`=1.8 and `METALWORK_YIELD_MULT`=2.0 are hand-tuned magnitudes — the DESIGN is authored, the OUTCOMES (who out-produces/out-fights whom) emergent; a different multiplier would move the exact thresholds but not the qualitative story. (2) The loop's WINNABLE-assault guards (`fieldable_force`, `realm_host_size`) still estimate by head COUNT, not arms, so a commander may occasionally misjudge an armed matchup — a conservative heuristic, not a resolution bug (the actual battle always resolves on arms); wiring the guards to arms too is a natural follow-on. (3) A dependent child that has learned weapons is still filtered out of every battle line by the M4.1 gate (as with all combat), so "armed" only counts adults — correct, but worth stating. Compose/boundaries: ZERO LLM, only metallurgy DISCOVERY draws RNG (the yield boost and force multiplier are deterministic reads — verified two on-runs byte-identical and `{decision:0, strategy:17}` identical on vs off); `--metallurgy` off byte-identical to the tech-tree baseline (never invented; the farm boost and battle multiplier no-op without the skills; `create_world` resets only the flag — metallurgy holds no world_state of its own); `--metallurgy` implies `--tech-tree`. Forge/armed status per settlement in a new METALLURGY readout. `metallurgy.py` imports knowledge (discovery model) + lineage (surplus read) + world; `monarchy.resolve_battle` lazily imports metallurgy for the force weighting (no load-time cycle); `knowledge.farm` gained a skill-gated multiplier (no import — a local constant + the `metalworking` literal). **256 tests green** (251 + 5 new, minimal per guidance: metallurgy prereqs bind; metalworking boosts farm yield; armed multiplier in battle (smaller-armed-beats-larger-unarmed / equal→numbers / unarmed-tie-held-by-defender); uprising arms balance (ruler-armed crushes / both-armed mob wins / neither→numbers); off == byte-identical + zero LLM). Levers: ARMED_MULTIPLIER (how far steel beats numbers), METALWORK_YIELD_MULT (the economic edge), and metallurgy's prereqs (tools + surplus) for how hard the forge is to reach.

Arc 4 continues: with tech now transforming both the economy and the balance of force, technology has become POWER — the accumulation M4.10 unlocked starts to compound into a material edge, and who knows how to make weapons decides who rules.

## V2 / M4.12 — Era progression: the march of ages (Arc 4 CLOSED)

The historical step: the world had scattered techs but no sense of ADVANCEMENT through ages. M4.12 (new module `eras.py`) structures the tech tree into ordered ERAS (Neolithic → Bronze → Iron → …), each transforming the economy, war, and a settlement's visible appearance — so a civilization visibly marches from the stone age toward modernity. This CLOSES Arc 4 and realises the long-standing "stone age to modern" goal. Scope held to the era MACHINERY + the first three eras, built so higher eras are a pure data addition — stated as a boundary (a marked "add an era here" seam in `eras.py`).

ERA IS DERIVED FROM MASTERED TECH (emergent, not declared). The `ERAS` ladder is an ordered list of `Era(name, techs, yield_mult, style)` — Neolithic (fire+tools+farming), Bronze (+metalworking), Iron (+weapons+writing), each tech-set CUMULATIVE. `settlement_era(sid)` is the highest rung whose techs are a subset of the UNION of the settlement's living members' knowledge; as the existing discovery/diffusion spreads techs, a town crosses the next threshold and `eras.update` logs the ADVANCE ("S001 entered the Bronze Age") and caches the era to `world_state["eras"][sid]`. Adding a fourth era is one appended tuple — no new machinery reads anything but the list (verified: a hypothetical Steel Age slots straight in once its tech is known).

THREE TRANSFORMATIONS, each composing with an existing system and each byte-identical when the flag is off. (a) ECONOMY: `knowledge.farm` lifts the production CEILING along the era curve — the expected tiles/turn is FARM_YIELD × era_yield_mult, realised as int(exp) guaranteed tiles plus one more on the fractional chance (one draw/farmer), so a higher era genuinely OUT-YIELDS a lower one (Iron 2.8× > Bronze 2.0× > Neolithic 1.0×) instead of saturating at the old probability cap — and the non-era path (one draw, one tile) is preserved exactly for byte-identical off. (b) WAR: `monarchy.resolve_battle` now decides on `eras.combat_force`, where a soldier's MARTIAL-era weight (weapons → 1.8 iron, metalworking → 1.4 bronze, else 1.0 stone) multiplies its head — keyed on the soldier's OWN martial tech (NOT the full collective era set, since a soldier need not farm or write), so the intra-town arms distinction of M4.5/M4.11 survives AND an iron host out-weights a neolithic one between towns. With eras off it DELEGATES to the M4.11 `metallurgy.combat_force` (armed multiplier, or a plain head count when metallurgy is also off), so the battle math is byte-identical. (c) APPEARANCE: the era is exposed in `world_state["eras"]` and the READ-ONLY renderer's `build_town_plan` keys its building style off it — Neolithic earthy huts → Bronze timber + a forge → Iron grey dressed stone + a wall ring — so towns visibly evolve; the renderer only reads the era string and maps it to a style (no new renderer import, so the read-only AST boundary test stays green).

**Eras emerge from tech (HEADLINE 1).** A settled populace mastering fire+tools+farming is Neolithic; add metalworking → Bronze Age (logged); add weapons+writing → Iron Age (logged). Two towns in the same turn diverge — the one that took up metalworking is Bronze while the one that did not LAGS at Neolithic — so eras are derived and seed-varying, never declared.

**Tech advantage compounds in war (HEADLINE 2).** In the shared battle math 3 Iron beat 5 Neolithic (1.8×3 = 5.4 > 5), 3 Bronze beat 4 Neolithic (1.4×3 = 4.2 > 4), and 4 Iron vs 4 Iron falls back to numbers (a tie held by the defender). And it decides a REAL conquest: a warlord's 3 Iron soldiers SEIZED a town of 5 Neolithic militia (`attempt_conquest`, fewer fighters, won) — knowledge beats numbers on an era curve, in conquest, war and uprising alike.

**Advanced economies out-produce (HEADLINE 3).** Over ~50 turns a Neolithic farmer grew 23 food, a Bronze farmer 49, an Iron farmer 70 — a clean, monotonic era curve; a more advanced settlement is materially richer.

The honest judgment (the result of M4.12 and the CLOSE of Arc 4): **eras genuinely EMERGE from mastered tech (thresholds binding, advances earned and logged, towns diverging by what they've discovered — nothing declared); an era advantage genuinely COMPOUNDS (a smaller advanced host provably beats a larger primitive one in the shared battle math and a real conquest, and out-produces it on the yield curve); towns VISIBLY evolve by age (the era drives the renderer's building style); and the ladder is trivially EXTENSIBLE (a fourth era is one appended tuple, verified to slot in with no new code).** No piece is scripted: the era is a pure set-derivation over the populace's own knowledge, the combat weight a pure function of each soldier's martial tech, the yield a pure function of the settlement's era, and every downstream fate (who advances, who out-fights or out-grows whom) is the existing discovery/battle/farm machinery reading those derivations. Three honest caveats. (1) The era tech-sets, yield multipliers (1.0/2.0/2.8) and combat weights (1.0/1.4/1.8) are hand-authored magnitudes — the DESIGN is authored, the DISTRIBUTION (which town reaches which age, and when) emergent; a different curve moves the thresholds, not the qualitative story. (2) COMBAT keys on a soldier's individual martial tech while ECONOMY/APPEARANCE key on the settlement's collective era — a deliberate split (a town is "Iron age" once it collectively has the techs, but a bare-handed peasant from that town still fights at stone weight), which is the coherent model but worth stating so the two "era" notions aren't conflated. (3) Fully organic runs reach the higher eras SLOWLY (the full fire→tools→farming→metalworking→weapons/writing chain is a long probabilistic climb, and short runs stay Neolithic), so the era transformations are shown in the honestly-labelled constructed `verify_m412.py`; the machinery (advance-logging, the summary/renderer hooks) runs end-to-end under `--eras`. Compose/boundaries: ZERO LLM and ZERO RNG (era is a pure derivation; the yield curve's one draw/farmer replaces — never adds to — the base draw; verified two on-runs byte-identical and `{decision:0, strategy:17}` identical on vs off); `--eras` off byte-identical to the metallurgy+writing baseline (never called; combat delegates to M4.11; `create_world` clears the era cache); `--eras` implies `--metallurgy` + `--writing` (so the gating techs are reachable). Per-settlement era in a new ERAS readout; the era drives the renderer building style. `eras.py` imports metallurgy (the M4.11 combat fallback) + world; `monarchy.resolve_battle` and `knowledge.farm` lazily import eras (no load-time cycle); the renderer reads the era string from state (no sim import). **261 tests green** (256 + 5 new, minimal per guidance: era derived from tech thresholds + advance logged + extensible; era gap as combat multiplier; era yield curve; era exposed in state + drives rendering; off == byte-identical + zero LLM). Levers: the `ERAS` ladder itself (tech-sets, yield_mult, style per era — the whole design surface), and the martial combat weights.

**Arc 4 CLOSED.** The road to modernity is paved: memory escaped the individual (M4.10 writing), technology became material power (M4.11 metallurgy), and now the civilization marches through structured AGES (M4.12), each transforming its economy, its wars, and its very appearance — with the engine built so history can be extended toward the modern world by adding eras, not code. Layered on Arcs 1–3 (the House, the class engine, and the inner life), the model now generates a civilization that not only lives, rules, believes and fights — but ADVANCES.

## V2 / M4.13 — Relations & treaties: kingdoms gain a second verb (Arc 5 OPENS)

The historical step: kingdoms had exactly ONE interaction — opportunistic WAR (M3.6). M4.13 (new module `diplomacy.py`) gives every kingdom-PAIR a STANCE (hostile/neutral/friendly) derived from their shared history, and TREATIES — non-aggression pacts and defensive alliances — that CONSTRAIN the existing war machinery: a pact stops a war the M3.6 loop would launch; an alliance brings a friend's host to a defender's aid. War stops being a kingdom's only language. This opens Arc 5 (the interstate system) and is the substrate for trade (M4.14) and coalitions (M4.15) — scope held there, with a marked `trade volume → warmer` seam left for M4.14. Treaties EMERGE from stance thresholds + conditions — NO agent deliberation, NO LLM.

STANCE IS DERIVED FROM HISTORY (emergent, not declared). Each unordered pair of sovereign kings carries an integer score in `world_state["diplomacy"]["stance"]`. Each turn `diplomacy.update` reconciles (drops entries for kings who are no longer living sovereigns), then WARMS a pair sharing culture (M4.7-9 `same_culture`) or faith (M4.8) toward friendly (IDENTITY_WARMTH/turn, capped) while DECAYING an unrelated pair toward neutral (old feeling fades). One-off shocks are applied where history is made: a WAR between them (`record_war`, called from `empire.wage_war` when diplomacy is on) drops the score toward hostile; a BROKEN treaty adds a betrayal penalty. `stance(k1,k2)` maps the score to hostile (≤ −3) / neutral / friendly (≥ +3).

THE TWO TREATIES, each purely additive and each byte-identical when the flag is off. NON-AGGRESSION PACT: a pair whose stance is warm enough (≥ PACT_FORM_AT) forms a pact, and the M3.6 war loop reads `diplomacy.war_forbidden` and REFRAINS from launching a war between them; the pact BREAKS when the stance sours past PACT_BREAK_AT — a distinct betrayal event that sours the pair further. DEFENSIVE ALLIANCE (a warmer tier, ≥ ALLIANCE_FORM_AT): when one ally is attacked, `empire.wage_war` adds every honouring ally's whole loyal host (`imperial_host`) to the defence, and the M3.6 loop's winnable-guard weighs the defender's host PLUS its allies' (`defensive_host_size`) so an alliance DETERS an attack it would otherwise launch — but honour is CONDITIONAL: `defensive_allies` excludes any ally whose stance with the defender has soured below HONOUR_AT, so a soured ally does not answer. Treaties are PERSONAL to the ruler: `_reconcile` drops any treaty referencing a king who has died or been subjugated, so a new reign must re-earn its peace — a cold heir does not inherit his father's pact.

**Stance emerges from history (HEADLINE 1).** Two fresh kingdoms are neutral; after they go to war the pair is hostile (−6); over ten quiet turns the grievance fades back to neutral; and two kings of one culture warm to friendly (+6) over seven turns. Stance is derived from what two crowns have shared and suffered, never assigned.

**Diplomacy prevents war (HEADLINE 2).** Strong Rich beside weak Poor: with no pact the M3.6 loop launches the war and subjugates Poor the same turn; with a non-aggression pact Poor survives (the war is stayed). And when the stance sours the pact is BROKEN (a logged betrayal) and the war fires — same pair, pact vs no pact, two outcomes. A pact genuinely prevents a war the machinery would otherwise launch.

**Alliances shift wars (HEADLINE 3).** Rich (host 5) beats a lone Poor (host 2); but with an honouring Ally (host 4) the defence grows to 6 and Rich LOSES; with a SOURED ally (stance −5 < honour line) the ally sits out, the defence stays 2, and Rich wins. Attacking one member of an alliance means fighting all who honour it — but honour is conditional, so alliances are never ironclad. And it composes with M4.3 (COMPOSE): King Peace's pact with Rich holds his kingdom safe; he dies of old age and his heir is crowned (M4.3) — but the pact was personal, so it lapses, Rich attacks the untreatied heir, and the heir's kingdom falls. The crown passes by blood; the alliances do not — echoing M4.3's "the heir inherits the seat, not the loyalty."

The honest judgment (the result of M4.13): **stance genuinely EMERGES from history (wars cool, culture/faith warm, betrayals sour, quiet fades — a pure function of what two kingdoms have done to and with each other, never assigned); non-aggression pacts genuinely PREVENT wars the machinery would otherwise launch (the M3.6 loop reads the treaty and refrains, verified against the identical pair with no pact); and alliances genuinely SHIFT war outcomes (an attacker who beats a lone defender loses to the combined hosts) while staying CONDITIONAL on honour (a soured ally does not answer).** No piece is scripted: the stance is deterministic integer accumulation over the pair's own history, treaties form and break on documented thresholds, and every war consequence is the EXISTING M3.6 machinery reading a stance/treaty that diplomacy merely maintained — `diplomacy.py` acts only by exposing pacts/allies for the war loop to consult and by adding real ally hosts to a real battle. Three honest caveats. (1) The thresholds and penalties (WAR_PENALTY=6, IDENTITY_WARMTH=1, PACT/ALLIANCE form/break lines) are hand-authored magnitudes — the DESIGN is authored, the DISTRIBUTION (which pairs pact or ally, and when) emergent from their history; a different curve moves the thresholds, not the qualitative story. (2) Treaties LAPSE on succession rather than actively transferring-then-breaking: a cold heir simply does not inherit the pact (the honest, M4.3-consistent model — "the crown, not the loyalty" — but it means the succession composition is a lapse, not a live betrayal by the heir; a stance-souring betrayal between two living kings IS a logged betrayal event). (3) Fully organic runs rarely leave two sovereign kingdoms coexisting long enough to accumulate a rich diplomatic history (the M3.6 loop tends to resolve neighbours by conquest into one empire), so the pacts/alliances are shown in the honestly-labelled constructed `verify_m413.py`; the machinery (stance recompute, the war-loop pact/ally checks) runs end-to-end under `--diplomacy`. Compose/boundaries: ZERO LLM and ZERO RNG (stance is integer math over sorted pairs; verified two on-runs byte-identical and `{decision:0, strategy:239}` identical on vs off); `--diplomacy` off byte-identical to the empire baseline (never called; the war loop's pact/ally checks are gated; `create_world` clears the diplomacy state); `--diplomacy` implies `--kingdoms` (pair with `--empire` for the war-prevention teeth). Pairwise stances, active pacts and alliances show in a new DIPLOMACY readout; the data is exposed for M4.14/M4.15 and a future renderer (alliance/hostility lines between capitals). `diplomacy.py` imports world and lazily imports empire/culture/religion (the systems it reads — no load-time cycle); `empire.py` gained two gated hooks (the war-loop pact/ally check in `update`, the record-war + ally-host augmentation in `wage_war`), both no-ops when diplomacy is off. **266 tests green** (261 + 5 new, minimal per guidance: stance derived from war/culture + decay; pact prevents an M3.6 war and breaks on souring; alliance adds the ally's host to defence; lapsed-honour ally fails to answer; off == byte-identical + zero LLM). Levers: WAR_PENALTY / IDENTITY_WARMTH / STANCE_DECAY (how history moves stance), PACT/ALLIANCE form & break thresholds, HONOUR_AT (how soured an ally must be before it abandons a friend).

Arc 5 opens: kingdoms now have a second verb. War remains, but around it grows a web of stance, pact and alliance that can prevent it, widen it, or betray it — the seed of an interstate SYSTEM that trade (M4.14) and coalitions (M4.15) will thicken into real geopolitics.

## V2 / M4.14 — Trade routes & interdependence: commerce and the peace it may buy

The historical step: M4.13 gave kingdoms stance + treaties. M4.14 (new module `intertrade.py`) adds a THIRD verb — TRADE. Peaceful neighbours with complementary surplus and need trade across the border (both enrich), trade WARMS the pair's stance (closing the feedback seam M4.13 left), and WAR SEVERS trade (an economic cost of war beyond casualties). Together these let the sim TEST the commercial-peace hypothesis — do interdependent kingdoms fight less? — with the answer required to EMERGE from the warming + severance feedback, never designed, and reported honestly. Scope held to trade + trade→stance + war-severs + the measurement; anti-hegemon coalitions are M4.15, stated as a boundary. It reuses the M2.3 price machinery lifted to the realm level; zero LLM.

INTER-KINGDOM TRADE (reuses M2.3). Each turn `intertrade.update`, for every pair of NEIGHBOURING sovereign kingdoms (reusing `empire._kingdom_neighbours`) whose stance is not hostile, checks complementarity: the crown with more exportable food (its treasury `stockpile`) SELLS to the crown whose realm has more NEED (its hungriest member's hunger) at the emergent M2.3 price — `(buyer_value + seller_reservation)/2` using `economy.FOOD_VALUE_COEF` × need and `economy.food_reservation` (which falls as a larder fills). Food flows to the buyer's granary, money to the seller — both strictly better off (the price sits between reservation and value, guaranteeing mutual surplus). Per-pair cumulative TRADE VOLUME is recorded in `world_state["intertrade"]["volume"]` and the pair is marked an active ROUTE. TRADE WARMS STANCE (M4.13's seam, closed): `diplomacy._recompute_stance` reads this module's active routes and warms a trading pair toward friendly exactly as shared kinship does — so sustained commerce trends a neutral pair into a pact/alliance, diplomacy emerging from economics. WAR SEVERS TRADE: `record_war` now applies a WAR_PENALTY raised to 10 — large enough to turn even a maximally trade-warmed pair (capped at +6) HOSTILE — so the next `intertrade.update` finds the route gone and logs a distinct SEVERANCE; both crowns lose the flow the route was generating. THE MEASUREMENT: cumulative volume per pair (here) and war count per pair (`diplomacy.war_count`) are exposed so a run can compare war frequency among heavily-trading vs isolated pairs.

**Trade enriches both (HEADLINE 1).** A food-rich kingdom (treasury food 18) beside a food-poor, hungry one (member hunger 8, holding coin): over four turns the poor realm's granary fills (0 → 8) while the rich realm's treasury grows (5 → 38), 33 units of value flowing across the border at the emergent price — both better off. A hostile pair (at war) trades nothing.

**Commerce builds relationships; war severs trade (HEADLINE 2).** Seven trading turns warm the pair's stance monotonically (1,2,3,4,5,6,6) from neutral into friendly and a pact forms — diplomacy emerging from economics. Then the two partners go to war: the trade route is SEVERED (logged) and both lose the commerce. War now costs the wealth and food the route was generating, beyond its casualties.

**The commercial-peace test (HEADLINE 3 — emergent, measured not forced).** A multi-kingdom setup with a TRADING pair (strong Rich beside weak, hungry Poor — complementary, so they trade) and a structurally-IDENTICAL ISOLATED pair (strong Warlord beside weak Victim — but no complementary surplus/need, so no route ever forms). Phase one (commerce): Rich↔Poor form a pact from trade; Warlord↔Victim do not. Phase two (the war loop runs for both): **the measured war frequency was 0 wars between the trading pair (Poor stays sovereign — the pact commerce built prevented the war) and 1 war for the isolated pair (Victim was conquered).** HONEST RESULT: in this run interdependence DID deter war — and it is EMERGENT, from trade→stance warming plus the M4.13 pact reading, not hardcoded (the two pairs are structurally identical; the only difference is whether they could trade). And it composes end to end (COMPOSE): the same strong-Rich-vs-weak-Poor pair is conquered with no prior commerce but stayed once trade has built the pact — economics → diplomacy → a war that never fires.

The honest judgment (the result of M4.14): **trade genuinely ENRICHES both kingdoms (the buyer's granary fills and the seller's treasury grows, at the reused M2.3 emergent price, only between peaceful realms); commerce genuinely WARMS relations into a pact and war genuinely SEVERS the route (both lose the flow); and the sim's measurement shows — in the verified run — that interdependence DETERRED war, emergent from the warming + severance feedback rather than scripted.** No piece is hardcoded: the price is the M2.3 midpoint over realm-level value and reservation, the warming is the M4.13 stance engine reading an active route, the severance is the pact/route dropping when a war cools the pair past hostile, and the peace is the UNCHANGED M4.13 pact-check in the M3.6 war loop — the commercial-peace outcome falls out of these rules, and the two structurally-identical pairs differing only in tradeability is the clean control that proves it. Four honest caveats. (1) The commercial-peace result is reported as MEASURED, not asserted as universal: it holds in the verified run because the trading pair reaches a pact before the aggressor's strike, but a strong neighbour that conquers on turn one — before commerce can build the pact — would win anyway; the sim CAN show deterrence, and here does, but the outcome genuinely depends on whether trade-built peace outpaces opportunistic war (which is the real, honest finding, not a guaranteed law). (2) Trade is modelled crown-to-crown (the king's treasury as the realm's exportable stock / the hungriest member as the realm's need) rather than a full multi-agent cross-border market — a deliberate realm-level simplification that reuses the M2.3 price rule without a new market; distributing the imported food to the realm's members is left to the existing levy/redistribution and draw-down. (3) The magnitudes (WAR_PENALTY=10, INTERTRADE_QTY, the warmth/decay rates) are hand-authored — the DESIGN is authored, the DISTRIBUTION (which pairs trade, warm, pact or fight) emergent; raising WAR_PENALTY to 10 also cools M4.13's post-war stances harder, which is consistent (a war is a bigger rupture than M4.13 first tuned) and left all 266 prior tests green. (4) As at M4.13, fully organic runs rarely leave several sovereign kingdoms coexisting long enough to build rich trade networks (the M3.6 loop tends to conquer neighbours into one empire), so the measurement is shown in the honestly-labelled constructed `verify_m414.py`; the machinery (trade, warming, severance, the readout) runs end-to-end under `--intertrade`. Compose/boundaries: ZERO LLM and ZERO RNG (trade is deterministic price math over sorted neighbour pairs — verified two on-runs byte-identical and `{decision:0, strategy:239}` identical on vs off); `--intertrade` off byte-identical to the diplomacy baseline (never called; no route warms stance; `create_world` clears the trade state); `--intertrade` implies `--diplomacy` (→ `--kingdoms`; pair with `--empire`). Per-pair volume, active routes and war count show in a new TRADE & INTERDEPENDENCE readout. `intertrade.py` imports world + economy (the M2.3 price machinery) and lazily imports empire (the neighbour read) + diplomacy (stance) — no load-time cycle; the only changes to other modules are diplomacy's route-warming read and its war tally (both no-ops when the flags are off). **271 tests green** (266 + 5 new, minimal per guidance: trade enriches both / blocked when hostile; trade warms stance into a pact; war severs trade; the interdependence measurement is exposed; off == byte-identical + zero LLM). Levers: INTERTRADE_QTY (caravan size), the WAR_PENALTY vs IDENTITY_CAP balance (how much commerce can cushion — or not — a war), and the M4.13 pact/alliance thresholds trade now feeds.

Arc 5 deepens: kingdoms now have three verbs — war, treaty, and trade — and the interstate system has a real economy woven through it. Commerce can buy peace, war can bankrupt it, and the sim can be ASKED whether interdependence deters conflict, answering from emergent dynamics rather than a designer's thumb. M4.15's coalitions will add the fourth verb: the many banding against the one.

## V2 / M4.15 — Coalitions & the balance of power: the many band against the one (Arc 5 CLOSED)

The historical step: kingdoms had three verbs — war (M3.6), treaty (M4.13), trade (M4.14). M4.15 (new module `coalitions.py`) adds the fourth — COALITION. When one power grows dominant, the weaker powers band together against it out of FEAR, even across old hostilities and cultural lines (the enemy of my enemy) — the anti-hegemon dynamic that keeps any single empire from swallowing the world. When the threat passes the coalition dissolves, old grievances resurface, and the field clears for the next would-be conqueror, so history churns rather than settling. This is the capstone of the interstate system and CLOSES Arc 5. Scope held to dominance detection + fear-driven formation + coalition action + dissolution + the balance-of-power measurement, reusing M4.13's mutual defence and the M3.6 host/battle machinery; zero LLM.

DOMINANCE DETECTION (emergent). A sovereign power's strength is the number of settlements it CONTROLS — its own realm plus its subject-kings' realms, recursively (`controlled_settlements`). `dominance(state)` returns the strongest sovereign as a HEGEMON iff its share of all settlements clears `HEGEMON_SHARE`=0.4 AND it towers `HEGEMON_MARGIN`=1.5× over the next-largest power (peer-less dominance) — both documented, tunable. FEAR-DRIVEN COALITION (the key mechanic): when a hegemon exists, `coalition_members` is EVERY other sovereign king — membership is driven by fear and OVERRIDES ordinary stance, so mutually hostile or culturally foreign kingdoms coalesce alike. Recorded in `world_state["coalitions"] = {target, members}`. COALITION ACTION through the existing machinery: DEFENCE — `empire.wage_war` (extended) adds every other member's loyal host to a member the hegemon attacks (`coalition_backers`), so it cannot pick them off one by one, and `empire.update` suspends a war between two coalition members (`allied_against_hegemon` — fear suspends the feud); JOINT ATTACK — `coalitions.update`, when the pooled member hosts out-field the hegemon, assaults it with the SAME `monarchy.resolve_battle`, and on victory `_fragment` BREAKS the hegemon (frees every subject-king via the M3.6 mechanism, secedes every vassal via M4.5's `kingdoms.secede_settlement`), dropping it below the threshold. DISSOLUTION: once no hegemon remains the coalition dissolves — the untouched M4.13 stances resurface and former allies may fight again.

**Dominance summons a coalition (HEADLINE 1).** A Hegemon controlling 60% of the world summons a coalition of the two weaker powers A and B — which are mutually HOSTILE (stance −6) yet both join, fear overriding grievance. A balanced world (no dominant power) summons none.

**The coalition checks the hegemon (HEADLINE 2).** The Hegemon (host 3) beats A (2) and B (2) each individually, but the pooled coalition (A+B = 4) DEFEATS it: the empire is broken, its subject-kings freed. A power that could conquer each kingdom one-by-one is torn down by the pooled host of all it threatened — and its mutual defence stops it picking members off singly.

**Dissolution & the churn (HEADLINE 3).** Once the hegemon is broken the coalition target goes to None (dissolved), the old A↔B grievance resurfaces (stance still hostile), and their feud is no longer suspended by fear (`allied_against_hegemon` → False) — free to reignite. Marriages of convenience end; history does not settle.

**The balance of power (HEADLINE 4 — the capstone, emergent).** Five kingdoms packed within reach, one a shade stronger, run through the full war loop for 24 turns: with coalitions OFF a hegemon runs away to a final dominance share of 60% (and holds it — one power dominant); with coalitions ON, every bid for dominance summons the coalition that breaks it, so the final share is knocked back to 40% (`off_final 60% > on_final 40%`). MEASURED RESULT: with coalitions on, one empire swallows a materially smaller share of the world — the interstate system SELF-BALANCES, no would-be world-conqueror safe from the fear it inspires. It is emergent from dominance → coalition → fragment, not forced; and it composes with M3.6 (empires now face BOTH internal fragmentation from overreach AND external coalitions).

The honest judgment (the result of M4.15 and the CLOSE of Arc 5): **dominance genuinely SUMMONS a fear-driven coalition that OVERRIDES old grievances (two mutual enemies provably band together against a common hegemon); the pooled coalition genuinely CHECKS a hegemon none could face alone (a power that beats each kingdom individually is broken by their combined host); the coalition genuinely DISSOLVES when the threat passes (so old rivalries resurface and history churns); and the world reaches a measured BALANCE OF POWER where coalitions keep a single empire from swallowing everything.** No piece is scripted: the hegemon is the argmax of controlled settlements crossing documented thresholds, membership is every weaker sovereign (fear, not stance), the joint attack is the UNCHANGED M3.6 battle math over a pooled host, the fragmentation reuses the M3.6/M4.5 machinery, and the balance-of-power outcome falls out of the ON-vs-OFF difference in a structurally-identical run. Three honest caveats. (1) The balance result is reported as MEASURED, not universal: in the verified 5-kingdom run the coalition knocks the peak-60% hegemon back to 40% (a real, material check) — but the magnitude depends on the setup (thresholds, host sizes, how fast conquest outruns coalition formation); the sim CAN show self-balancing and here does, but a hegemon that conquers everyone before the coalition can pool (leaving no sovereigns to coalesce — as an over-short staged run shows, a 100% hegemon with an empty coalition) wins anyway. That dependence is the honest finding, not a guaranteed law. (2) The joint attack breaks the hegemon by FRAGMENTING it (freeing its subjects/vassals) rather than a coalition member absorbing it — a deliberate choice (a coalition has no single victor; it dismantles rather than replaces the hegemon), which is why knocking one down clears the field rather than crowning a new one immediately. (3) The magnitudes (HEGEMON_SHARE=0.4, HEGEMON_MARGIN=1.5, whole-world membership) are hand-authored — the DESIGN is authored, the DISTRIBUTION (who dominates, who coalesces, whether the check holds) emergent; and as at M4.13/M4.14 fully organic multi-kingdom coexistence is rare, so the headlines run in the honestly-labelled constructed `verify_m415.py` while the machinery (dominance, formation, joint attack, dissolution, the readout) runs end-to-end under `--coalitions`. Compose/boundaries: ZERO LLM and ZERO RNG (dominance/membership are deterministic reads; the joint attack reuses RNG-free `resolve_battle` — verified two on-runs byte-identical and `{decision:0, strategy:239}` identical on vs off); `--coalitions` off byte-identical to the diplomacy baseline (never called; the war loop never suspends a feud or adds a coalition defender; `create_world` clears the coalition state); `--coalitions` implies `--diplomacy` (→ `--kingdoms`; pair with `--empire`). The hegemon's dominance share and any active coalition show in a new BALANCE OF POWER readout. `coalitions.py` imports world and lazily imports empire/kingdoms/monarchy/diplomacy (the systems it reads — no load-time cycle); `empire.py` gained two gated hooks (the coalition-backer defence in `wage_war`, the internal-war suspension in `update`), both no-ops when the flag is off. **276 tests green** (271 + 5 new, minimal per guidance: dominance detection; fear overrides stance so foes co-ally; the pooled coalition host breaks the hegemon; the coalition dissolves when the threat passes; off == byte-identical + zero LLM). Levers: HEGEMON_SHARE / HEGEMON_MARGIN (how dominant a power must be to trigger the alarm), and the coalition membership rule (world-wide fear vs a proximity restriction).

**Arc 5 CLOSED.** Kingdoms now have four verbs — war, treaty, trade, and coalition — and the interstate system is complete and SELF-BALANCING: powers fight (M3.6), make and break treaties (M4.13), grow interdependent through commerce that may buy peace (M4.14), and, when one grows too strong, unite in fear to tear it down (M4.15). Layered on Arcs 1–4 (the House, the class engine, the inner life, and the march of ages), the model now generates not just a civilization but a WORLD OF CIVILIZATIONS — rising, dealing, trading, and balancing against one another, with no single power able to end history by conquering it all.

## V2 / M4.16 — The Chronicle: the world writes its own history (Arc 6 CLOSED, v3 plan COMPLETE)

The historical step: through fifteen milestones the sim GENERATED history — kings crowned, wars fought, dynasties founded and extinguished, prophets risen, empires balanced. M4.16 (new module `chronicle.py`) makes that history LEGIBLE: the world reads back its own event log and writes itself as a SAGA — recognising the figures who mattered, NAMING their deeds, assembling their HOUSES, and telling the story at a FIDELITY set by whether the age could WRITE (M4.10). It SURFACES what already happened; it invents nothing. This is the capstone that CLOSES Arc 6 and COMPLETES the v3 plan. Scope held to the read-back structuring + the literacy-gated fidelity + a deterministic exported saga + an optional walled-off LLM prose garnish (`narrator.py`), reusing the M4.10 literacy read and the existing `world_state["events"]` log; ZERO added LLM in the structured core.

THE READ-BACK (incremental, read-only). Each turn `chronicle.update(state, turn)` folds any NEW events (a cursor over `world_state["events"]`, each event carrying its OWN turn parsed from its `turn N:` prefix so it is robust to batching) into a structured record in `world_state["chronicle"]` — never mutating the sim. Regex classifiers recognise the deed-kinds already logged (conquest/seizure, crowning, succession, extinction, uprising-won/led, prophet, levy, birth, writing-devised, era-advance, faith-founded, hegemon-fall) and thread them onto FIGURES (`{archetype, first/last turn, deeds}`), HOUSES (`house_of` maps kin → dynasty; births and successions grow the generations and crown-count; extinction sets the fate) and named EVENTS. THE EPITHET IS A FUNCTION OF THE DEEDS (nothing assigned): `epithet(fig)` derives deterministically — a prophet is *the Prophet*, an uprising-leader *the Liberator*, a deposed levier *the Grasping*, ≥CONQUEROR_MANY=2 conquests or a war *the Conqueror*, ≥GRASPING_LEVIES=3 levies *the Grasping*, a long fair reign (≥LONG_REIGN=15) *the Just*, a crowned heir *the Crowned* — so "Rex the Conqueror", "Cyn the Liberator", "Vlad the Grasping", "Ada the Just" are each EARNED, recomputable from the record alone. HISTORY REQUIRES WRITING (the fidelity gate): `_fidelity(state, sid)` marks an event **history** if its seat settlement is literate (M4.10 `writing.is_literate`), else **legend** — a preliterate deed keeps its shape but LOSES its names ("a forgotten war", "a lost conquest"); the same deed after writing is named history ("Rex's Seizure of S001"). `literacy_dawn` (the turn writing is first devised OR the first history-fidelity record) headers the saga, and `export_markdown` renders The Great Figures / The Houses / The Saga as deterministic markdown. THE OPTIONAL GARNISH, WALLED OFF: `narrator.narrate_saga` can render the same entries as LLM prose (`--narrate`), cached, with a structured-detail fallback — but it reads the finished chronicle and NEVER writes back, so the structured history is byte-identical whether or not the narrator ever runs.

**Figures recognised from deeds (HEADLINE 1 — the epithet is derived, not assigned).** A probe logging four independent careers — a serial conqueror, an uprising leader, a serial levier deposed by revolt, and a long peaceful reign — yields, from the deed records alone, **Rex the Conqueror [conqueror], Cyn the Liberator [revolutionary], Vlad the Grasping [conqueror-archetype tyrant], Ada the Just [dynast]**: four different epithets, each a pure function of what the figure DID, recomputed identically on every run.

**Events & houses assembled from the records (HEADLINE 2 — nothing invented).** From a real birth/succession/extinction stream the chronicle assembles the named events ["Rex's Seizure of S001", "the Crowning of Aldo", "the Crowning of Bran", "the End of the House of Bran"] and the **House of Rex — 3 generations, 2 crowns passed, kin (Aldo, Bran, Rex), fate: the line was extinguished** — founder, three generations, two crowns, and the line's end, every part traceable to a logged event.

**The saga reads as history and MATCHES the run that wrote it (HEADLINE 3 — deterministic, cross-checked).** A staged war-and-dynasty run (a conqueror Aldric who forges an empire, dies of old age at t=10, and is succeeded by his heir Eirik) produces a saga whose every named chapter cross-checks 3/3 against a real event in the log, and two seeded runs write the byte-identical saga. And the fidelity gate is shown directly: **the SAME conquest is "a lost conquest" [legend] before writing and "Rex's Seizure of S001" [history] after it** — prehistory's names dissolve into legend; literate ages keep theirs.

The honest judgment (the result of M4.16 and the CLOSE of Arc 6): **the chronicle genuinely RECOGNISES the emergent figures of a run from their recorded deeds (the epithet is a derived function of what each did, never assigned), NAMES the events and assembles the HOUSES purely from the real event log (founder, generations, crowns, extinction — all traceable), and writes a saga that is DETERMINISTIC and MATCHES the run that produced it (nothing invented); and the fidelity is genuinely gated on WRITING, so the same deed is nameless legend in a preliterate age and named history once the age can write.** No piece is fabricated: every figure, house, and chapter is derived by reading `world_state["events"]` — the log the previous fifteen milestones already wrote — and the only authored parts are the epithet thresholds and the classification patterns, both pure functions of that log. Four honest caveats. (1) The chronicle is a READER, not a generator — it adds no new history, so its richness is exactly the richness of the run beneath it (a dull run yields a thin saga); it makes the existing dynamics LEGIBLE rather than creating drama, which is precisely the intended scope of "the world writes its own history". (2) The epithet catalogue and its thresholds (CONQUEROR_MANY=2, GRASPING_LEVIES=3, LONG_REIGN=15) are hand-authored — the DESIGN of what earns a name is authored, but the ASSIGNMENT (who gets which) is emergent from each figure's deeds; a figure with mixed deeds resolves by a fixed priority order, so the naming is deterministic rather than "correct", and two conquerors are both "the Conqueror" regardless of scale. (3) The classification is regex over the event STRINGS the sim already emits, so it is coupled to those phrasings (a reworded event would need a reworded pattern) — a deliberate trade to avoid touching the fifteen emitting call-sites; unrecognised events are simply not chronicled rather than mis-chronicled. (4) As with the interstate milestones, the richest sagas come from the honestly-labelled constructed `verify_m416.py` staging (organic runs rarely coexist enough powers for a sprawling multi-house epic); the machinery (read-back, figures, houses, fidelity, export) runs end-to-end under `--chronicle` on any run and the sample saga is exported from a real seeded history. Compose/boundaries: ZERO LLM and ZERO RNG in the structured core (the chronicle is deterministic string-processing over the event log — verified two on-runs byte-identical); `--chronicle` off byte-identical to the base run (never called; `world_state["chronicle"]` never built; `create_world` clears the chronicle state and resets the flag); the optional `--narrate` LLM prose layer is proven WALLED OFF (it reads the finished chronicle and leaves the structured records unchanged, so history is identical with or without it). The exported saga (The Great Figures / The Houses / The Saga, headed by whether the age learned to write) prints at end of a `--chronicle` run and to `--chronicle-out FILE`. `chronicle.py` imports world + writing (the literacy read) and re-uses only the existing event log; `narrator.py` imports the chronicle read-only and the LLM provider; the only change to other modules is main.py's gated per-turn `chronicle.update` call (a no-op when the flag is off). **281 tests green** (276 + 5 new, minimal per guidance: the archetype+epithet are derived from deeds; events and houses are assembled from records; the saga is deterministic under a seed; the chronicle is read-only and off==byte-identical; the narrator is walled off from the structured chronicle). Levers: the epithet catalogue + thresholds (what earns a name), and the fidelity rule (per-settlement literacy vs a global gate).

**Arc 6 CLOSED — the v3 plan is COMPLETE.** The civilization is now LEGIBLE to itself. Over six arcs the model grew from bartering agents into a world of civilizations — the House and its inheritance (Arc 1), the class engine of discontent, uprising and the revolutionary (Arc 2), the inner life of belief, religion and culture (Arc 3), the march of writing, metallurgy and eras (Arc 4), and the interstate system of war, treaty, trade and coalition (Arc 5) — and now, in Arc 6, it READS ITSELF BACK as history: recognising its great figures, chronicling its houses, and telling its own saga at a fidelity its own invention of writing determines. The world doesn't just run; it remembers, and it writes down what it remembers.

## V2 / M5.1 — Minds at the pivots: great figures gain souls at the moments that matter (OPENS Phase 5)

The historical step: for thirty-six milestones the institutional layer — war, breakaway, revolt, treaties, coalitions — has been pure, deterministic STATE MATH, and that determinism is exactly why every A/B finding in the whole project is credible. M5.1 (new module `mind.py` + a new `llm.get_inclination` primitive) adds CHARACTER at the margins WITHOUT breaking that physics. At a handful of PIVOT POINTS where the math's verdict is a near-tie — where material conditions genuinely leave the outcome UNDECIDED — the great figure's MIND is consulted and TILTS the result. Decisive situations stay pure math: a mind is NEVER consulted when the odds are overwhelming. Character decides only what the world left undecided. This OPENS Phase 5 (on top of the complete v3 plan, Arcs 1-6, tagged v4.0). Scope held to three pivots (a clean seam invites more later): an opportunistic WAR (M3.6 launch check), a vassal/subject-king BREAKAWAY (M3.5/M3.6 loyalty check), a settlement's RISING (M4.5 trigger check).

THE CLOSE-MARGIN BAND (the verifiability spine, the load-bearing design). At each pivot the caller computes its exact deterministic verdict AND a signed MARGIN (distance from the threshold; positive = the math leans to ACT). If `|margin| > BAND` the math stands UNTOUCHED and the mind is never even consulted (no mind talks a 3-host king into attacking a 12-host coalition; no contented town is talked into revolt). If `|margin| <= BAND` the figure's INCLINATION (`-1..+1`) tilts the effective margin (`eff = margin + inclination * TILT`) and MAY flip the outcome. BANDs are documented and tunable per pivot (`WAR_BAND=2` host-units, `BREAKAWAY_BAND=1` trust-unit, `UPRISING_BAND=3` pressure-units; `TILT = BAND + 0.5` so a wholly decisive character can flip even a band-edge case while a mild temperament rarely overturns a real material lead). THE MIND: `llm.get_inclination(prompt)` mirrors the v1 strategy-layer provider abstraction — the prompt is the figure's PERSONALITY (personality.py traits) + BELIEFS (M4.7, a small nudge) + the situation + the question; the answer is an inclination in `[-1, 1]` plus a REASON. Consults are CACHED per `(figure, situation-signature)`; a malformed/absent response degrades to inclination 0.0 (NO tilt — the math's verdict stands), never a crash. THE ABSOLUTE CONSTRAINT (met): under `AICIV_PROVIDER=random` (every test and verify) the inclination is a DETERMINISTIC, personality-weighted STAND-IN computed in mind.py and carried through `get_inclination` (whose random backend reads it from a `DISPOSITION:` marker in the prompt, exactly as `_random_decision` reads "food is North") — it consumes NO global RNG, so the institutional layer stays RNG-free, two minds-on runs are byte-identical, and with `--minds` OFF every pivot uses its exact deterministic verdict and writes no `mind_*` state, so an off run is byte-identical to v1 everywhere.

**The band binds absolutely (HEADLINE 1 — decisive cases are never consulted).** Two staged kingdoms, run through the real `empire.update`: a DECISIVE standoff (host 8 vs 3, margin +5, outside the band) launches war IDENTICALLY minds-on and minds-off and consults ZERO minds; a CLOSE standoff (host 5 vs 5, a dead tie, inside the band) launches NO war under the pure math (5 does not beat 5) but a bold king's mind tilts it INTO war minds-on. The overwhelming case is untouched; only the near-tie can differ — the band is the guarantee that the physics still rules everything that is not genuinely marginal.

**Character tilts the undecided (HEADLINE 2 — offline personality stand-in).** Two kings in the IDENTICAL close war (their host 5, the enemy's 5), differing ONLY in personality: the COMPETITIVE king MARCHES (disposition +1 → tilts the tie into war) and the CAUTIOUS king REFRAINS (disposition −1 → holds) — driven end-to-end through `empire.update` by the deterministic, offline, zero-RNG stand-in. Identical material odds; personality alone decides. And it flips both ways: with a slim 6-vs-5 lead the cautious king OVERRIDES the math's go and holds, while the bold king presses on.

**Motives enter the written history (HEADLINE 3 — the payoff).** A pivot decision records its REASON, and the chronicle (M4.16) surfaces the WHY in the saga: a real staged run writes **"Turn 3 — Bold's Conquest of Slim's Kingdom: KING Bold defeated Slim and forged an empire, saying 'the odds were even and fortune favours the bold'."** History now records not just WHAT figures did but WHY — motives enter the record. Offline the reasons are deterministic templates derived from the figure's leaning; online they are the model's own words (walled off exactly like `--narrate` — prose only, no new physics). All three pivots are wired off one seam: the same tilt flips a close BREAKAWAY (a proud, independent vassal at borderline loyalty breaks; a loyal, cautious one endures) and a close RISING (a daring firebrand raises the banner at the trigger; a patient one waits).

The honest judgment (the result of M5.1 and the OPENING of Phase 5): **the mind decides ONLY what the material world leaves undecided — the band binds absolutely (a decisive war is byte-identical minds-on vs off and consults no one; only a near-tie can change); character GENUINELY tilts close calls, offline and on (two kings differing only in personality reach opposite decisions in the identical standoff, through the real institutional code path); and motives now GENUINELY enter the written history (the saga records the figure's stated reason).** No piece is scripted or leaky: the verdict outside the band is the caller's untouched deterministic result, the tilt inside the band is `margin + inclination·TILT` with the inclination a pure function of typed personality (+ a small belief nudge), and a neutral or malformed inclination reproduces the deterministic verdict exactly (so the math is the strict fallback, never overridden by accident). Four honest caveats. (1) The offline tilt is a personality-weighted STAND-IN, not reasoning — it maps traits to a boldness-vs-restraint scalar; it is deliberately DETERMINISTIC (so tests/verifies and the byte-identical guarantees hold) and is itself the feature ("character at the pivots works offline; a live model upgrades it to genuine reasoning"). The claim is NOT that offline figures "think", only that their CHARACTER consistently colours the marginal calls. (2) The BANDs and TILTs are hand-authored magnitudes — the DESIGN of how wide "undecided" is, is authored; the DISTRIBUTION (which figure faces a close call, and which way their nature tilts it) is emergent from the run's material state and the figures' seed-derived personalities. Set the bands to 0 and M5.1 vanishes into pure v1 math; widen them and character reaches deeper — a single, legible lever on how much soul the margins have. (3) The three pivots are wired at their exact deterministic sites (a strict-`>` war launch, a `<= floor` loyalty check, a `>= threshold` rising) by passing the caller's own `base_act` in, so OFF/out-of-band is byte-identical to the pre-M5.1 boundary regardless of `>`/`>=` subtleties; more pivots (treaty-breaking, succession disputes) are a clean addition on the same seam, deliberately not built here. (4) As with the interstate milestones, organic runs seldom land a pivot exactly in the band, so the headlines and the cost measurement run in the honestly-labelled constructed `verify_m51.py` (a 4-kingdom close cluster) — but the machinery runs end-to-end under `--minds` on any run, and a measured cluster shows **6 pivot consults (3 flipping a close call) and 6 inclination requests** — single-digit, cached per (figure, situation), the honest cost a live provider would pay. Compose/boundaries: ZERO added RNG (the stand-in draws no random number; the institutional layer stays RNG-free — two minds-on runs verified byte-identical) and ZERO live LLM offline; `--minds` off byte-identical to v1 everywhere (the pivots use their exact verdicts, `create_world` clears the consult cache / consult log / motives and resets the flag, and the chronicle's motive clause is empty when minds are off); the live LLM path exists only with a real provider and is walled off like `--narrate`. `mind.py` imports llm and lazily reaches strategy (the personality cache) / beliefs / the pivot modules — no load-time cycle; the only changes to other modules are the three gated pivot hooks (empire war-launch + fragmentation, kingdoms breakaway, uprising trigger — each a no-op when the flag is off), `llm.get_inclination` (+ the offline stand-in + an `inclination` call counter), the chronicle's gated motive clause, main.py's flag/wiring/cost-readout, and `create_world`'s clear. **285 tests green** (281 + 4 new, minimal per guidance: the band binds and only close calls are consulted; character tilts a close war offline; the motive enters the written history; minds-off is byte-identical AND a malformed response falls back to no-tilt). The one benign ripple: adding the `inclination` LLM counter changed the exact `get_call_stats()` shape asserted by 12 tests + 12 verify scripts, all updated to `{"decision": 0, "strategy": 0, "inclination": 0}`. Levers: the per-pivot BAND (how wide "undecided" is) and TILT (how hard character pushes within it), and the trait→disposition weighting (what each personality leans toward at each pivot).
