"""
tests
=====

`test_simulation.py` is the suite — one file, an explicit runner list at the bottom, run it
directly (`python3 tests/test_simulation.py`) or as a module (`python3 -m tests.test_simulation`).
Both work from the repo root; the file puts the root on `sys.path` itself.

`verify/` holds the per-milestone verification scripts (`verify_m51.py`, `verify_day14.py`, …).
Each is a standalone DEMONSTRATION of one milestone — it stages a world, runs it, and prints the
evidence a human reads to judge whether the system does what it claims. They are not part of the
suite and are not run by it; they are kept because the milestone write-ups in `docs/FINDINGS.md`
cite their output.
"""
